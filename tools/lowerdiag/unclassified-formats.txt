# tools/lowerdiag/unclassified-formats.txt — the frontend unsup(...) format strings (verbs -> X) that causes.tsv
# does NOT classify from the FORMAT alone. Regenerate: lowerdiag vocabulary tools/nativefrontend (TestVocabularyCoverageIsTracked
# asserts equality, so a new unclassified string surfaces here, never silently). Most rows classify from the REAL text —
# the pattern keys on the argument (`builtin println in statement position`, `sync.Map.Load with 2 arguments`,
# `&composite in short-circuit operand`); the others are frontend invariants that carry another message (emit.go:215 "%s").
# 2026-09-04 round-11 merge train [AGENT]: regenerated after the fr22-fr23 slice landed — FOUR new frontend formats surfaced here (FR-4 stencil-naming, FR-22 whole-export, FR-22 $poisoned reference, FR-24 type-unlowerable);
# classifying them from the format alone (a causes.tsv pattern per row) is OWED to the lower-diagnose lane, not decided at the merge. The real-text paths may still classify (FR-24 is static-only by design).
&composite (elided) in X
&composite in X
X
X with X arguments
builtin X in statement position
interface method substitution produced non-signature for X
map literal in X
method X not found on substituted receiver X
method expression (*X).X over a value-receiver method (deref adapter not modeled)
method stencil X.X does not lower (X) — FR-4: method stencils have no per-declaration quarantine yet, so the export refuses whole
package-level initializer of X does not lower (X) and is not H-11 effect-isolated — skipping it cannot be argued unobservable, so the WHOLE export refuses (FR-22: per-declaration poisoning covers only initializers whose every call is a direct call to an init-callee register row [pureUnmodeledCallees, rendered in docs/stdlib-admission-register.md] with effect- and panic-free argument shapes)
package-level initializer with no declaration site
package-level var X: its TYPE does not lower (X) — FR-24: a reached var of an unlowerable type refuses the whole export (per-declaration `$poisoned` cell + reader quarantine is the queued remedy, ledger FR-24)
package-level var X: its initializer does not lower (X) — H-11 poison: the cell is seeded as the `$poisoned` placeholder and every reference (read, write, address-of) refuses by name; reference refused
slice literal in X
substitution into X (X)
sync.Once.Do argument is not a func()
sync.Once.Do with X arguments
sync.WaitGroup.Add with X arguments
sync.X.X with X arguments
