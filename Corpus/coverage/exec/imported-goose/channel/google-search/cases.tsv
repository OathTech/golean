# id	subject	args	expected_status	expected_reason	features
# tier=slow: certification measured 39,976,295 steps / 94 s single-core
# wall on the dev box at these params (2026-08-14) — the largest slow
# row by far (rwmutex-order is ~26 s). A hosted 2-core runner's slower
# core lands near ~300 s, so under the old shared 300 s enumerator
# timeout this row flipped with runner speed: the 2026-08-13 nightly
# FAIL vs 08-14 PASS on the SAME commit was exactly that. Slow-tier
# re-certification now runs under LEAN_ENUM_SLOW_TIMEOUT_SECONDS
# (scripts/diff-coverage; investigation record:
# docs/2026-08-14_ci-runner-death-and-slow-tier-timeout.md).
-	goleanGoogle	-	ok	-	imported_goose,channels,goroutines,nondet	membership	the three worker sends commit into the cap-3 buffer in scheduler order (L1): the arrival-order code ranges over the 6 permutations of {1,2,3} (members=6); width 4 covers the up-to-4-runnable scheduler bound; no append spill (the results slice is pre-capped at 3); sites raised for the 4-thread tree	width=4,sites=48,work=60000000,members=6,tier=slow
