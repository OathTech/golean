# id	subject	args	expected_status	expected_reason	features
# OUT-OF-LANGUAGE BOUNDARY MARKERS (slice 6): Package_unsafe is
# classified out-of-language in docs/language-coverage-ledger.md (the
# justification lives there); these rows keep the boundary visible
# instead of grey. MARKER-ONLY by logged justification: full
# enumeration of unsafe would pin gc's layout — modeling gc, the
# doctrine's anti-goal. sizeof-const is GREEN (a spec-forced constant,
# folded by go/constant at the frontend — see main.go); the RED
# boundary is pointer-roundtrip ("basic type unsafe.Pointer").
sizeof-const	unsafeSizeofConst	-	ok	-	unsafe,constants
pointer-roundtrip	unsafePointerRoundtrip	-	ok	-	unsafe,pointers
