# Ceilings for unexplained baseline fidelity failures (scripts/check-bugs.sh
# check 4). A count may only go DOWN; raising one requires justification in
# the same commit. Recorded 2026-07-20: 77 lean-observation + 8 differential.
#
# FORMAT (since 2026-08-20, ledger T-5): one `<class> <n>` row per
# disposition class of baselines/untriaged-ids — `coverage`, `latitude`,
# `wrong-answer`, all three required, order irrelevant, unknown or
# duplicated or non-numeric rows fail the gate. Before that date this file
# held a single scalar covering all three classes at once, which is what
# T-5 fixed: refusals of unmodeled constructs (coverage) and spec-open
# points (latitude) can retire neither by a fix nor by a triage, so a
# scalar captioned "ratchet toward 0" was unreachable by construction —
# and a genuine wrong answer could enter behind a frontier refusal that
# left it, moving nothing. Per class, raising `wrong-answer` above 0
# requires an explicit reviewable two-file edit (an untriaged-ids row +
# this file's ceiling, justified in the same commit), whatever the
# frontier rows do. The classes are defined in the header of
# baselines/untriaged-ids; the log below is chronological and its
# pre-2026-08-20 numbers are the old single scalar (kept verbatim — they
# are the record of what was true when written, not restatements in the
# new scheme).
#
# 2026-07-24 (W2 slice 1)  85 -> 86: switch-interface-case-panic advanced.
# 2026-07-25 (W5 lambda lifting)  86 -> 91: five advanced (four
#   non-addressable-base, one fault-identity: func-value interface equality).
# 2026-07-25 (W3 method values)  91 -> 98: seven advanced, all ONE class —
#   struct field writes (BUG-001/W4) — making W4 the highest-value rung.
# 2026-07-25 (W4 slice 1)  98 -> 65: THE PREDICTION PAID OFF — addressable
#   chains + read-modify-write-once + map-compound cleared 33 backlog
#   entries in one slice and CLOSED BUG-001. The remaining 65 split across
#   interfaces/type-assertion machinery, builtins (append/copy/delete/new),
#   strings edge semantics, and the 8 pre-existing differential-stage wrong
#   answers (strings/variadic/interface classes, tracked for triage).
# 2026-07-25 (audit response)  65 -> 64: variadic/variadic-method flipped PASS
#   when method calls gained variadic packing (audit finding 6's fix).
# 2026-07-26 (wrong-answers slice 0)  64 -> 48: the 8 recorded
#   differential wrong answers fixed (7 green) or refused at the
#   boundary, and 8 mis-lowered interface cases moved to
#   frontend-export by the implicit-conversion guard — the first
#   16-point drop; ratchet toward 0 continues.
# 2026-07-26 (audit response)  48 -> 31: the extended interface-
#   conversion guard refuses 18 formerly-lean-observation cases at
#   the boundary (they were mis-lowered raw-representation cases,
#   BUG-006), removing them from the fidelity ledger honestly.
# 2026-07-30 (interfaces campaign, quorum-pilot phase 1)  31 -> 27: two
#   map-key panic cases now conformant (unhashable-key prechecks); two
#   embedding ids folded into BUG-007 (method promotion) with the six
#   newly-visible promotion cases the campaign surfaced.
# 2026-07-31 (interim pre-merge audit response)  27 -> 26:
#   maps/delete-nil-interface-key-panic became conformant — a NIL map still
#   hashes its key in Go, and the hashability precheck now runs on the
#   nil-map paths too. The four NEW red pins from this wave are all
#   explained by BUG-004/007/008, so they do not enter the backlog.
# 2026-07-31 (FINAL pre-merge audit response)  26 -> 26, unchanged. The
#   wave added 26 corpus ids, 5 of them red; every red is explained by an
#   entry (BUG-009 for the two imported-method-set pins, BUG-010 for the
#   package-name collision, BUG-005 for maps/update-during-range) or is a
#   frontend-export refusal pin, which is not a fidelity failure. No
#   pre-existing id changed result or stage, so the backlog set is
#   untouched.
# 2026-08-04 (control-flow slice, stages 1-2)  26 -> 21: the five init/*
#   package-level-variable cases moved to a FRONTEND-boundary refusal
#   (globals fail closed at emitIdent/emitAssignTarget until the init
#   slice) — they were surfacing as machine-level "unbound variable"
#   stucks, a misclassified stage, not a fidelity ledger entry.
# 2026-08-05 (general-coverage slice 2, field promotion)  21 -> 15: six
#   promoted-field cases conformant (frontend flattening of embedded
#   field paths, design note 2026-08-05 D1.1): deep-promoted-field,
#   embedded-pointer-field, embedded-pointer-nil-panic,
#   promoted-field-depth-shadow, structs/anonymous-field,
#   structs/tag-embedded-field.
# 2026-08-05 (general-coverage slice 2, struct value conversions)
#   15 -> 13: identical-underlying struct value conversions conformant
#   (retagged copy in convertValueToTyFuel, design note 2026-08-05 D6):
#   structs/tag-defined-conversion,
#   structs/selector-addressability-edge/read-conversion-result.
# 2026-08-05 (general-coverage slice 4, floats)  13 -> 15: the two
#   EXPECTED-UNSUPPORTED negative pins floats/to-int-out-of-range/
#   {range,nan} (floats design note §3.3, decision 4) moved
#   frontend-export -> lean-observation/unsupported when the frontend
#   learned floats. They are DELIBERATE fail-closed refusals of the
#   spec's implementation-dependent out-of-range/NaN float->int
#   conversion — the red IS the pin (baseline header of the same
#   commit; machine half pinned by the float_to_int_refusal_F eval
#   test) — not untriaged fidelity bugs, and they never flip PASS.
# 2026-08-05 (init slice, guardrails commit)  15 -> 16, TRANSIENT:
#   init/init-defer-recover added red-first (two init() functions export
#   as duplicate FuncId "init" — the same recorded refusal as
#   init/multiple-init-order); it flips PASS in this slice's frontend
#   commit, which lowers the ceiling back.
# 2026-08-05 (init slice, frontend commit)  16 -> 15:
#   init/init-defer-recover and init/multiple-init-order flipped PASS
#   (init() functions export as $initN, called by $pkginit);
#   init/hidden-dep-order ENTERS as a PERMANENT differential-stage
#   deviation record (design note §1: the spec leaves hidden-dependency
#   initialization order unspecified; go/types' conforming order — ours
#   — differs from gc's conforming order on exactly this shape; never
#   expected to flip PASS, re-checked every toolchain bump).
# 2026-08-06 (channels arc slice 1)  15 -> 16:
#   channels/select-multi-ready ENTERS as a DELIBERATE fail-closed
#   refusal pin (design of record D4/slice 4): a select with more than
#   one ready case is the L2 "uniform pseudo-random" latitude — the
#   deterministic slice REFUSES it (unsupported at the readiness step)
#   rather than picking silently; the scheduler arc's slice 4 flips it
#   via the membership-lane choice envelope. Same class as the floats
#   refusal pins: permanently red until its slice lands.
# 2026-08-07 (channels arc slice 2)  16 -> 18: two DELIBERATE red pins
#   enter with the goroutines corpus (same class as select-multi-ready,
#   both recorded in the baseline header of the same commit):
#   goroutines/sched-dependent/first-come (schedule-dependent
#   observable, membership; the schedule enumerator is slice 4) and
#   goroutines/spawn-in-init/in-init (go during $pkginit stays
#   fail-closed while the init phase runs sequentially). The other two
#   goroutines reds (spawn-edge/nil-func-fatal at the go-run stage,
#   spawn-edge/child-recovers at frontend-export) are stage-classified
#   gaps, not fidelity entries.
# 2026-08-07 (S2 audit response)  18 -> 20: the two sched-dependent
#   membership red pins for the audit-major envelope shapes enter
#   (select-default-handshake, len-handoff — see untriaged-ids; same
#   deliberate slice-4 class as first-come). The three other new ids
#   land PASS.
# 2026-08-07 (channels arc slice 3)  20 -> 21: race/litmus/sb-chan
#   enters as a DELIBERATE membership red pin (lane e's DRF-SC
#   boundary shape; slice 4's schedule enumerator certifies its
#   admitted set {1,10,11} and the SC-forbidden 00). The other 12 new
#   race-lane ids land PASS (racy-negative refusals + green edge/
#   granularity guards).
# 2026-08-07 (channels arc slice 4; raise RECORDED IN the BUG-040
#   commit, one commit after the red-first commit it describes — the
#   recorded S4 deviation)  21 -> 24,
#   TRANSIENT: the three L2 multi-ready envelope shapes enter red-first
#   (entry/wake/arrival — see untriaged-ids). The L2 site and the pool
#   enumerator land in this same slice and flip them; the ceiling drops
#   back in those commits.
# 2026-08-07 (channels arc slice 4, L2 movement)  24 -> 22: the L2
#   clause pick went LIVE (applySelect consumes at multi-ready; wake
#   head-commits; arrival .multi analysis) — channels/select-multi-ready
#   (slice-1's refusal pin) and channels/select-multi-ready/observable
#   (this slice's entry-path membership pin) both flip PASS.
# 2026-08-07 (channels arc slice 4, pool enumerator + lanes)  22 -> 16:
#   the six schedule-enumeration red pins flip PASS — sched-dependent/
#   {first-come,select-default-handshake,len-handoff} (certified sets
#   {12,21}/{7,99}/{100,110}), race/litmus/sb-chan (certified {1,10,11},
#   members=3 — the DRF-SC boundary pin), select-wake-multi ({1,2}) and
#   select-arrival-multi ({10,20}). Their untriaged-ids entries retired;
#   the retained comments above them record the history.
# 2026-08-08 (arc-final audit response, F11/F10)  16 -> 17:
#   channels/select-select/core ENTERS as a PERMANENT-until-lifted
#   fail-closed refusal marker: select-with-select rendezvous is
#   refused AT RUN TIME (.unsupported from selectArrivalCases), a
#   machine-boundary refusal the stage taxonomy can only express as
#   lean-observation — a coverage gap (goose/perennial model AND test
#   the capability; matrix S2 row), not a fidelity bug. Its sibling
#   beside-loop (F10's over-refusal trigger: any polled channel whose
#   partner is a select loop) sits at stage nondet, outside this
#   ledger's filter.
# 2026-08-08 (convergence response)  17 -> 18: check-bugs' fidelity
#   filter gains stage `nondet` (the F5 rationale applies verbatim —
#   the previous entry's "outside this ledger's filter" note was the
#   gap, not a justification), so channels/select-select/beside-loop
#   ENTERS the ledger as the same PERMANENT-until-lifted
#   select-with-select refusal marker as its sibling core (F10's
#   over-broad trigger: the default stream commits the cell-ready
#   clause, adversarial streams hit the refusal — stage nondet).
# 2026-08-08 (check-in response round, BUG-048 fix)  18 -> 16: the two
#   long-standing value-receiver-auto-deref backlog reds flip PASS —
#   methods/value-auto-deref and pointers/nil-value-receiver-call-panic
#   were the SAME class as BUG-048 (value receiver through a pointer
#   operand; the frontend now emits the spec's auto-deref), repaired by
#   its fix and folded into its Cases. The buildout's import of
#   unittest/embedded.go re-surfaced the class and forced the triage.
coverage 14
latitude 4
wrong-answer 0
# 2026-08-09 (spec-parity slice 2, sync guardrails)  16 -> 50, mostly
#   TRANSIENT: the 34 fidelity-stage sync-package red pins enter
#   red-first BEFORE the machine/frontend sync work (design note
#   docs/2026-08-09_sync-package-design.md §10; baseline header of the
#   same commit) — 28 sync/* rows minus the frontend-export cond
#   marker, plus 4 race/free-sync and 3 race/negative-sync rows. The
#   machine+frontend commits of this same slice flip the in-scope ones
#   back and lower the ceiling; sync/out-of-scope-trylock stays as a
#   PERMANENT-until-lifted refusal marker (TryLock is the atomics arc's
#   spin-wait enabler, D4/design note §9 — the go-vs-machine surface
#   can only express it as lean-observation, the select-select
#   precedent).
# 2026-08-09 (spec-parity slice 2, machine+frontend flip)  50 -> 16: the
#   34 transient sync guardrail reds resolve — 33 PASS (the sync package
#   is live end-to-end), and sync/out-of-scope-trylock reclassifies to a
#   frontend-export refusal (outside the fidelity stages), keeping only
#   the pre-existing 16-entry backlog.
# 2026-08-18 (spec-truth P3, spec-examples tranche)  16 -> 25: nine
#   coverage-gap reds enter (see untriaged-ids' dated block: slice<->
#   array conversions x4, string<->rune conversions x2, float min/max
#   x3) — features GoCore lacks, surfaced by the spec's own examples,
#   failing closed as unsupported. The tranche's three FIDELITY finds
#   are NOT here: BUG-056 (&*nil), BUG-057 (typed comma-ok receive),
#   BUG-058 (comma-ok assert leaking past export) pin their cases in
#   docs/BUGS.md.
# 2026-08-19 (19-red GoCore slice, families 1-5)  25 -> 7: eighteen
#   retired across five arm-family fixes — rune<->string conversions
#   (L1, 9), slice->array length-check panics (L2a, 3), IEEE float
#   min/max (L3, 3), pointer-to-array indexing read arms (L5+L6, 2),
#   struct-tag-convertible field access (L7, 1). The seven that remain:
#   floats/to-int-out-of-range x2 (R6 latitude refusal),
#   init/hidden-dep-order (permanent deviation record),
#   spawn-in-init (frontier), select-select x2 (frontier),
#   slice-to-array/ok-forms (L2b frontier). Family 6 (go-of-nil-func,
#   L10) is not in this ledger — its red is stage go-run.
# 2026-08-19 (bug-fix arc slice 6, the whole-language bar)  7 -> 9: the
#   sync/atomic FRONTIER suite lands (concurrency-entangled design
#   question Q-ATOMIC, docs/language-coverage-ledger.md) and two of its
#   five red rows surface at FIDELITY stages (lean-observation, membership
#   — both in the check-bugs filter; label corrected at the audit fix
#   round) —
#   atomic-frontier/value (lean-observation: the atomic.Value zero
#   value is a D5 imported-type stub refusal) and
#   atomic-frontier/mp-litmus (membership: the enumerator refuses at
#   the quarantined store). Same class as the select-select and
#   trylock markers: coverage backlog failing closed, never wrong
#   answers; both flip at the atomics arc. The other three atomic rows
#   (add-load-store, cas, swap) refuse at frontend-export, outside this
#   ledger.
# 2026-08-20 (bug-fix arc gate, ledger T-5 — the DISPOSITION SPLIT)
#   scalar 9 -> coverage 6 / latitude 3 / wrong-answer 0. Not a movement:
#   the same 9 ids, the same baseline, the same set check — only the
#   accounting stops conflating three things. Derivation, per id, from
#   docs/2026-08-19_triage-table.md and docs/language-coverage-ledger.md:
#     coverage 6 — goroutines/spawn-in-init/in-init (triage L8: `go` during
#       $pkginit, sequential driver), channels/select-select/{core,
#       beside-loop} (L9: select-to-select rendezvous unbuilt),
#       spec-examples-decl/slice-to-array/ok-forms (L2b: `Loc` has no
#       subarray-view constructor), sync/atomic-frontier/{value,mp-litmus}
#       (ledger Q-ATOMIC). Every one a fail-closed refusal of a NAMED
#       frontier feature; each retires when its arc lands.
#     latitude 3 — floats/to-int-out-of-range/{nan,range} (ratified (c)-row
#       C5: the spec declares the result implementation-dependent and gc's
#       own ports disagree) and init/hidden-dep-order (C1: the spec says
#       the order "is unspecified"; go/types and gc realize two DIFFERENT
#       conforming orders). Retire by an envelope, routed to W3.2 — never
#       by a fix.
#     wrong-answer 0 — the class the ratchet actually exists for is EMPTY
#       and now says so. Any new unexplained forced-point divergence trips
#       check (4) immediately against this floor, whatever the other two
#       classes are doing. Raising this row is the one raise that should
#       never be routine.
# 2026-08-19 (bugfix-arc audit fix round, BUG-063 guardrails)
#   coverage 6 -> 7: pointers/nil-array-ptr-slice/slice-expr-nil ENTERS
#   as an honest-STUCK refusal pin (the slice-base nil arm is
#   unmodeled; the machine refuses visibly where gc panics — BUG-063's
#   lesser sibling, scoped OUT of that fix on purpose: its base
#   position is a store-adjacent emitAddressOf consumer, the five
#   store-order pins' territory). Same class as slice-to-array/ok-forms.
#   The two BUG-063 differential reds are NOT here — they pin on the
#   entry's Cases line.
# 2026-08-19 (audit fix round F1, zero-size address latitude)
#   latitude 3 -> 4: pointers/zero-size-address/escaped-same ENTERS as
#   the R15 deviation record — spec latitude ("may have the same
#   address"), gc probed non-single-valued (stack distinct, heap
#   equal), machine the conforming never-same singleton. Same class as
#   init/hidden-dep-order; retires only by a W3.2 envelope.
# 2026-08-21 (holes-arc audit fix round, finding F1)
#   coverage 7 -> 11: the four
#   pointers/nil-array-ptr-slice-elided-high/* rows ENTER as the
#   ELIDED-HIGH spellings of the class the single
#   nil-array-ptr-slice/slice-expr-nil row (2026-08-19) already tracks —
#   the unmodeled slice-base nil arm, one honest STUCK with one message,
#   retiring together when that arm lands. A raise of 4 for zero new
#   holes, which is the cost of pinning a class by its variants instead
#   of by one witness; the alternative (leave three spellings unpinned)
#   is what let BUG-066 hide the accident in the first place.
#   TWO of the four (deref-elided-high, deref-low-only) were previously
#   ACCIDENTALLY CORRECT — the pre-BUG-066 double emission panicked on
#   the nil deref. That accident is what the fix removed; it never made
#   a right answer wrong, so this is a WIDENED REFUSAL, not a
#   regression, and the movement is recorded on BUG-066's entry too.
#   wrong-answer stays 0 — nothing here is a divergence at a forced
#   point that no entry explains.
# 11 -> 12 (2026-08-21, raft W4.3 landing B): strings/split-conformance/
# empty-sep enters as `coverage` BY DESIGN — the row is the fail-closed
# BOUND PIN of the strings.Split shim (the empty-separator per-rune
# explode needs UTF-8 decoding, outside the modeled subset; the machine
# panics naming the gap where go run explodes). Landed deliberately
# with the feature as its boundary guardrail, not an accidental
# entrant; retires only if the rune explode is ever modeled.
# 12 -> 14 (2026-08-21, raft W4.3 landing C): fmt/sprintf-dyn/
# {unmodeled-kind,arity-mismatch} enter as `coverage` BY DESIGN — the
# dynamic-fmt shims' fail-closed bound pins (an unmodeled dynamic kind
# and a verb/argument arity mismatch are machine panics naming the gap
# where go run renders). Landed deliberately with the feature as its
# boundary guardrails; each retires only if its dynamic arm is ever
# modeled.
