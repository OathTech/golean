# Ceiling for unexplained baseline fidelity failures (scripts/check-bugs.sh
# check 4). The count may only go DOWN; raising it requires justification in
# the same commit. Recorded 2026-07-20: 77 lean-observation + 8 differential.
# Raised 85 -> 86 on 2026-07-24 (W2 slice 1): switch-interface-case-panic
# ADVANCED from frontend-export into lean-observation.
# Raised 86 -> 91 on 2026-07-25 (W5 slice 1b, lambda lifting): FIVE cases
# advanced the same way — the frontend now exports them and the residue is a
# machine-side fail-closed gap, so the failure moved piles rather than
# anything getting worse. All five inspected before raising:
#   arrays/index-eval-order, maps/compound-assign-eval-once,
#   maps/tuple-map-expr-targets, structs/selector-eval-once
#     -> the non-addressable-base gap (taking an address into an array/map/
#        struct VALUE). One class, W4/BUG-001 territory.
#   functions/function-interface-compare-panic
#     -> interface equality on a func value: Go PANICS, we report unsupported,
#        so this is a fault-IDENTITY gap (docs/2026-07-22_fault-model.md), not
#        just missing coverage. Worth its own fix when interfaces land.
# A sixth advanced case was FIXED rather than absorbed
# (variadic/variadic-function-value: named functions as values now lower to
# captureless func values), which is why this is +5 and not +6.
91
