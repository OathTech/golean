# Ceiling for unexplained baseline fidelity failures (scripts/check-bugs.sh
# check 4). The count may only go DOWN; raising it requires justification in
# the same commit. Recorded 2026-07-20: 77 lean-observation + 8 differential.
# Raised 85 -> 86 on 2026-07-24 (W2 slice 1): switch-interface-case-panic
# ADVANCED from frontend-export into lean-observation — the switch desugar
# means the frontend now emits it, and the residue is the machine's
# fail-closed `unsupported` for interface equality (interfaces lane,
# docs/2026-07-24_sequential-coverage-scoping.md). No behavior changed and
# nothing regressed; the same gap simply moved piles, from the frontend's
# to the machine's.
86
