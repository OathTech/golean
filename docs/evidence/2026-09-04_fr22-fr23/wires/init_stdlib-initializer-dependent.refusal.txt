# init/stdlib-initializer-dependent — whole-export refusal (frontend stderr), 2026-09-04
nativefrontend: native frontend unsupported: package-level initializer of maxYear does not lower (package-level var maxDatetime: its initializer does not lower (package-selector call time.Date (package "time" surface not modeled)) — H-11 poison: the cell is zero-seeded and every reference (read, write, address-of) refuses by name; reference refused) and is not H-11 effect-isolated — skipping it cannot be argued unobservable, so the WHOLE export refuses (FR-22: per-declaration poisoning covers only initializers whose every call is a direct call to an init-callee register row [pureUnmodeledCallees, rendered in docs/stdlib-admission-register.md] with effect- and panic-free argument shapes)
exit status 1
