# G0 — the opening kit phase (campaign log)

Charter section: `docs/2026-08-15_gallery-campaign.md` §G0. Owner:
the kit-and-example lane (this lane also owns `Corpus/`, `baselines/`,
`Examples/` for the campaign; it touches no corpus in G0). Format
rules: `INDEX.md`.

Mandatory units: (1) log init, (2) the brick-wp review mapping note,
(3) the named preemptive lifts — P5 setup-loop schema, MapMem,
entry-equation macro completion — each with chartered consumers named,
≥2 existing-example retrofits as fixtures, the P6 rollback in the same
commit series, measured deltas here; (4) kit-surface axiom pins
(`Audit/Kit.lean`); (d, optional) anything further the review
surfaces, under the chartered-consumer test.

## Unit G0.1 — campaign log init (this commit)

- STATUS: done. `docs/gallery-campaign-log/` created (INDEX + this
  file), per the parallel-sub-lanes process amendment (`ea7c689a`).
- The G3 dossier register is NOT enumerated here: the amendment moves
  it to the dossier lane's first commit (`g3.md`). Recorded so the
  original G0 brief's register item has an explicit disposition.
- JC: gate cadence for the two opening docs-only commits — one full
  `scripts/ci` run at the second commit's tree covers both (identical
  build inputs; zero Lean/corpus delta between them) — CLAUDE.md's
  own "do not impose that ceremony on a one-line fix" scoping, logged
  rather than silently assumed. Every code commit gets its own run.
- Gate note: fresh worktree ⇒ no recorded differential run exists;
  this lane is proofs-only, so per the charter the cached full record
  at the merge-base stands and `GOLEAN_ALLOW_NO_DIFF=1` is set
  explicitly (the CLAUDE.md fresh-lane hatch), reported visibly in
  every gate summary. The first corpus-touching successor re-runs
  `--diff`.

## Unit G0.2 — the brick-wp review mapping (this commit)

- STATUS: done. `docs/2026-08-15_brick-wp-promotion-wave-mapping.md`
  — the promotion wave (`deps/brick-wp` `944c555..52f8a6e`, read-only)
  mapped in the slice-2 format: F1/P3 and F3 → build-now (G0 items
  3a/3b and 3c), P5 audit wiring → build-now (item 4), P4 flagship →
  scheduled into G1's first example, P6 rollback → adopted as the
  per-lift completion criterion, F2/F5/F6 + meta-review outcome →
  exists / not-applicable with reasons.
- JC: F5 (factor a conflated tactic) declined — no conflated kit
  member found, no consumer pain; logged as a non-promotion per the
  outcome-section practice this note adopts.

## Unit G0.3a — P5 setup-loop iteration schema (lift commit)

- STATUS: lift landed. `stepFnIter_iterate` + `stepFnIter_iterate_exit`
  in `GoLeanProofs/FuelMeasure.lean` (+65 lines incl. docstrings) —
  the per-example `Nat.strongRecOn` setup induction stated once.
  Chartered consumers: every G1 array-setup candidate (bubble sort,
  selection sort, two-sum, RLE, palindrome, dedup, Kadane, dot
  product, sieve, matrix, stack, queue). Fixtures retrofitted in this
  commit: isort `hIsetup_loop` (`Examples/InsertionSort/Setup.lean`,
  +62/−50 — the per-iteration composite `hIsu_iter` extracted with
  its own docstring, the induction boilerplate deleted) and wordcount
  `wcH_setup_loop` (`Examples/WordCount/HarnessSetup.lean`, +16/−17 —
  it already had its composite, so the retrofit is pure deletion).
  Statements of both theorems unchanged (vestigial `μ` binder kept so
  zero consumer churn); elaboration cost unchanged (both shards
  ~1.2 s before and after).
- HONEST ACCOUNTING: the retrofit itself is near-line-neutral (+8 net
  across the two fixtures, docstrings included); the value is (a) each
  of the ~11 chartered G1 array examples writes a composite + one
  schema application instead of a fresh ~28-line strong induction,
  and (b) the remaining 6 shipped induction copies become deletable —
  the P6 wave, next commits in this series.
- JC: schema lives in `FuelMeasure` (the fuel-arithmetic composition
  kit, beside `stepFnIter_chain`) rather than `StepKit` (one-step
  glue) — placement by kind, matching the module docstrings.
- JC: two schema forms only (`iterate`, `iterate_exit`); the
  family-function/backing-address abstraction the scale-out P5 row
  sketched is NOT lifted — the per-iteration composite differs per
  example (different segment towers), so the honest shared part is
  the induction, and the composite stays example-local as the
  schema's `hstep` hypothesis. Non-promotion of the wider form
  logged per the outcome-section practice.

## Unit G0.3a-P6 — the setup-induction rollback wave (this commit)

- STATUS: done — ALL 9 shipped setup-loop inductions now instantiate
  the schema; zero `strongRecOn` setup copies survive (the P6
  completion test for the chartered family). This commit retrofits
  the remaining 7: isort rebuild (`hrebuild_loop` + extracted
  `hrb_iter`), binsearch (`hsetup_loop` + extracted `hsu_iter`),
  minmax v1 (`sh_loop` + extracted `sh_iter`), reverse v1 (`su_loop`,
  `iterate_exit` form), reverse HarnessV (`su_loopV`, `iterate_exit`),
  minmax HarnessR (`su_loopR`, `iterate_exit`), wordcount HarnessR
  (`su_loopR`). Every statement byte-unchanged; all modules rebuild
  green at their previous cost class (MinMax 3.1 s, BinSearch 8.8 s,
  HarnessV 3.7 s, WC HarnessR 9.5 s — same class as before).
- JC (scope boundary, logged non-retrofit): the same induction SHAPE
  also appears in the copy/swap/test-phase loops (`cp_loopV`,
  `cp_loopR` ×2, `rh_loop`/`tst_loop`, `rv`/`tv` loops) and, in a
  bounded-`∃k`-per-iteration variant, in the subject loops
  (`mh_loop(R)`, `search_loop`, gcd/fib). NOT retrofitted here: the
  copy/swap loops' code is dominated by their call-entry exit towers
  (the induction scaffold is ~12 of ~60 lines), so replacement is the
  brick-wp meta-review's "churn without capability" non-promotion
  class; the subject loops need a third (bounded-step) schema variant
  — a real candidate for item (d), assessed there. The chartered P5
  family (setup inductions) has zero surviving copies, which is the
  lift's completion test.
- FINDING (input stream): a `stepFnIter_iterate_bounded` variant
  (per-iteration `∃k ≤ c`) would cover the subject-loop induction
  family (≥4 landed instances); consumer-backed, deferred to (d).

## Unit G0.3b — MapMem promotion (this commit)

- STATUS: done. `proofs/GoLeanProofs/MapMem.lean` (new, ~430 lines
  incl. the sealed-API contract): the `map[uint64]uint64` analogue of
  `SliceMem` — `mapCells`/`mapVal` vocabulary, the `idxOf?`/`cnt`/
  `setk`/`toEntries` model + model lemmas, `scan_generic`,
  `mapEntryIndex?_toEntries`, `applyStrictOp_mapGet`,
  `mapAssignValue_toEntries`, `snapshot_toEntries`, the `toEntries`
  bridges. Promoted VERBATIM from `Examples/WordCount/Pure.lean`
  (which had flagged the family as a promotion candidate at birth).
  P6 half in the same commit: Pure.lean −346/+17 (the copies deleted,
  a delegation note left); 8 WordCount files consume via
  `open GoLean.MapMem`. Consumers: wordcount (landed, retrofitted) +
  chartered histogram + fibonacci-memo = 3.
- Statements: wordcount headlines byte-unchanged (the moved names
  never appear in headline closures); internal lemma statements now
  spell `GoLean.MapMem.*` constants — same definitions, moved origin.
- Costs: MapMem 2.7–3.1 s standalone; the WordCount tree rebuilds at
  its previous per-shard costs (Pure 0.9 s, EmptyRun 85 s unchanged).
- JC: WordCount's pure counting layer (`bump`/`countsList`/max fold)
  NOT moved — wordcount spec vocabulary, not map machinery; the
  generic-looking `cnt`-vs-`bump` bridge (`setk_cnt_succ`) stays with
  `bump`. Boundary = "what histogram/fib-memo will restate" vs "what
  only wordcount says".
- JC: three internals kept `private` in MapMem (`valueEq_u64`,
  `toEntries_setk`, `snapshot_norm`); the model lemmas made public —
  histogram-class consumers reason about `idxOf?`/`setk` directly.
- NOTE: `linter.unusedSimpArgs` false-flags two load-bearing simp
  args (removal breaks the proofs — verified); suppression + reason
  recorded at the site, matching the example modules' convention.

## Unit G0.3c — entry-equation macro completion (this commit)

- STATUS: done — all 10 landed entry equations are now DERIVED by
  `derive_entry_eq`; zero hand-written entry dances survive (P6).
  [QUALIFIED 2026-08-18, WP arc s2 item 6: "all landed" was true of
  the 10 equations landed AT THIS UNIT; the later strrev and wordfreq
  examples each hand-wrote one (string result defaults, outside the
  quoter — recorded as GAP-WITNESSes in g1.md). The string arm has
  since been added and both are macro-derived; the zero-survivors
  claim holds again at the WP-arc tip.]
  - The 5 flat retrofits: `mmh_entry_eq` (MinMax −21 net),
    `revH_entry_eq` (Reverse −23), `iharness_entry_eq`
    (InsertionSort/Setup −17), `wcH_entry_eq` (HarnessSetup −17),
    `harness_entry_eq` (BinSearch −19).
  - THE DESIGNED EXTENSION: the macro gains an optional 6th ident —
    the program-generic form. `derive_entry_eq thm prog func state
    cont base` emits the state as the record update
    `{ base with heap := …, nextAddr := … }`, preserving the
    record-update structure the σ-generic segment lemmas are stated
    over, so the consumer bridge to `rSt/vSt/wSt base (heap …) na` is
    a STRUCTURAL rfl (the slice-2 §3 recorded show-bridge), never a
    whnf of the program constant. Fail-closed note recorded: a
    base/prog mismatch is refused by the probe (different program on
    the runConfig side) or the final rfl.
  - The 3 program-generic retrofits: `rH_entry_eq`
    (MinMax/HarnessR −7), `revHV_entry_eq` (Reverse/HarnessV −6),
    `rH_entry_eq` (WordCount/HarnessR −7); each consumer carries the
    one `have hst : … := rfl` bridge line; `rProg`/`vProg` moved
    above the invocation (still the module's one program-constant
    site, now shared with the invocation — docstrings updated).
- Consumer churn beyond the bridges (all proof-side; every HEADLINE
  statement byte-unchanged): isort + binsearch headline proofs moved
  the unorm collapse from inside their `hσ` lemma to the rw chain
  (the macro's normalize-at-argument-position convention);
  `isortH_runs`/`hsearch_runs`/`wcH_runs`-consumer now state their
  config at the emitted names (internal statements, defeq content).
- Deltas: example modules net −131 lines; macro +41 (the extension +
  its doctrine). Full proofs build green incl. the in-build Audit
  gate (axiom pins byte-identical) and the statement-TCB sweep.
- JC (elaborator behavior, recorded for the next macro): `rw` with an
  ASCRIBED rfl (`rw [(rfl : a = b)]`) is unreliable — the elaborated
  proof can carry type `a = a`, making the rewrite a silent no-op;
  the reliable forms are a `have h : a = b := rfl` (declared type)
  or restating the runs lemma at the target spelling. Both used here.

## Unit G0.4 — kit-surface axiom pins (this commit)

- STATUS: done. `proofs/Audit/Kit.lean` (new shard, wired into
  `Audit.lean` like the example shards): **81 verbatim
  `#print axioms` `#guard_msgs` pins** — StepKit 23, SliceMem 17,
  MapMem 13, FuelMeasure 18 (every public lemma per each module's
  sealed-API list; `private` internals deliberately unpinned), plus
  all 10 `derive_entry_eq`-emitted entry-equation theorems as the
  macro's fixture pins. Distribution, recorded verbatim from the
  build: 55 × `[propext, Quot.sound]`, 18 × `[propext]`,
  8 × `[propext, Classical.choice, Quot.sound]` (the charter's
  "expect [propext, Quot.sound] or the trio" understated it — a
  `[propext]`-only class exists; recorded as found). Full proofs
  build green with the shard in the gate (337 jobs).
- Pins were GENERATED from the actual build output (a scratch
  `#print axioms` probe file, then transcribed verbatim), not typed
  from expectation — the #guard_msgs then re-verify them in-build.

## Item (d) — anything further: assessed, one candidate DEFERRED

- The review surfaced exactly one further chartered-consumer
  candidate: `stepFnIter_iterate_bounded(_exit)` — the per-iteration
  `∃k ≤ c` variant of the P5 schema, covering the subject-loop
  inductions (landed: `mh_loop`, `mh_loopR`, gcd/fib/search loops;
  chartered: bubble/selection sort's branch-dependent inner loops).
  DEFERRED, not built: each landed consumer needs an index-shifted
  composite (their iteration domains start at `m ≥ 1`) — a retrofit
  wave, not a lift — and the honest trigger is the first G1 example
  that pulls it (§12's consumer rule; the charter's honest-gap
  allowance for (d)). Design sketched here so the pulling example
  starts warm: schema over `j` with `T j := state (j+1)`, bound
  `c·(n−i)+e`, exit hypothesis as in `iterate_exit`.

## G0 CLOSED (pending the final gate at this tip)

All four mandatory units done + (d) dispositioned. The flagship rule
(G0 done-item 4) discharges inside G1 by charter design: G1's first
example is the kit's integration exercise.
