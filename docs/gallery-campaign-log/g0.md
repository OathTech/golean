# G0 — the opening kit phase (campaign log)

Charter section: `docs/2026-08-15_gallery-campaign.md` §G0. Owner:
the kit-and-example lane (this lane also owns `Corpus/`, `baselines/`,
`Examples/` for the campaign; it touches no corpus in G0). Format
rules: `INDEX.md`.

Mandatory units: (1) log init, (2) the brick-wp review mapping note,
(3) the named preemptive lifts — P5 setup-loop schema, MapMem,
entry-equation macro completion — each with chartered consumers named,
≥2 existing-example retrofits as fixtures, the P6 rollback in the same
commit series, measured deltas here; (4) kit-surface axiom pins
(`Audit/Kit.lean`); (d, optional) anything further the review
surfaces, under the chartered-consumer test.

## Unit G0.1 — campaign log init (this commit)

- STATUS: done. `docs/gallery-campaign-log/` created (INDEX + this
  file), per the parallel-sub-lanes process amendment (`ea7c689a`).
- The G3 dossier register is NOT enumerated here: the amendment moves
  it to the dossier lane's first commit (`g3.md`). Recorded so the
  original G0 brief's register item has an explicit disposition.
- JC: gate cadence for the two opening docs-only commits — one full
  `scripts/ci` run at the second commit's tree covers both (identical
  build inputs; zero Lean/corpus delta between them) — CLAUDE.md's
  own "do not impose that ceremony on a one-line fix" scoping, logged
  rather than silently assumed. Every code commit gets its own run.
- Gate note: fresh worktree ⇒ no recorded differential run exists;
  this lane is proofs-only, so per the charter the cached full record
  at the merge-base stands and `GOLEAN_ALLOW_NO_DIFF=1` is set
  explicitly (the CLAUDE.md fresh-lane hatch), reported visibly in
  every gate summary. The first corpus-touching successor re-runs
  `--diff`.

## Unit G0.2 — the brick-wp review mapping (this commit)

- STATUS: done. `docs/2026-08-15_brick-wp-promotion-wave-mapping.md`
  — the promotion wave (`deps/brick-wp` `944c555..52f8a6e`, read-only)
  mapped in the slice-2 format: F1/P3 and F3 → build-now (G0 items
  3a/3b and 3c), P5 audit wiring → build-now (item 4), P4 flagship →
  scheduled into G1's first example, P6 rollback → adopted as the
  per-lift completion criterion, F2/F5/F6 + meta-review outcome →
  exists / not-applicable with reasons.
- JC: F5 (factor a conflated tactic) declined — no conflated kit
  member found, no consumer pain; logged as a non-promotion per the
  outcome-section practice this note adopts.

## Unit G0.3a — P5 setup-loop iteration schema (lift commit)

- STATUS: lift landed. `stepFnIter_iterate` + `stepFnIter_iterate_exit`
  in `GoLeanProofs/FuelMeasure.lean` (+65 lines incl. docstrings) —
  the per-example `Nat.strongRecOn` setup induction stated once.
  Chartered consumers: every G1 array-setup candidate (bubble sort,
  selection sort, two-sum, RLE, palindrome, dedup, Kadane, dot
  product, sieve, matrix, stack, queue). Fixtures retrofitted in this
  commit: isort `hIsetup_loop` (`Examples/InsertionSort/Setup.lean`,
  +62/−50 — the per-iteration composite `hIsu_iter` extracted with
  its own docstring, the induction boilerplate deleted) and wordcount
  `wcH_setup_loop` (`Examples/WordCount/HarnessSetup.lean`, +16/−17 —
  it already had its composite, so the retrofit is pure deletion).
  Statements of both theorems unchanged (vestigial `μ` binder kept so
  zero consumer churn); elaboration cost unchanged (both shards
  ~1.2 s before and after).
- HONEST ACCOUNTING: the retrofit itself is near-line-neutral (+8 net
  across the two fixtures, docstrings included); the value is (a) each
  of the ~11 chartered G1 array examples writes a composite + one
  schema application instead of a fresh ~28-line strong induction,
  and (b) the remaining 6 shipped induction copies become deletable —
  the P6 wave, next commits in this series.
- JC: schema lives in `FuelMeasure` (the fuel-arithmetic composition
  kit, beside `stepFnIter_chain`) rather than `StepKit` (one-step
  glue) — placement by kind, matching the module docstrings.
- JC: two schema forms only (`iterate`, `iterate_exit`); the
  family-function/backing-address abstraction the scale-out P5 row
  sketched is NOT lifted — the per-iteration composite differs per
  example (different segment towers), so the honest shared part is
  the induction, and the composite stays example-local as the
  schema's `hstep` hypothesis. Non-promotion of the wider form
  logged per the outcome-section practice.

## Unit G0.3a-P6 — the setup-induction rollback wave (this commit)

- STATUS: done — ALL 9 shipped setup-loop inductions now instantiate
  the schema; zero `strongRecOn` setup copies survive (the P6
  completion test for the chartered family). This commit retrofits
  the remaining 7: isort rebuild (`hrebuild_loop` + extracted
  `hrb_iter`), binsearch (`hsetup_loop` + extracted `hsu_iter`),
  minmax v1 (`sh_loop` + extracted `sh_iter`), reverse v1 (`su_loop`,
  `iterate_exit` form), reverse HarnessV (`su_loopV`, `iterate_exit`),
  minmax HarnessR (`su_loopR`, `iterate_exit`), wordcount HarnessR
  (`su_loopR`). Every statement byte-unchanged; all modules rebuild
  green at their previous cost class (MinMax 3.1 s, BinSearch 8.8 s,
  HarnessV 3.7 s, WC HarnessR 9.5 s — same class as before).
- JC (scope boundary, logged non-retrofit): the same induction SHAPE
  also appears in the copy/swap/test-phase loops (`cp_loopV`,
  `cp_loopR` ×2, `rh_loop`/`tst_loop`, `rv`/`tv` loops) and, in a
  bounded-`∃k`-per-iteration variant, in the subject loops
  (`mh_loop(R)`, `search_loop`, gcd/fib). NOT retrofitted here: the
  copy/swap loops' code is dominated by their call-entry exit towers
  (the induction scaffold is ~12 of ~60 lines), so replacement is the
  brick-wp meta-review's "churn without capability" non-promotion
  class; the subject loops need a third (bounded-step) schema variant
  — a real candidate for item (d), assessed there. The chartered P5
  family (setup inductions) has zero surviving copies, which is the
  lift's completion test.
- FINDING (input stream): a `stepFnIter_iterate_bounded` variant
  (per-iteration `∃k ≤ c`) would cover the subject-loop induction
  family (≥4 landed instances); consumer-backed, deferred to (d).
