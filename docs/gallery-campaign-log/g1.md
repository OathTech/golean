# G1 — the gallery grows to twenty (campaign log)

Charter section: `docs/2026-08-15_gallery-campaign.md` §G1, plus §G0
item 4 (THE FLAGSHIP RULE, discharged by the first entry here). Owner:
the kit-and-example lane, which owns `Corpus/`, `baselines/`,
`proofs/GoLeanProofs/**` and the kit for the campaign. Format rules:
`INDEX.md`.

COMPLETE = the charter's eight-item checklist, every item
machine-checkable. An attempted example that resists ends as a RECORDED
HONEST GAP and does NOT count toward twenty.

---

## Unit G1.1 — `histogram`, THE FLAGSHIP — **COMPLETE**

All eight COMPLETE-checklist items met (see below). This also
discharges G0's done-item 4, THE FLAGSHIP RULE, by charter design.

The kit's integration exercise: proved end-to-end from the kit alone
wherever a kit form exists, with every place it cannot carry the proof
recorded as a KIT GAP below and then closed with minimal local
machinery marked as gap-witness code.

### The example

**Go** (`Corpus/coverage/exec/examples/histogram/main.go`): `histogram`
builds a `map[uint64]uint64` of counts over a `[]uint64`, reads the
count of a queried key (`counts[q]` — Go's zero value on an absent
key), then counts the map's entries with a VARIABLE-FREE
`for range counts {}`. Harness `histogram_harness_r(n, seed, q)` is the
S3 RELATIONAL style: setup family `v[i] = seed + i%3`, a copy loop into
a fixed-cap `[8]uint64`, then `histogram(v, q)` — the values AND both
summaries returned as data.

- **JC (observable choice):** both summaries returned, not one — the
  queried count exercises the map INDEX READ and `distinct` exercises
  the enveloped `for range`. The charter asked that the observable
  range over the map so the `∀ch` teaching point is real; `distinct`
  does, and its spec (`distinctCount`) is a function of the returned
  values alone. Principle: the harness ruling (returned data over a
  verdict) + the charter's order-invariance requirement.
- **JC (variable-free range):** `for range counts {}` rather than
  wordcount's `for _, c := range counts`. It is the honest lowering of
  "count the entries", and it makes the machine side markedly cheaper —
  `bindIterVars` with no binders allocates NOTHING, so the range loop is
  state-stable. Principle: velocity, where honesty is unaffected (the
  Go is what a Go programmer would write).

### Checklist status

1. **Corpus + differential** — DONE. 13 rows, all PASS: `distinct`,
   `all-same`, `mode-three`, `miss`, `one`, `one-miss`, `empty`,
   `harness-r-{seven,eight,one,empty,miss,wrap}`. Edge cases per the
   brief: empty (`n=0`), single (`n=1`), all-same (`5,5,5,5`),
   all-distinct (`1,2,3,4`), and the wrap-region seed at the int64
   driver maximum (`harness-r-wrap`, `seed = q = 2^63−1`; E1 is the
   reason it cannot go past that, and the row is named for what it
   actually is). THE CAMPAIGN'S FIRST CORPUS TOUCH: full
   `scripts/coverage run` (1573 cases, 2m12s) + `scripts/coverage-negative`
   (311 cases) run, `baselines/native-full.tsv` re-pinned same-commit
   with the reason.
   - **JC (re-pin honesty):** the first draft of the re-pin reason
     claimed the rows bought NEW frontend coverage (`for range m {}`,
     absent-key reads). That was checked and found FALSE — both already
     appear under `maps/` and `native-smoke/` — and the reason was
     rewritten to say what is actually new (the combination inside a
     proof-carrying example). Principle: honesty beats velocity; a
     re-pin reason is a claim like any other.
2. **Golden pin + Program term + named harness `rfl` pin** — DONE.
   `baselines/golden/histogram-lowered.repr` generated from the
   frontend; `proofs/GoLeanProofs/Examples/HistogramProgram.lean`
   generated from that repr; `scripts/check-golden` green on both links
   (`ok [histogram]`). The named pins `histogram_pin` and
   `histogramHarnessR_pin` live in `Examples/Histogram/Machine.lean`
   and both close by `rfl`.
   - NOT designated, by charter: `Examples/Targets.lean`, `scripts/ci`'s
     Targets allowlist line, `Audit.lean`'s designated-name list,
     `judge-config.json`, `Challenge.lean` and `Solution.lean` are
     UNTOUCHED. The one `scripts/ci` line added is the per-module import
     pin in the `check_surface_imports` block (the pattern every
     `*Program` module follows), which carries no closure implication.
3. **Headline** — DONE. `histogram_ok` in
   `proofs/GoLeanProofs/Examples/Histogram.lean` (the ROOT, so the
   aggregator's `import GoLeanProofs.Examples.Histogram` reaches it by
   name — the C-H4/C-H5 shape the phase-2 audit asked for, adopted from
   birth rather than retrofitted). `∃vals-∃N-∀fuel-∀ch` over
   `runFunctionWithContextM`, returned values only, no heap/cell/seed
   vocabulary. Axioms `[propext, Classical.choice, Quot.sound]` — the
   trio — `#guard_msgs`-pinned with 12 others in the new shard
   `proofs/Audit/Histogram.lean` (wired from `Audit.lean`'s import list
   + roll-call). `histogram_readout` is the D1 twin.
4. **Fuel bound** — DONE. SHIPPED `N = 210·n + 344`, quoted as a BOUND.
   MEASURED, recorded separately: EXACTLY `194·n + 16·distinctCount vals
   + 344` — verified at `n = 0…8` and independent of the choice stream
   and of `q`. The two coincide only while every value is new (`n ≤ 3`);
   the measurement is not affine in `n`, because the family stops adding
   map entries after the third value. Constant term `344` is exact (it
   is the `n = 0` measurement).
5. **Deletion test** — RUN, not asserted. `lean_minimal_hypotheses` on
   `histogram_ok`: all four explicit binders (`n seed q`, `hcap`,
   `hseed`, `hq`) are LOAD-BEARING — dropping any breaks the proof (the
   three domain bounds each feed an `omega` that needs them). No
   decorative hypothesis.
6. **Gallery entry** — DONE. `docs/verified-examples.md`, full harness
   verbatim with `<!-- verbatim: -->` markers; `scripts/render-gallery`
   exit 0 (52 blocks, was 45). Claim ≤ theorem. Domain bounds attributed
   separately (`n ≤ 8` = the program's own array cap; `seed`/`q < 2^64`
   = Go's uint64 domain; the counting = mathematics; machine
   idealization stated). ∃-family-determination disclosed.
   Order-invariance framed as LOAD-BEARING (the observable does range
   the map). The doc's counts updated (`Seven` → `Eight Go programs`;
   "six of the seven" → "six of the eight"), and a new paragraph in
   *How to read an entry* states plainly that this ninth entry is NOT
   designated and what that costs (no mechanized statement-TCB walk, no
   Comparator replay — hence the hand-run deletion test).
7. **`scripts/proof-costs`** — DONE, all under the ~2.5 GiB bar:
   CountLoop 2045 MiB / 71.0 s, HarnessR 1926 MiB / 9.4 s, Machine
   1799 MiB / 2.1 s, root 1766 MiB / 0.8 s, Audit shard 1483 MiB /
   0.7 s, Pure 671 MiB / 0.4 s, Program 492 MiB / 0.3 s. Program-generic
   discipline held: the pinned program is unfolded exactly TWICE in the
   whole example (the two lowering pins, and the one `enterFrame`
   discharge); every raw segment is proven over an abstract `σ`.
8. **`scripts/ci` PASS at each commit** — commit 1 and commit 2 both
   green.

### The proof, in one paragraph

Entry → setup loop (57 steps/iteration, the P5 schema) → copy loop (53)
→ ONE program-consulting `enterFrame` → the subject's prologue (51) →
the counting loop (84/iteration, `Histogram/CountLoop.lean`) →
`hits := counts[q]` (the map READ, `MapMem.applyStrictOp_mapGet`, Go's
zero value on an absent key) → `distinct := 0` → the map-range loop (16
steps/iteration, one choice consumed, ZERO allocation) → the epilogue.
Two pure bridges carry the answers: `cnt_countsList'` (the map's count
at a key IS that key's number of occurrences) and `countsList_length`
(the histogram has exactly as many entries as the list has distinct
values). The second is what makes the enveloped loop's answer readable.

### Judgment calls (this unit)

- **JC (headline placement):** the headline sits in the example ROOT,
  not in the leaf run shard. Principle: the recorded C-H4/C-H5 debt —
  a designated theorem that the aggregator cannot reach by name is a
  gate hole; do not reproduce the shape that needs the repair.
- **JC (shipped bound):** ship the branch-uniform `210·n + 344` rather
  than the exact measured formula, and record the exact formula
  separately. Principle: the 2026-08-15 audit precedent — a bound is a
  bound; a measurement is a measurement; never present one as the other.
- **JC (delegation):** the counting-loop tower (the single largest
  chunk, and a near-verbatim address-rename of wordcount's) was
  delegated to a Fable worker against a fully specified theorem
  statement, while the lane owner wrote the setup/copy/query/range/exit
  halves. Principle: worker-model rule (Fable for proof slices) +
  disjoint-file ownership; the shared placement defs were fixed in
  `Machine.lean` FIRST so neither side could drift. It came back green
  on its first build with the predicted bound unchanged.
- **JC (probe-first, not guess-first):** a step tracer and a structured
  continuation printer (`.tmp/trace.lean`, `.tmp/show.lean`) were built
  before any segment was written, so every segment's step count and
  target configuration came from the real machine. Principle: `#eval`
  before you `decide` — the same reflex, applied to `rfl` goals. Cost:
  ~20 minutes; benefit: the entire entry/setup/copy tower was correct
  first time (its step counts are identical to wordcount's, which the
  tracer confirmed rather than assumed).

### THE KIT-GAP LIST (first-class deliverable) — FINAL

Each gap: what the kit could not carry, and the exact shape wanted.

- **GAP-P1 — the counting fold is example-local.**
  `bump`/`countsList` and their lemma chain (`setk_cnt_succ`,
  `countsList_append_value`, `cnt_countsList'`, `countsList_val_le`,
  the `mem`/`nodup` keys chain) had to be re-derived verbatim from
  `Examples/WordCount/Pure.lean`. G0 item 3b deliberately left them
  there ("wordcount spec vocabulary, not map machinery"); as the
  chartered consumer this example shows that boundary call was wrong.
  *Shape wanted:* `MapMem.bump`, `MapMem.countsFold : List Int → List
  (Int × Nat)`, with `setk_cnt_succ`, `countsFold_append`,
  `cnt_countsFold`, `countsFold_val_le`, `countsFold_nodup_keys` and
  `countsFold_key_mem` proven once. Consumers: wordcount (landed),
  histogram (landed), fibonacci-memo (chartered) — over the §12 bar
  today. Cost of the gap here: ~150 lines re-derived.
- **GAP-P2 — the setup family is example-local.** `histFamily` and its
  five facts re-derive `wcFamily`'s. The P5 lift covered the setup
  INDUCTION only; the G0 log records the family abstraction as
  deliberately not lifted.
  *Shape wanted:* `SliceMem.familyMod (k n seed : Nat) : List Int` with
  `length`/`range`/`set`/`getD`/`succ` proven once, `k` the modulus.
  Consumers: wordcount, histogram (both `k = 3`), and every chartered
  array-setup G1 candidate. Cost of the gap here: ~55 lines.
- **GAP-M1 — the `mapIterK` choice-pick step is example-local AND
  binder-specialized.** `stepFn_pick` lives in
  `Examples/WordCount/Machine.lean` and is written for the
  `none`/`some "c"` binder shape; this example's `for range m {}` needs
  `none`/`none`, so it re-derives the step as `stepFn_pick_novars`.
  Nothing in either proof is example-specific — both are statements
  about `mapIterK`, `Choices.consume` and `MapMem.toEntries`.
  *Shape wanted:* one kit lemma beside `MapMem.snapshot_toEntries`,
  parameterized over `(keyVar valVar : Option String)` with the
  allocation described by `bindIterVars`, plus the two corollaries at
  the shapes that actually occur. Consumers: wordcount, histogram,
  fibonacci-memo (chartered). Cost of the gap here: ~45 lines.

- **GAP-C1 — the counting-loop generic layer is name-specialized.**
  `Examples/WordCount/CountGeneric.lean` already states this exact
  iteration (`wcIter_generic`, 53 steps) and loop induction
  (`wcLoop_generic`) generically over the state family, the placement
  environments/continuations and the per-segment facts — but over the
  CONCRETE statement constants `asgnC1`/`seqnC2`/`mapAsgnStmt`, which
  embed the Go variable name `"words"`. Histogram's body says `"vals"`,
  so neither theorem applies and `Histogram/CountLoop.lean` re-derives
  the whole tower.
  *Shape wanted:* the same generic layer, additionally parameterized
  over the body's three statement constants (equivalently over the
  slice/counter variable names they embed). Consumers: wordcount
  (3 placements, landed), histogram (1, landed), fibonacci-memo
  (chartered). Cost of the gap here: ~600 lines — the single largest
  gap by far, and the one to close first.
- **GAP-R1 — the range-loop choice-pick induction is body- and
  binder-specialized.** `Examples/WordCount/RangeGeneric.lean` states
  the §10b induction once, but over `wcRangeBody` and the `some "c"`
  value binder. A variable-free `for range m {}` matches neither, so
  `hg_range_iter`/`hg_range_loop` re-derive it.
  *Shape wanted:* the induction over an ABSTRACT body with the
  per-iteration effect supplied as a hypothesis ("one iteration takes
  the state at `d` to the state at `f d` in `c` steps"), which is all
  either consumer actually needs; the body and binder shape then never
  appear in the schema. Consumers: wordcount, histogram (both landed).
  Cost of the gap here: ~130 lines.
- **GAP-M2 — `DeadFrom` and its two `push` lemmas are example-local.**
  They live in `Examples/WordCount/Machine.lean` and are pure heap
  algebra ("this tail has nothing at or above `na`"). Re-derived here.
  *Shape wanted:* `StepKit.DeadFrom` beside `set_fresh` /
  `lookup_append_right`. Cost of the gap here: ~30 lines. Trivial to
  close and it unblocks nothing else — but it is the kind of thing that
  will be re-derived twelve more times if it is not moved.
- **GAP-P1 addendum (found by the counting-loop half):**
  `take_succ_getD` and `cnt_take_le` (`Examples/WordCount/CanonCount.lean`)
  and `nilMapCell` (`WordCount/CountGeneric.lean`) are in the same
  boat as `bump`/`countsList` and belong with them in the GAP-P1 lift.

**What the kit DID carry, stated as plainly as the gaps.** Every
machine step in this example that touches the heap, the map or the
program went through a named kit lemma — **366 kit invocations across
the five histogram modules, and ZERO hand-rolled `simp only [stepFn,
…]` unfoldings** on either half of the work. `derive_entry_eq` produced
the entry equation (state def, start config and the theorem) from two
identifiers, in its program-generic form, and the consumer bridge is
the one recorded structural `rfl`. `stepFnIter_iterate` (the G0 P5
schema) took the setup loop's induction. `MapMem` carried every map
fact (`applyStrictOp_mapGet`, `mapAssignValue_toEntries`,
`snapshot_toEntries`, the `toEntries` bridges) unchanged. The gaps
above are all ABSTRACTION gaps — a kit form that exists but is stated
too narrowly, or a family that was left in an example — not capability
gaps. Nothing in this example needed a machine fact the kit could not
state.

**Honest kit-only accounting.** Of ~3,890 lines of histogram Lean
(excluding the generated `HistogramProgram.lean`), roughly **1,050
lines (~27%) are gap-witness code that closing the five gaps above
would delete**. The remaining ~2,840 are irreducibly per-example: the
two `Func` literals and their `rfl` pins (~300), the address placement
— environments, continuations, heap fronts (~330), the raw
`with_unfolding_all rfl` segments (~700), the conditioned discharges
(~350), the run assembly (~230), docstrings (~800), and the statement
functions + cardinality bridge + headline (~130). The flagship's
verdict: **the kit carried the example end to end at the STEP level and
not at the STRUCTURE level.** Twelve successors will each pay the ~1,050
again unless GAP-C1 and GAP-R1 are closed; the per-example floor after
closing them is roughly 2,800 lines, of which ~1,000 is the raw-segment
tower that only a placement-generic segment generator could touch.

### Costs so far

| artifact | lines | build |
|---|---|---|
| `Corpus/.../histogram/main.go` + `cases.tsv` | 66 + 14 | — |
| `baselines/golden/histogram-lowered.repr` | 436 | generated |
| `Examples/HistogramProgram.lean` | 462 | 0.3 s |
| `Examples/Histogram/Pure.lean` | 400 | 0.4 s |
| `Examples/Histogram/Machine.lean` | 350 | 1.0 s |
| `Examples/Histogram/CountLoop.lean` | 825 | 71.0 s |
| `Examples/Histogram/HarnessR.lean` | 1701 | 9.4 s |
| `Examples/Histogram.lean` (root + headline) | 148 | 0.8 s |
| `Audit/Histogram.lean` | 109 | 0.7 s |
| **total Lean (excl. generated Program)** | **3892** | **~85 s** |

Peak memory, all under the ~2.5 GiB bar: 2045 / 1926 / 1799 / 1766 /
1483 / 671 / 492 MiB. Full-corpus differential: 2m12s (1573 cases).
Negative corpus: 11s (311 cases). `scripts/ci`: PASS at both commits.
render-gallery: exit 0, 52 blocks.

**Wall time for the whole unit** (the number twelve successors are
sized against): about 3.5 hours of agent time end to end, of which the
counting-loop shard ran ~14 minutes in parallel on a delegated worker.
The dominant costs were, in order: writing and debugging the
raw-segment towers (~40% — mechanical once the tracer existed), the
pure spec layer and its cardinality bridge (~15%), the run assembly
(~15%), and the corpus/golden/gallery/audit wiring (~15%). The tracer
and continuation printer cost ~20 minutes and are reusable by every
successor — they are the single highest-leverage thing built here that
is not a Lean lemma.

---

## Unit G1.1-KG — KIT-GAP CLOSURE (the six recorded gaps)

Closing the flagship's kit-gap list so successors stop re-paying the
~1,050 gap-witness lines. Per gap: the lift, BOTH landed consumers
retrofitted as fixtures, the P6 rollback (gap-witness code deleted in
the same commit), measured deltas, kit pins. One commit per gap;
`scripts/ci` PASS at each (proofs-only — the flagship's full
differential record stands; `GOLEAN_ALLOW_NO_DIFF` not needed, the
lane's recorded run is present).

- **JC (closure order):** dependency-driven, not the list order —
  GAP-M2 (`DeadFrom`) and GAP-P1 (`countsFold`) are STATEMENT
  vocabulary of GAP-C1's kit theorems, so they land first as quick
  commits; C1 (the conceptual core) follows with the freshness budget
  intact. Principle: a lift's statement cannot cite an example-local
  name, so the vocabulary gaps are prerequisites, and both are small.

### GAP-M2 — `DeadFrom` + `push`/`push2` → `StepKit` — **CLOSED**

- Lift: `GoLean.Surface.DeadFrom`, `DeadFrom.push`, `DeadFrom.push2`
  in `StepKit.lean` (P11 group; +35 lines incl. docstrings + API
  listing). Both consumers retrofitted: WordCount `Machine.lean`
  −25/+4 (copy deleted, delegation note), Histogram `Machine.lean`
  −26/+4 (the gap-witness copy deleted). References resolve through
  the existing `open GoLean.Surface`; zero call-site churn.
- Pins: +2 in `Audit/Kit.lean` (StepKit 23 → 25), transcribed
  verbatim from a fresh `#print axioms` probe
  (`[propext, Classical.choice, Quot.sound]` both).
- What a successor no longer pays: ~30 lines (the g1 estimate held).
- Gate: full proofs build green (344 jobs); `scripts/ci` PASS at the
  commit.

### GAP-P1 — the counting fold → `MapMem` — **CLOSED**

- Lift: `MapMem.bump` / `countsFold` / `nilMapCell` + the lemma chain
  (`setk_cnt_succ`, `countsFold_nil`, `countsFold_append`,
  `cnt_countsFold` — stated over the neutral filter-length so neither
  example's statement function is named — `countsFold_key_mem`,
  `countsFold_nodup_keys`, `cnt_of_mem_nodup`, `cnt_pos_mem`,
  `countsFold_val_le`) plus the recorded addendum members
  (`take_succ_getD`, `cnt_take_le`). MapMem +279 lines incl. the
  sealed-API update; its stale "not map machinery" boundary note
  corrected in place.
- P6 rollback, both landed consumers: WordCount `Pure.lean` −255/+42
  (the whole fold layer deleted; keeps `multiplicity` bridge
  `cnt_countsFold'` and the max fold), Histogram `Pure.lean` −228/+40
  (fold layer deleted; keeps the `occurrences` bridge and its own
  cardinality bridge), plus the scattered copies: CanonCount's
  `take_succ_getD`/`cnt_take_le`, CountGeneric's `nilMapCell`,
  CountLoop's private `take_succ_getD`/`cnt_take_le`/`nilMapCellH`.
  WordCount-side spelling renamed `countsList` → `countsFold`
  (kit name) throughout — proof-layer only, no WC pin names it.
- **JC (pin-preserving delegations):** Histogram's audit shard PINS
  `countsList`/`countsList_length`/`cnt_countsList'`/
  `countsList_val_le` by name, and this unit's rules freeze existing
  `#guard_msgs` pins byte-for-byte. Resolution: histogram keeps those
  NAMES — `countsList` as a one-line `abbrev` delegation to
  `countsFold`, `countsList_val_le` as a one-line delegation theorem,
  the two bridges under their pinned names — so `Audit/Histogram.lean`
  is byte-identical while zero witness PROOF lines survive (the G0
  "reduced to delegations" allowance, logged).
- Pins: +11 in `Audit/Kit.lean` (MapMem 13 → 24), transcribed verbatim
  from a fresh probe (a `[]`-axiom class appears: `countsFold_nil`).
- Net across the two consumers: −665/+572 repo-wide for the commit;
  what a successor no longer pays: ~150 lines of fold re-derivation
  plus the ~18-line `take_succ_getD`/`cnt_take_le`/`nilMapCell`
  addendum (the g1 estimate held).
- Gate: full proofs build green; `scripts/ci` PASS at the commit.

### GAP-C1 — the counting-loop generic layer → `MapLoops` — **CLOSED**

The big one. New kit module `GoLeanProofs/MapLoops.lean` (978 lines),
in TWO layers:

1. **The name-parameterized generic pair.** `mapCountIter_generic`
   (53 steps) and `mapCountLoop_generic` are `wcIter_generic`/
   `wcLoop_generic` lifted out of `Examples/WordCount/CountGeneric.lean`
   with the body's three statement constants parameterized by the ONE
   name they embed (`slVar`, the Go slice variable — `asgnC1G` /
   `seqnC2G slVar` / `mapAsgnG`; everything else in the desugared
   `m[k]++` body is the frontend's fixed shape). The loop needs no
   `slVar` at all.
2. **The per-placement discharge pack** (`mapCountIter_at`): the nine
   conditioned discharges every placement had re-derived
   (`init1`/`st1`/`init2`/`st2`/`var1`/`var2`/`read`/`mapGet`/
   `mapAsgn`, ~270 lines per placement, FOUR landed copies) are proven
   once over an abstract placement — base state + live front + nine
   placement facts, of which eight are `rfl` at every landed placement
   and the ninth is the placement's existing front-freshness lemma.
   `defaultValue`/`normalizeValueForTy` at the loop's two types proved
   over a fully abstract σ (they consult `σ.types` only at `.defined`
   types, so no hypothesis is needed).

- **JC (loop/exit boundary):** the kit loop ends at the exit test's
  `false` delivery (`84·(L−i)` steps EXACTLY — the ∃k slack in the old
  layer came entirely from the exit) and each consumer chains its own
  exit: wordcount's allocates + snapshots (23 steps ×3 placements),
  histogram's does neither (9 steps). Principle: the honest shared part
  stops where the witnesses diverge — same boundary call as the P5
  setup-loop schema.
- **JC (design against both witnesses):** the parameterization was
  chosen by diffing histogram's re-derived tower against wordcount's
  generic layer: the ONLY deltas were the slice name, the placement
  numbers (`bArr`/`bMap`/`base0`, already parameters) and the exit —
  so `slVar : String` + the exit split covers both, and nothing wider
  (a per-statement `Stmt` parameterization) was built. Principle:
  over-abstraction is over-specialization's twin; parameterize by what
  the witnesses actually vary.
- P6 rollback, all FOUR landed placements retrofitted in the same
  commit: `CountGeneric.lean` DELETED (−531; the WC root's import list
  and shard table updated), CanonCount −356 net, HarnessRun −351,
  HarnessR −364, Histogram CountLoop −574 net (825 → 376 lines).
  Repo-wide: **−1,948/+259 in consumers** against +978 kit
  (+22 StepKit: `set_append_left`, promoted from RangeGeneric's
  private copy on becoming a kit-internal consumer).
- Costs (measured, `scripts/proof-costs`): MapLoops 0.8 s / 671 MiB;
  the retrofitted modules all UNDER their previous class — Histogram
  CountLoop **71.0 s → 1.2 s** / 2045 → 1740 MiB (the deleted tower
  was the module's entire cost), CanonCount 4.4 s / 1802 MiB,
  HarnessRun 2.2 s / 1835 MiB, HarnessR 8.3 s / 1976 MiB. All under
  the ~2.5 GiB bar.
- Pins: +3 (`MapLoops` section) +1 (`set_append_left`) in
  `Audit/Kit.lean`, verbatim from a fresh probe; `MapLoops` imported
  there like the other kit modules.
- What a successor now does NOT pay: the conditioned discharges
  (~270), the composed iteration (~135) and the loop induction (~95) —
  ~500 lines; what it still pays for its counting loop: the raw `rfl`
  segments (~210, its own program's step transcriptions), ~15 lines of
  placement facts (8 `rfl`s + the front-freshness lemma it needs
  anyway), and two ~30-line instantiations + its exit chain.
- Gate: full proofs build green (344 jobs); `scripts/ci` PASS at the
  commit.

### GAP-R1 — the range-loop choice-pick induction → `MapLoops` — **CLOSED**

- Lift: `MapLoops.mapPickLoop_generic` — the §10b induction over an
  ABSTRACT state descriptor `δ`: the entire per-iteration content
  (body, binders, allocation, accumulator effect) enters through ONE
  hypothesis ("given the pick, an iteration reaches the erased
  snapshot at some `d'` preserving the invariant, within `c` steps"),
  so neither body nor binder shape appears in the schema — exactly the
  recorded gap shape. Plus the three support lemmas both examples had
  re-derived: `consume_lt`, `eraseIdx_length_of_lt`,
  `mem_of_mem_eraseIdx`.
- **JC (conservation invariants):** the consumer encodes its
  accumulator law as a conservation invariant `P` — wordcount:
  `max best (maxOf remaining) = const` (re-established per pick by
  `maxOf_eraseIdx`); histogram: `distinct + |remaining| = const`.
  Principle: this is the §10b order-invariance teaching point made
  structural — a pick-order-independent claim IS a conservation law of
  the pick loop, and the schema forces that phrasing.
- P6 rollback: WordCount `RangeGeneric` restructured — the induction
  body became `wcRangeIter_generic` (one iteration, GIVEN the pick,
  both branches, `best` at `max bv p.2`) and `wcRange_generic`
  (statement UNCHANGED — its three placement consumers are untouched)
  is now a ~70-line instantiation; the private `consume_lt`/
  `mem_of_mem_eraseIdx`/`set_append_left` copies deleted. Histogram:
  `hg_range_iter` reshaped to the pick-given form, `hg_range_loop`'s
  induction replaced by a kit instantiation, `Machine`'s re-derived
  `consume_lt`/`eraseIdx_length_of_lt` deleted; its statement's exact
  `16·m + 1` became the schema's `∃ k ≤ 16·m + 1` (the run assembly's
  fuel arithmetic carries the bound — the only consumer-visible
  change, and the pinned name/axioms are unchanged).
- Deltas: net +137 repo-wide (the kit schema + the iteration lemma's
  explicit statement outweigh the deleted inductions — the honest
  accounting: this gap's value is the SCHEMA for successors, not a
  net deletion in the two witnesses; histogram-side witness code
  deleted ~90 lines).
- Pins: +4 (`MapLoops` 3 → 7), verbatim from a fresh probe.
- What a successor now does NOT pay: the pick destructuring + the
  induction + the invariant threading (~90–130 lines; it writes one
  iteration lemma and a ~30-line instantiation with its conservation
  invariant).
- Gate: full proofs build green (344 jobs); `scripts/ci` PASS at the
  commit.

### GAP-P2 — the setup family + copy prefix → `SliceMem` — **CLOSED**

- Lift: `SliceMem.familyMod k n seed` (the `v[i] = seed + i%k` wrapped
  family, modulus generic) with `length`/`range`/`Z_range`/`succ`/
  `set`/`getD` proven once, and `prefixPad fam cap m seed` (the copy
  loop's family-prefix-zero-tail invariant) with `zero`/`length`/
  `range`/`familyMod_set`/`full` — the pad lemmas conditioned on the
  family facts, discharged by the `familyMod` lemmas at both
  consumers. SliceMem +109.
- P6 rollback, both landed consumers: `wcFamily`/`wcPre` and
  `histFamily`/`histPre` (and all their facts) reduced to one-line
  delegations — every re-derived proof deleted (WC Family −46 gross,
  WC HarnessR −34, Histogram Pure −60, Histogram HarnessR −26). The
  pinned names (`wcFamily`, `histFamily`, `histFamily_set` among them)
  survive as delegations, so both audit shards stay byte-identical.
- **JC (minmax out of scope):** `mmFamily` is a DIFFERENT formula
  (`seed + i`, no modulus) and designated statement vocabulary — not
  touched; the g1 record's "three landed consumers" counted the two
  familyMod-shaped ones plus the prefix pattern's minmax sibling,
  which stays as-is under the frozen-statement rule. Two fixtures
  (wordcount + histogram) discharge the §12 bar.
- Pins: +11 (`SliceMem` 17 → 28), verbatim from a fresh probe.
- What a successor now pays for an array-setup family: one delegation
  line per fact instead of ~80 lines of family/prefix proofs.
- Gate: full proofs build green (344 jobs); `scripts/ci` PASS at the
  commit.

### GAP-M1 — the `mapIterK` choice-pick step → `MapMem` — **CLOSED**

- Lift: `MapMem.stepFn_pick_bind` — the §10b pick parameterized over
  `(keyVar valVar : Option String)`, the per-binder allocation
  supplied as a `bindIterVars` fact (exactly the recorded gap shape) —
  plus the two corollaries at the shapes that occur:
  `stepFn_pick_value` (value binder: fresh cell at `nextAddr`, scope
  pushed and declared) and `stepFn_pick_novars` (variable-free: state
  UNCHANGED, scope pushed — the reason a `for range m {}` loop is
  state-stable). MapMem +112.
- P6 rollback, both landed consumers: WordCount `Machine`'s
  binder-specialized `stepFn_pick` DELETED (−50; the three placement
  pick-discharges re-pointed to `stepFn_pick_value (v := "c")`);
  Histogram `Machine`'s `stepFn_pick_novars` proof deleted — the
  PINNED name survives as a one-line delegation (axioms unchanged, the
  audit shard byte-identical).
- Pins: +3 (`MapMem` 24 → 27), verbatim from a fresh probe.
- What a successor now pays: nothing for the pick step at either
  binder shape; a `some k`/`some v` two-binder consumer discharges one
  `bindIterVars` fact against `stepFn_pick_bind`.
- Gate: full proofs build green (344 jobs); `scripts/ci` PASS at the
  commit.

### KIT-GAP CLOSURE — the unit summary

All six recorded gaps CLOSED, each with both landed consumers
retrofitted and the witness code deleted or reduced to pinned
delegations in the same commit. The acceptance number — what a
histogram-class successor now does NOT pay, against the flagship's
recorded ~1,050 gap-witness lines:

| gap | successor no longer pays |
|---|---|
| C1 (counting loop) | ~500 (discharges + composition + induction) |
| R1 (range/pick loop) | ~90–130 (pick + induction + threading) |
| P1 (counting fold) | ~150 + the 18-line addendum |
| P2 (setup family + prefix) | ~80 (delegation lines remain) |
| M1 (pick step) | ~45 |
| M2 (`DeadFrom`) | ~30 |

Total: **~900–950 of the ~1,050 closed**; the remainder is the
delegation/instantiation lines a consumer legitimately writes
(~40-line kit-loop instantiations, one-line fact delegations). The
projected per-successor cost for a histogram-class example drops from
~3,890 lines to roughly **2,900–3,000 lines** (the irreducible
per-example floor: `Func` pins ~300, placement ~330, raw segments
~700, remaining conditioned discharges outside the counting loop ~350,
run assembly ~230, docstrings ~800, spec+headline ~130 — the g1
verdict's floor, now actually reachable because the structure level is
kit-carried too), and the wall estimate from ~3.5 h to roughly
**2.5–3 h** (the counting/range towers were ~40% of the flagship's
proof-writing time; their composition halves are now instantiations —
measured directly: Histogram/CountLoop.lean is 825 → 376 lines and
71 s → 1.2 s).

Kit pin count after the unit: **116 `#print axioms` pins** in
`Audit/Kit.lean` (measured), up from G0.4's 81: +35 new kit-lemma
pins across the six closures (StepKit 26, SliceMem 28, MapMem 27,
MapLoops 7, FuelMeasure 18 public lemmas, plus the 10 entry-equation
fixture pins), every one transcribed verbatim from a fresh
`#print axioms` probe.

Final-tree costs (`scripts/proof-costs` at the unit tip, every module
the closures touched): MapLoops 0.8 s / 683 MiB, MapMem 3.6 s / 1178,
SliceMem 3.5 s / 811, WC RangeGeneric 2.7 s / 1803, WC Machine 0.9 s /
1717, WC Family 0.9 s / 1767, WC HarnessR 8.2 s / 1977, Histogram
Machine 1.0 s / 1764, Histogram HarnessR 9.8 s / 1922 (+ the C1-time
measurements above). Everything well under the ~2.5 GiB bar; nothing
above its previous cost class.

---

## Unit G1.2 — THE GUARDRAILS WAVE (corpus half of every remaining G1
## example) — IN PROGRESS

Guardrails-first, executed at scale: land the CORPUS half of every
remaining G1 candidate in ONE pass, so the proof units can then run in
parallel lanes without contending on `baselines/` (the worktree
discipline's one serialized resource). Each example ships:

(a) `Corpus/coverage/exec/examples/<dir>/main.go` — subject + harness in
the settled style triad, rung 0, `gofmt`-clean, `go run`-verified;
(b) `cases.tsv` rows including the edge cases, differentially green
against the real oracle;
(c) the golden pin (`baselines/golden/<dir>-lowered.repr`) +
`<Name>Program.lean` + a named harness `rfl` pin, `scripts/check-golden`
green on both links;
(d) a STUB proof root — the Program import and the harness pin ONLY, no
theorems — plus its two `GoLeanProofs.lean` imports, so the module is in
the audited closure from birth.

**Items 3–8 of the COMPLETE checklist are deliberately NOT met here.**
A wave entry is a landed GUARDRAIL, not a COMPLETE gallery entry; each
becomes COMPLETE when its proof lane states the headline in the stub
root it inherits. G1's count of twenty moves only on COMPLETE entries.

- **JC (wave shape):** corpus-half-for-everything before any second
  proof, rather than example-at-a-time end-to-end — guardrails-first
  says the case that pins a target costs almost nothing *before* the
  implementation and fixes the target before it can drift; doing all of
  them at once also makes the baseline re-pin ONE dated event instead of
  thirteen, which is what lets the proof lanes parallelize at all.
- **JC (harness `Func` extraction):** the stubs' harness `Func`s are
  EXTRACTED from the pinned repr mechanically (`.tmp/wave/genlean.py`)
  rather than hand-transcribed in the flagship's readable dot-notation
  form. Honesty beats elegance: a byte-derived transcription cannot
  quietly say something the lowering does not, and the `rfl` pin is a
  real check either way. A proof lane may restate it readably; the pin
  must keep holding.
- **JC (per-example gate lines):** each new `*Program` module costs the
  standing one reviewable allowlist line in `scripts/check-golden` plus
  one import pin in `scripts/ci`, following the existing seven-entry
  pattern verbatim (charter G1 item 2). No gate LOGIC is touched.

### The wave table

| example | style | rows | classification | notes |
|---|---|---|---|---|
| `selsort` (SelectionSort) | S3 relational — returns `(pre, post)` | 14 | 14 PASS | sorts state their property directly as a relation over returned data; LCG setup family is genuinely unsorted (probe-verified) |
| `bubble` (BubbleSort) | S3 relational — returns `(pre, post)` | 14 | 14 PASS | early-exit `swapped` flag exercises unary `!` (no corpus precedent — probed before use); brief's suggested family was already sorted, author substituted a wrapping LCG and said so |
| `twosum` (TwoSum) | S3 relational — returns `(vals, i, j)` | 14 | 14 PASS | the searched-over data AND the answer indices are returned, so the post is a relation over both; sentinel `(len, len)` on no-pair, pinned by rows |
| `rle` (RunLength) | S3 relational — returns `(pre, runVals, runCounts, k)` | 14 | 14 PASS | four returns: the encoded input and BOTH parallel output arrays plus the run count, so the post can state the decode relation without a family function |
| `powmod` (PowMod) | S2 scalar — returns the value | 13 | 13 PASS | scalar-in / scalar-out: S3 would degenerate (pre-state IS the arguments), so S2 says strictly more in one line — the `gcd` precedent |
| `palin` (ArrayPalindrome) | S3 relational — returns `(pre, verdict)` | 14 | 14 PASS | S3 rather than S1: returning the checked array turns a bare `= 1` verdict into a relation the reader can audit; alternating family gives BOTH outcomes at n>=2 |
| `dedup` (DedupAdjacent) | S3 relational — returns `(pre, post, k)` | 13 | 13 PASS | compaction is a pre/post relation; the alternating row pins that ADJACENT-only dedup keeps `1,2,1,2` whole — the classic misreading, guarded |
| `kadane` (Kadane) | S3 relational — returns `(pre, best)` | 14 | 14 PASS | the wave's only SIGNED example (`int64` + negative args, both probed `ok`); all-negative row pins the classic trap (largest element, not 0) |
| `dotprod` (DotProduct) | S3 relational — returns `(av, bv, dot)` | 14 | 14 PASS | **E1's designated consumer**: 4 rows exercise genuine mod-2^64 wrap; the reachable extreme is int64 max, the true uint64 wrap region awaits E1 (see `g2.md`) |
| `sieve` (Sieve) | S2 scalar — returns the count | 11 | 11 PASS | deliberately NO extreme-seed row: `n` is an ALLOCATION size, and the boundedness rule outranks the wave's generic edge-case rule (JC below) |
| `matmul` (MatMul) | S3 relational — returns `(a, b, product)` | 11 | 11 PASS | the wave's only 2-D example: `[3][3]uint64` returned and observed as nested `{"tag":"array"}`; Go arrays are VALUES, which is why it fits the pass-by-value fragment with no cap workaround |
| `stack` (SliceStack) | S3 relational — returns `(pushed, popped, remaining)` | 13 | 13 PASS | LIFO is the property: 7 rows have distinct values so FIFO order would flip every entry; `append` + `s[:len(s)-1]` both probe-verified |
| `queue` (SliceQueue) | S3 relational — returns `(enqueued, dequeued, remaining)` | 13 | 13 PASS | the `stack` mirror: 6 rows would break under LIFO; `q[1:]` re-slicing probe-verified. The PAIR is what pins the discipline |
| `strrev` (StringReverse) | S3 relational — returns `(pre, post, isPalin)` | 12 | 12 PASS | **RESERVE-list example, landed E4-INDEPENDENT.** Strings cross the boundary as `{"tag":"string","bytes":[...]}`, so pre AND post are genuinely observed. No extension was needed — see `g2.md` |
| `stein` (Stein) | S2 scalar — returns the value | 9 | **9 FAIL, stage `frontend-export`** — the RECORDED QUARANTINE | **E3's pull.** Natural Go (`for isEven(a) && isEven(b)`) is quarantined; Go oracle verified against Euclid (0 mismatches, 100k-pair sweep). A visible expected gap, never a false pass |
| `fibmemo` (FibMemo) | S2 scalar — returns the value | 10 | 10 PASS | the SPARE, added because the wave went smoothly; a chartered MapMem consumer (G0.2). Comma-ok `v, ok := memo[n]` probed and SUPPORTED; `fibMemoSize` counts via `for range memo` (order-invariant, the flagship's teaching point) |

**Wave totals: 16 examples, 203 new corpus rows — 194 PASS, 9 FAIL
(all nine the `stein` quarantine, at stage `frontend-export`).**

- **JC (the spare):** `fibmemo` was added because the wave met no
  resistance — zero examples hit the divergence guard's 3-probe limit,
  and no example was dropped or substituted. It also discharges the
  second half of a G0 debt: the MapMem promotion (G0.2) named
  `fibonacci-memo` a CHARTERED consumer, and a chartered consumer with
  no corpus is a promise, not a consumer.
- **Recorded correction from the field:** the brief for `fibmemo`
  assumed the existing `fib` example is recursive. It is ITERATIVE. The
  author probed plain recursion through the machine rather than
  inheriting the assumption, found it supported, and said so. The wave
  authored 16 examples against a probed construct list and hit ZERO
  unexpected quarantines; the two quarantines it did hit (`stein`,
  and `fmt`/`strings` in the E4/E2 probes) were both sought
  deliberately.
- **Divergence guard: never triggered.** No example resisted more than
  one probe round.

### What a wave entry is NOT

Items 3–8 of the COMPLETE checklist — headline theorem, fuel bound,
deletion test, gallery entry, proof-cost measurement — are absent by
design. G1's count of twenty is unchanged by this unit: it stands at
**1 COMPLETE** (`histogram`) with 16 examples now holding a landed
guardrail suite, a pinned lowering and a stub root waiting for a
headline. The wave's purpose is that those 16 proof units can now run
as PARALLEL LANES without contending on `Corpus/` or `baselines/` —
the one serialized resource, now touched once.

---

## Unit G1.3 — `powmod` [lane A] — **COMPLETE**

The first entry of PROOF LANE A (the parallel proof half of the
guardrails wave; lane A owns `powmod · sieve · fibmemo · kadane ·
dotprod · dedup`). All eight COMPLETE-checklist items met; items 1–2
were landed by the wave and are unchanged.

### The example

**Go** (`Corpus/coverage/exec/examples/powmod/main.go`): right-to-left
binary exponentiation modulo `m` — `result` accumulates the odd bits,
`base` squares, `exp` halves, everything reduced `% mod`. Harness
`powmod_harness(base, exp, mod)` is the S2 SCALAR style (setup and test
are identities), the `gcd` precedent.

### Checklist status

1. **Corpus + differential** — landed by the wave (13 rows, 13 PASS).
   Untouched by this unit.
2. **Golden pin + Program term + named harness `rfl` pin** — landed by
   the wave. This unit ADDS a second `rfl` pin, `powMod_pin`, on the
   SUBJECT `Func` (the wave pinned only the harness), so both links of
   the golden chain are named for this example.
3. **Headline** — DONE. `powmod_ok` in the stub ROOT
   `proofs/GoLeanProofs/Examples/PowMod.lean`. `∃N-∀fuel-∀ch` over
   `runFunctionWithContextM`, returned values only, no heap/cell/seed
   vocabulary. Axioms `[propext, Classical.choice, Quot.sound]` — the
   trio — `#guard_msgs`-pinned with 8 others in the new shard
   `proofs/Audit/PowMod.lean` (wired from `Audit.lean`'s import list +
   shard roll-call). `powmod_readout` is the D1 twin.
4. **Fuel bound** — DONE. SHIPPED `N = 6027`, a CONSTANT over the whole
   domain, quoted as a bound. MEASURED, recorded separately: exactly
   `139 + 72·bits(exp) + 20·popcount(exp)` (probe-verified at
   `exp = 0, 1, 2, 5, 13`: 139 / 231 / 303 / 395 / 487), plus `59` on
   the `mod = 0` guard and `68` on the `mod = 1` guard. The two
   coincide only at `exp = 2^64 − 1`, where every pass is odd.
5. **Deletion test** — RUN, not asserted. `lean_minimal_hypotheses` on
   `powmod_ok`: ALL FIVE explicit binders (`base exp mod`, `hb`, `he`,
   `hm`, `hnw`) are LOAD-BEARING. Recorded honestly: `hm` is
   *logically* implied by `hnw` and is kept for statement uniformity;
   the proof does consume it, which is what the test reports.
6. **Gallery entry** — DONE. `docs/verified-examples.md`, full harness
   AND subject verbatim with `<!-- verbatim: -->` markers;
   `scripts/render-gallery` exit 0 (58 blocks, was 52). Claim ≤
   theorem. Domain bounds attributed separately (`base`/`exp`/`mod`
   `< 2^64` = Go's domain; `(mod−1)² < 2^64` = the program's own
   arithmetic, quoted from the Go comment; the exponentiation =
   mathematics; machine idealization stated). Doc counts updated
   (`Eight` → `Nine Go programs`; "six of the eight" → "seven of the
   nine"; the not-designated paragraph now names both undesignated
   entries).
7. **`scripts/proof-costs`** — DONE, both under the ~2.5 GiB bar:
   `GoLeanProofs.Examples.PowMod` 1.70 s / **1817 MiB**,
   `Audit.PowMod` 0.69 s / **1538 MiB**.
8. **`scripts/ci` PASS at the commit** — proofs-only (the wave's full
   differential record stands; this unit touches no `Corpus/`,
   `baselines/` or runtime code).

### The proof, in one paragraph

Entry → the `mod == 0` guard (21 steps) → the `mod == 1` guard (9) →
`result := 1` and the `base % mod` apply (25 + 1) → the `$forFirst`
block to the loop head (18) → first-pass dispatch (25) → **the loop**,
by induction on the exponent's BIT BUDGET → exit (40). One iteration is
92 steps when `exp` is odd and 72 when it is even; the two branches
share a 44-step tail (`pm_iter_tail`) covering the squaring, the
halving and the next dispatch. Six `applyStrictOp` steps per iteration
are data-dependent and each is discharged by a conditioned executable
fact. Two pure bridges carry the answer: `powLoop` (the loop's own
recursion) and `powLoop_eq` (that it computes `(r · bᵉ) mod m`), so the
headline states MATHEMATICS while the induction states the loop.

### Judgment calls (this unit)

- **JC (the spec is mathematics, not the loop):** the postcondition is
  `if mod = 0 then 0 else base ^ exp % mod`, with `powLoop` confined to
  the proof layer behind `powLoop_eq`. Principle: claim ≤ theorem cuts
  both ways — a spec that merely re-describes the program is a claim
  that says nothing, and the gallery exists to be READ. Cost: the
  modular-arithmetic bridge (~45 lines).
- **JC (the domain is the SOURCE's no-wrap condition, verbatim):** the
  hypothesis is `(mod − 1) * (mod − 1) < 2 ^ 64`, which is exactly the
  condition the Go comment states, rather than a rounder `mod ≤ 2^32`.
  Principle: honesty beats elegance — the theorem's domain and the
  program's documented domain are then the same sentence, and the wrap
  region is excluded in the open rather than by a convenient constant.
- **JC (all three guard paths in ONE theorem):** `mod = 0` and
  `mod = 1` are proved as machine paths rather than excluded by
  hypothesis, so the entry covers the whole documented domain below the
  wrap threshold. Cost: two extra raw segments and two short
  compositions (~35 lines). `mod = 1` needed no case in the SPEC,
  because `base ^ exp % 1 = 0` — the guard and the mathematics agree,
  and that is worth a reader's attention.
- **JC (bit-budget induction ⇒ a CONSTANT bound):** the loop induction
  is `∀ d, exp < 2 ^ d → k ≤ 92·d + 40`, instantiated at `d = 64`,
  rather than an induction on `exp` giving a bound linear in `exp`.
  Principle: bound tightness is a delegated judgment call, and `6027`
  is both tighter and far more legible than any `c·exp` would be.
- **JC (probe-first, not guess-first — the flagship's reflex, paid
  forward):** a step tracer + structured continuation printer
  (`.tmp/pmprobe.lean`, using `deriving instance Repr for Cont, Config`)
  was built before a single segment was written. Every step count and
  every continuation in this module came from the real machine. Result:
  **12 of the 16 raw segments were correct on the first compile**; the
  four that were not were two real transcription errors, found in one
  round. See the tooling note below — it is reusable by every lane.

### KIT GAPS (lane A, unit 1) — recorded, NOT fixed here

Per the lane's kit discipline: minimal local gap-witness code, marked
as such in the source; the operator consolidates across lanes.

- **GAP-A1 — `applyStrictOp_mul_u64` is example-local.** The uint64 `*`
  executable fact, conditioned on `a * b < 2 ^ 64` (below the wrap
  threshold), has no kit form; `SliceMem`'s executable-op family has
  `applyStrictOp_mod_u64` and `applyStrictOp_lessCmp_int` but no
  multiply. Nothing in the proof is example-specific — it is a
  statement about `applyStrictOp`, `intBinaryResult` and
  `IntKind.normalize`.
  *Shape wanted:* `SliceMem.applyStrictOp_mul_u64 {a b : Nat}
  (h : a * b < 2 ^ 64)`, beside `applyStrictOp_mod_u64`, in exactly its
  hypothesis style. Consumers: powmod (landed), **dotprod** (chartered
  — `acc += a[i] * b[i]` is the whole subject), **sieve** (chartered —
  `i * i <= n`). Cost of the gap here: ~10 lines.
- **GAP-A2 — `applyStrictOp_div_u64` is example-local.** Same story for
  `/`: the divide-by-zero check is the one data-dependent branch, the
  proof is a near-copy of the `%` fact's.
  *Shape wanted:* `SliceMem.applyStrictOp_div_u64 {a b : Nat}
  (hb : 0 < b) (ha : a < 2 ^ 64)`. Consumers: powmod (landed),
  **dedup** (chartered — the setup family is `seed + i/2`), and any
  future halving loop. Cost of the gap here: ~14 lines.
  - Note for the consolidator: A1 and A2 are ONE lift — the integer
    executable-op family completed (`+`, `-`, `*`, `/` at uint64, and
    the signed kinds `kadane` will want). Doing them singly would pay
    the same fixture cost three times.

### FINDINGS (byproduct stream, outside G1's four goals)

- **INFRA — `scripts/setup-deps` does not seed `proofs/.lake/packages`.**
  A fresh lane worktree's first proofs build tries to CLONE `iris-lean`
  and dies in the offline sandbox; `scripts/ci` then reports FIVE
  failures (`proofs + Audit gate`, `import-goose fixtures`,
  `golden lowering DRIFTED`, `imported-goose R2 pin DRIFTED`,
  `check-imported-pins`) whose single root cause is that clone. The
  drift lines are especially misleading — nothing has drifted. Fix used
  (and independently recommended by the debt lane the same day): copy
  `proofs/.lake/packages` from the primary checkout; the manifest pins
  verify integrity, and the two manifests were diffed as identical
  before copying. *Suggested:* `scripts/setup-deps --from <sibling>`
  should seed it, and `check-golden` / `check-imported-pins` should
  distinguish "pin module BUILD failed" from "pin DRIFTED" — the latter
  is a claim about content and this was not one.
- **TOOLING — the lane tracer, reusable and cheap.** `deriving instance
  Repr for Cont, Config` in a scratch file makes the whole machine
  printable, which turns segment transcription from guesswork into
  transcription: a per-step tag stream locates every segment boundary,
  and a full `repr` dump at a boundary gives the continuation to write
  down. Magic argument values (`700001, 700003, 700007`) make the
  symbolic slots obvious in the dump. Built in ~20 minutes; it is the
  reason 12/16 segments compiled first try. Every remaining lane-A
  example will reuse it verbatim.
- **PITFALL — `stepFnIter_chain` nesting must match `+`'s
  associativity.** `Nat` addition is LEFT-associated, so a fuel witness
  written `a + b + c + k` needs LEFT-nested `stepFnIter_chain`s. A
  right-nested chain does not unify syntactically, Lean falls back to
  defeq, and the failure surfaces as a `whnf` HEARTBEAT TIMEOUT rather
  than as a type error — which reads like an expensive proof and is
  actually a typo. Cost here: two debug rounds. Worth a line in the kit
  docstring.
- **PITFALL — well-founded `def`s want isolated equation lemmas.**
  Unfolding `powLoop` with `rw [powLoop]` inside a goal carrying a large
  machine term also timed out; extracting `powLoop_zero` and
  `powLoop_step` once and rewriting with those fixed it. Same class as
  above: a cost symptom with a structural cause.

### Costs

| artifact | lines | build |
|---|---|---|
| `Examples/PowMod.lean` (stub 60 → full) | 846 | 1.7 s |
| `Audit/PowMod.lean` | 92 | 0.7 s |
| `docs/verified-examples.md` (entry) | +118 | — |
| **total new Lean** | **878** | **~2.4 s** |

Peak memory: 1817 / 1538 MiB, both well under the ~2.5 GiB bar.
`scripts/render-gallery`: exit 0, 58 blocks (was 52).

Against the flagship's ~3,890-line, ~3.5 h histogram: powmod is **878
lines**. Most of the difference is the example (a scalar loop, no map,
no setup family, no fixed-cap array), but the kit-gap closure unit is
visible too — `derive_entry_eq` produced the entry equation from two
identifiers, `StepKit`'s `stepFn_strict_apply` / `stepFnIter_chain` /
`stepFnIter_one` carried every composition, and `SliceMem`'s
`unorm_nat_of_lt` / `applyStrictOp_mod_u64` carried the machine-integer
normal forms. The only kit-shaped things this example had to write
itself are the two gap-witness op facts above (~24 lines).

---

## Lane A — interim record: sizing the remaining five, and the delegation call

Written after unit G1.3 (`powmod`) landed, before its successors, so the
ordering decision is on the record rather than in a transcript.

### Measured sizing (probe, not estimate)

Total machine steps to the entry terminal, read off the real machine
with the lane tracer (`.tmp/size.lean`, `derive_entry_eq` +
`stepFnIter` search for `.next .stop`):

| example | style | measured total steps | shape |
|---|---|---|---|
| `powmod` | S2 scalar | 139 … 6027 (`exp = 2^64−1`) | 1 loop, 6 applies/iteration — **DONE** |
| `sieve` | S2 scalar | 55 (n=0) · 279 (n=2) · 1174 (n=10) · 3296 (n=30) · 6588 (n=60) | **nested** loops + a `make([]bool, n+1)` DYNAMIC allocation + a count loop |
| `fibmemo` | S2 scalar | 107 (n≤1) · 249 (n=2) · 731 (n=5) · 1581 (n=10) | **recursion** (growing frame stack) + a map + a `for range` |
| `dedup` | S3 relational | 361 (n=0) · 606 (n=1) · 1486 (n=5) · 2103 (n=8) | 4 loops; the compaction WRITES `s[k]` while READING `s[i]` |
| `kadane` | S3 relational | (delegated) | 3 loops, SIGNED int64 |
| `dotprod` | S3 relational | (delegated) | 4 loops, genuine mod-2⁶⁴ WRAP in the observable |

- **JC (order by measured difficulty, not by the style label):** the
  brief's order was `powmod · sieve · fibmemo` (the S2 scalars) then the
  S3s, on the reasoning that scalar harnesses are cheapest. The probe
  says the label describes the HARNESS, not the proof: `sieve` and
  `fibmemo` are the two HARDEST units in the lane, not the easiest.
  `sieve` needs a dynamically-sized slice in the state, a nested-loop
  induction whose inner trip count depends on the outer index, and — for
  a spec worth reading — the sieve's own correctness (a composite `k ≤ n`
  has a divisor `d` with `d·d ≤ n`), which is real number theory.
  `fibmemo` is RECURSIVE, so the machine state carries a continuation
  stack whose depth grows with `n`; the induction must be
  continuation-parametric, which nothing in the gallery has needed yet.
  The three S3 array examples, by contrast, are the shape the G1.1-KG
  kit closure was built for (`SliceMem.familyMod`/`prefixPad`,
  `stepFnIter_iterate`). Principle: the charter delegates ordering
  explicitly, and honest-gaps-over-grinds says spend the budget where
  completions actually are.
- **JC (delegate three in parallel, integrate centrally):** `kadane`,
  `dotprod` and `dedup` were delegated to three Fable workers against
  fully specified briefs (worker-model rule: Fable for proof slices),
  each owning EXACTLY two files — its example root and its Audit shard.
  Everything shared — `proofs/Audit.lean`'s import line,
  `docs/verified-examples.md`, this log — stays with the lane owner, so
  concurrent writes are file-disjoint by construction and no worker can
  make a claim in the gallery that the lane owner has not checked.
  Workers do not commit and do not run `scripts/ci`. Memory: each worker
  builds only its own module under `GOLEAN_MEM_MAX=16G` (a landed
  example module peaks near 1.8 GiB, so 16 G is generous), which keeps
  three concurrent lanes well inside the shared box.
- **JC (what each worker was given):** the PowMod module as a worked
  template, the tracer recipe, the kit inventory relevant to its shape,
  the two named pitfalls (chain associativity, well-founded unfolding),
  its known kit gaps NAMED IN ADVANCE (so a worker does not re-discover
  them as failures), the spec-quality bar (mathematics, not a
  restatement of the loop — the `powModAnswer`/`powLoop` split), and an
  explicit divergence guard with permission to report an honest gap.
  Principle: a delegated proof needs its theorem STATEMENT and its
  boundaries fixed before it starts, or the reviewer inherits a moving
  target (the flagship's delegation lesson, G1.1).

### Standing position on `sieve` and `fibmemo`

Both are **REMAINING, not attempted** — the honest word. They are not
recorded as gaps, because a gap is the outcome of an attempt and neither
has had one. What a successor inherits: the sizing above, the shape
diagnosis, the tracer, and the note that each of them wants a kit shape
the gallery does not yet have (`sieve`: a dynamically-allocated-slice
setup schema; `fibmemo`: a continuation-parametric recursion induction).
Either is a plausible next unit for a dedicated lane; neither is a
half-hour of tidying.

### Lane-A self-review correction (same day, before the workers landed)

Re-checking `powmod`'s own gallery text against arithmetic rather than
memory turned up a FALSE claim of my own, now fixed in the module
docstring and the gallery entry:

- **Claimed:** "the corpus rows `wrap` and `harness-extreme` pin [the
  wrap] region [that the theorem excludes]".
- **True:** the no-wrap condition `(mod−1)² < 2⁶⁴` is `mod ≤ 2³²`.
  `harness-extreme` has `mod = 999999937 < 2³²`, so it is INSIDE the
  theorem's domain — its extremity is in `base` and `exp`, not `mod`.
  Exactly ONE of the 13 rows, `wrap` (`mod = 2⁶³ − 1`), is outside.
- The corrected text says the stronger and true thing: **12 of the 13
  differential rows lie inside the theorem's domain**, and names the one
  that does not.
- Principle: honesty beats velocity, and "which rows does the theorem
  actually cover" is a claim like any other — it needed a computation,
  not a recollection. Recorded because the original error was the easy
  kind: two rows tagged `widths` in `cases.tsv`, read as "both are in
  the wrap region", when the tag is about the arguments' width and not
  about the modulus.

---

## Unit G1.4 — `dotprod` [lane A, delegated] — **COMPLETE**

Proved by a delegated Fable worker against the brief recorded above;
reviewed, re-verified and integrated by the lane owner. All eight
COMPLETE-checklist items met (1–2 landed by the wave).

### The example

**Go** (`Corpus/coverage/exec/examples/dotprod/main.go`): `dotProduct`
accumulates `acc += a[i]*b[i]` over `min(len(a), len(b))`, wrapping mod
2⁶⁴. Harness `dotprod_harness_r(n, seed)` is S3 RELATIONAL: one setup
loop builds BOTH families (`a[i] = seed+i`, `b[i] = i+1`), two copy
loops lift them into fixed-cap `[8]uint64` arrays, and the observable is
`(av, bv, dot)`.

### Checklist status

3. **Headline** — DONE. `dotprod_ok` in the ROOT
   `Examples/DotProduct.lean`; `∃av-∃bv-∃N-∀fuel-∀ch`, returned values
   only. Axioms `[propext, Classical.choice, Quot.sound]`, verified by
   the LANE OWNER from a fresh probe (not inherited from the worker's
   report) and `#guard_msgs`-pinned in `proofs/Audit/DotProduct.lean`,
   wired from `Audit.lean`. `dotprod_readout` is the D1 twin. A
   `dotProduct_pin` `rfl` pin on the SUBJECT `Func` was added, as in
   powmod.
4. **Fuel bound** — DONE. `N = 237·n + 398`, and here the bound is
   EXACT: every loop iteration is branch-free, so the composed count is
   an equality. Measured at `n = 0…8`: 398, 635, 872, 1109, 1346, 1583,
   1820, 2057, 2294 — coinciding. Per-loop: setup 70/iter (two stores),
   each copy loop 53/iter, accumulate 61/iter. Both facts stated;
   neither presented as the other, and the gallery says explicitly that
   they agree HERE and do not in powmod/histogram.
5. **Deletion test** — RUN by the LANE OWNER, not inherited. The worker
   had recorded it "by inspection of the proof term", which is an
   assertion, and the charter says RUN. `lean_minimal_hypotheses` on
   `dotprod_ok`: all THREE explicit binders (`n seed`, `hcap`, `hseed`)
   LOAD-BEARING. The shard's wording was corrected in the same pass —
   it also said "all four" of three binders.
6. **Gallery entry** — DONE. Subject + harness verbatim;
   `render-gallery` exit 0 (64 blocks, was 58). Doc counts updated
   (`Nine` → `Ten Go programs`; "seven of the nine" → "eight of the
   ten"; the not-designated paragraph now names three entries).
7. **`scripts/proof-costs`** — DONE, both under the ~2.5 GiB bar:
   `Examples.DotProduct` 17.00 s / **2090 MiB**, `Audit.DotProduct`
   0.67 s / **1485 MiB**. This is the most expensive example module in
   the lane so far and still has ~400 MiB of headroom.
8. **`scripts/ci`** — run at the lane's next commit boundary
   (proofs-only; the wave's differential record stands).

### What makes this entry worth reading

- **It is the WRAPPED-claim entry.** `powmod` excluded its wrap region
  by hypothesis; `dotprod` does the opposite and states the wrapped
  answer on the full `seed < 2^64` domain, so the four deliberately
  wrapping corpus rows are INSIDE the theorem rather than outside it.
  The specification is `(Σ aᵢ·bᵢ) mod 2^64` — ONE reduction of the true
  integer sum, not the machine's per-step wrapping — and the two are
  proved equal (`dpAcc_eq`, mod distributing over the sum) rather than
  conflated. The gallery now carries both stances side by side, each
  labelled.
- **An honesty disclosure the brief did not ask for.** The worker
  noticed that the subject's `min(len(a), len(b))` guard is never
  exercised by this harness (both slices have length `n`), and said so
  in the headline docstring and the shard: the theorem claims the
  harness's runs, and the `uneven` corpus row is what pins the guard.
  That is precisely the fail-closed-classification instinct the audit
  dimensions ask for, arrived at unprompted.

### Judgment calls (this unit)

- **JC (re-run the deletion test rather than accept it):** a delegated
  worker's "by inspection" is not the charter's "RUN". Re-running took
  two minutes and also caught a miscount ("all four" of three binders)
  in the shard docstring. Principle: the lane owner owns every claim
  that reaches the gallery; delegation moves work, not accountability.
- **JC (independent axiom re-probe):** the pins were checked against a
  fresh `#print axioms` run by the lane owner before wiring the shard,
  rather than trusting the worker's transcription. Principle: the pin
  is the claim; a transcription is not evidence of itself.

### KIT GAPS (unit G1.4) — recorded, NOT fixed here

- **GAP-A1-wrap — the WRAPPING uint64 `*` normalization identity.**
  Shipped as marked gap-witness `unorm_mul_nat :
  IntKind.normalize .uint64 ((a:Int)*(b:Int)) = ((a*b % 2^64 : Nat):Int)`
  (+ an `unorm_nat` helper). Note the useful negative finding: the
  CONDITIONED `applyStrictOp` wrapping-mul form was NOT needed —
  `mul`/`add` are total, so they ride inside `with_unfolding_all rfl`
  segments and only the NORMALIZATION identity is consumed. That is a
  different shape from powmod's `applyStrictOp_mul_u64`, which is
  needed because its `%` neighbours force a split.
  *Shape wanted:* `SliceMem.unorm_mul_nat`, beside the existing
  `unorm_add_nat` (which already covers the wrapping `+`). Consumers:
  dotprod (landed), and every future wrapping-product example.
- **GAP-P2b — the AFFINE setup family `seed + i` has no kit form.**
  `SliceMem.familyMod` is `seed + i%k`; `dpFamA` is `seed + i`, which is
  MinMax's `mmFamily` formula — the THIRD instance of the shape. ~40
  lines of `_length`/`_range`/`_succ`/`_set`/`_getD` were re-derived
  (the `prefixPad` pads themselves came from the kit unchanged).
  *Shape wanted:* `SliceMem.familyAff (n seed : Nat)` with the same five
  facts, or `familyMod` generalized so `k = 0` means "no modulus".
  Consumers: minmax (landed, but its name is DESIGNATED statement
  vocabulary — retrofit as a delegation only), dotprod (landed),
  kadane (in flight, same `seed + i` shape). Over the §12 bar.

### Costs

| artifact | lines | build | peak |
|---|---|---|---|
| `Examples/DotProduct.lean` (stub 268 → full) | 2042 | 17.0 s | 2090 MiB |
| `Audit/DotProduct.lean` | 115 | 0.7 s | 1485 MiB |
| `docs/verified-examples.md` (entry) | +105 | — | — |

Worker wall time: ~31 minutes. **All 23 raw `rfl` segments compiled on
the first full build** — the tracer-first method, handed over in the
brief, transferred intact to a delegated worker on its first use. The
four fixes needed were a mistyped continuation name, `as` being a
reserved token, one `push_cast` max-steps, and one wrong import path.

---

## Unit G1.5 — `kadane` [lane A, delegated] — **COMPLETE**

Proved by a delegated Fable worker; reviewed, re-verified and
integrated by the lane owner. All eight COMPLETE-checklist items met
(1–2 landed by the wave).

### Checklist status

3. **Headline** — DONE. `kadane_ok` in the ROOT `Examples/Kadane.lean`.
   Axioms `[propext, Classical.choice, Quot.sound]`, re-probed by the
   LANE OWNER before wiring; pinned in `proofs/Audit/Kadane.lean`.
   `kadane_readout` is the D1 twin; `kadane_pin` is the added SUBJECT
   `rfl` pin.
4. **Fuel bound** — DONE. SHIPPED `N = 227·n + 220` (branch-uniform:
   setup 86/iter on the negated index and 66 on the even one, copy 53,
   scan ≤ 88, plus the exits). MEASURED, recorded separately and NOT
   affine (the branches are data-dependent): at seed 5, `n = 0…8` →
   213, 427, 642, 845, 1060, 1263, 1478, 1681, 1896, cross-checked at
   seeds −7 and −100.
5. **Deletion test** — RUN by the LANE OWNER (the worker had checked it
   by hand). `lean_minimal_hypotheses` on `kadane_ok`: ALL FIVE
   explicit binders (`n`, `seed`, `hcap`, `hs1`, `hs2`) LOAD-BEARING.
   The shard's wording was corrected from "all three explicit
   hypotheses" to the run's actual verdict.
6. **Gallery entry** — DONE. `render-gallery` exit 0.
7. **`scripts/proof-costs`** — `Examples.Kadane` 7.60 s / **2291 MiB**,
   `Audit.Kadane` 0.81 s / **1662 MiB**. Under the ~2.5 GiB bar, and
   the closest to it in the lane — worth watching if this module grows.
8. **`scripts/ci`** — PASS at the lane commit.

### What makes this entry worth reading

- **The spec is the OBVIOUS quadratic definition, and the theorem is
  that the clever linear scan computes it.** `maxSubarraySum` enumerates
  every non-empty contiguous segment (`nePrefixes`/`segments`), sums
  each, takes the max. `kadCur`/`kadBest` — the scan — are proof-side
  and bridged. This is the strongest form of the "mathematics, not a
  restatement" bar the lane has produced: the two programs are visibly
  different algorithms.
- **The all-negative trap costs no clause.** `segments` are non-empty by
  construction, so the answer is the largest single element rather than
  `0`, without a special case; the `four-negative` row pins it.

### THE HONESTY POINT THE LANE OWNER ADDED

The worker's seed window `−2⁵⁹ ≤ seed ≤ 2⁵⁹` is a no-wrap window and is
NARROWER THAN THE CORPUS. Checking it against `cases.tsv` (arithmetic,
not recollection — the same discipline that caught the powmod error):
of the six relational-harness rows, FOUR are inside the theorem's
domain and TWO — `harness-r-maxseed` (`2⁶³−1`) and `harness-r-minseed`
(`−2⁶³`) — are OUTSIDE it. The gallery entry and the audit shard now say
so explicitly: *that is a real gap between what the oracle checks and
what the theorem claims*, and it is stated rather than elided. Neither
the worker's report nor its docstrings had drawn the comparison.

### KIT GAPS (unit G1.5)

- **GAP-I64 — the kit's executable-op family is uint64-ONLY.** kadane is
  the gallery's first signed example and had to gap-witness SIX int64
  mirrors: `inorm64_of_range`, `inorm64_nat_of_lt`, `getElem?_mapI`,
  `normalizeValueForTy_arr_i64`, `storeTarget_slice_i64`,
  `storeTarget_arrayLocal_i64`, plus `stepFn_makeSlice_i64_step`.
  *Shape wanted:* the SliceMem/StepKit store-and-index facts
  parameterized over `IntKind` and its range window, rather than i64
  twins — twins double the surface for every future kind.
  Consumers: kadane (landed), every future signed example.
  - **Sub-finding worth the operator's attention:** the worker had to
    re-prove `SliceMem`'s PRIVATE `validateSlice_ok` / `sliceIndexLoc_ok`
    to build the i64 store fact. The sealed-API rule says consumers
    state their needs against PUBLIC facts; here no public fact had the
    needed shape, so the seal forced duplication rather than preventing
    it. Either publicize those two, or (better) ship the kind-generic
    store fact above.
- **GAP-P2c — `familyMod`/`prefixPad` do not fit a SIGNED, Int-seeded
  family.** Their signature is `Nat → Nat → List Int`; kadane needs
  `Nat → Int → List Int` with an alternating sign and a TWO-STORE
  iteration (an intermediate list between the two stores).
  *Shape wanted:* `prefixPad` generalized to `Nat → Int → List Int`,
  plus a two-phase-store helper for the mid-list.

---

## Unit G1.6 — `dedup` [lane A, delegated] — **COMPLETE**

Proved by a delegated Fable worker; reviewed, re-verified and
integrated by the lane owner. All eight COMPLETE-checklist items met.

### Checklist status

3. **Headline** — DONE. `dedup_ok` in the ROOT
   `Examples/DedupAdjacent.lean`. Axioms
   `[propext, Classical.choice, Quot.sound]`, re-probed by the lane
   owner; pinned in `proofs/Audit/DedupAdjacent.lean`.
   `dedup_readout` is the D1 twin; `dedupAdjacent_pin` the SUBJECT pin.
4. **Fuel bound** — DONE. SHIPPED `N = 263·n + 361` (branch-uniform:
   every element charged the widest branch, the 98-step `k ≠ 0` keep,
   plus a 55-step post-copy slot). MEASURED separately and
   input-dependent: `361` at `n = 0`, `177·n + 86·K + 343` for `n ≥ 1`
   with `K` the survivor count (`⌈n/2⌉` for this family) —
   probe-verified at `n = 0,1,2,3,5,8` → 361, 606, 783, 1046, 1486,
   2103, and 2103 again at the wrap-boundary seed `2⁶⁴−2`. These match
   the lane's independent sizing probe exactly.
5. **Deletion test** — RUN by the LANE OWNER (the worker had checked it
   by hand, and miscounted three binders as four).
   `lean_minimal_hypotheses` on `dedup_ok`: all THREE explicit binders
   LOAD-BEARING. Shard wording corrected.
6. **Gallery entry** — DONE. `render-gallery` exit 0 (76 blocks total,
   was 58 before this integration round).
7. **`scripts/proof-costs`** — `Examples.DedupAdjacent` 5.50 s /
   **2262 MiB**, `Audit.DedupAdjacent` 0.78 s / **1717 MiB**. Under the
   bar.
8. **`scripts/ci`** — PASS at the lane commit.

### What makes this entry worth reading

- **"Adjacent" is the entry's whole content**, and it is what readers
  get wrong: `1,2,1,2` survives INTACT. The spec says "kept iff first,
  or differs from the last KEPT one", the `four-alternating` row pins it
  against real Go, and the gallery says plainly that a "distinct values"
  spec would be a different and false claim.
- **A genuine machine finding, recorded:** the guard
  `k == 0 || s[i] != s[k-1]` is LAZY in the machine (`Expr.or` via
  `Cont.orK` + `Cont.boolK`), so on the `k == 0` branch the `s[k-1]`
  read — the out-of-range index a reader worries about — is never
  evaluated. The raw segments show it directly. This is exactly the
  class of fact the differential cannot report and reading can.
- **In-place mutation carried by one invariant:** after `i` elements
  with `k` kept, the slice is `dedupAdj (take i)` ++ the untouched
  original tail; the stale region `[k, i)` is never read again. The
  backing list stays existential, so the proof never has to describe the
  stale bytes.

### KIT GAPS (unit G1.6)

- **GAP-P2d — `familyMod` does not fit `seed + i/k`.** *Shape wanted:*
  `SliceMem.familyDiv (k n seed)` with the same five facts;
  `ddFamily` is its `k = 2` instance and its proofs mirror `familyMod`
  verbatim. Useful positive finding: **`prefixPad` itself IS
  family-generic and worked unchanged** — only its `_set` lemma is
  family-specific, so the lift is small.
- **GAP-A2 (second consumer) — `applyStrictOp_div_u64`** was re-derived
  from powmod. It now has TWO landed consumers and is promotable
  immediately.
- **New op-fact candidates the kit lacks** (each with a consumer here):
  `applyStrictOp_eqCmp_int` / `neqCmp_int` (note: `with_unfolding_all
  rfl` is needed — plain `rfl` does not close `valueEq`),
  `applyStrictOp_sub_int`, `applyStrictOp_convert_u64`, and
  `applyStrictOp_sliceExpr_slice` (the kit's slice-expression fact
  covers only an ARRAY-pointer base, not a SLICE base).

---

## Lane A — process finding: `git add -A` is unsafe with concurrent workers

Recorded because it produced a wrong commit and a worker caught it.

The `dotprod` unit was first committed with `git add -A proofs docs`,
which swept INTERMEDIATE snapshots of two other workers' in-flight
files (`Kadane.lean`, `DedupAdjacent.lean`) into a commit whose subject
says "dotprod COMPLETE". No false gate claim followed — the gate was
deliberately deferred to the next boundary — but the commit did not
contain what its message said, and a bisect through it would have hit
half-written proofs.

Fixed by `git stash create` → `refs/snapshots/gc-proofs-a-preamend`
(the recorded crash rule, so the workers' dirty files were recoverable),
then `git reset --soft HEAD~1` + `git restore --source=<prev> --staged`
on the two files — INDEX-ONLY surgery, worktree untouched, so the
running worker never noticed — and a re-commit with explicit paths.

**Rule for any future multi-worker lane: stage by explicit path, never
`git add -A`/`-u`.** The worktree is shared; the index is the only place
the lane owner's intent is expressed, and `-A` overrides it with
"whatever anyone happened to have saved".

### Lane-A self-review correction #2 — the gallery's own counting

Verified the gallery's count claims by counting rather than trusting the
running edit. Two findings:

- **`Twelve Go programs` is correct** — 12 `##` example sections
  (fib, gcd, reverse, minmax, binsearch, isort, wordcount, histogram,
  powmod, dotprod, kadane, dedup), and "ten of the twelve" for the
  cheap-`∀ ch` claim is right (only wordcount and histogram consume
  choices).
- **An ordinal I introduced was ambiguous and had to go.** I had written
  "the entries after the eighth" for the undesignated set. Designation
  counts HEADLINES, not sections: the designated list holds EIGHT
  theorems across SEVEN sections, because `fib` carries two
  (`fib_ok` and `fib_total`). So "the eighth" means one thing for
  headlines and another for sections, and histogram is the 8th section
  but the 9th headline. Replaced with "the five newest entries",
  naming them, plus a parenthetical stating the 8-headlines-over-7-
  sections fact so a reader can reconcile the two counts.
- Principle: a count in the object of agreement is a claim. Both of this
  lane's self-review corrections were of exactly this kind — a sentence
  that was easy to write and had never been checked against arithmetic.

---

## PROOF LANE A2 — `stack · queue · matmul`

Lane A2 runs the proof half of three more wave entries, in parallel with
lane B and the hard lane. It owns exactly its three example modules,
their Audit shards, their gallery sections and these `[lane A2]`
entries — no `Corpus/`, no `baselines/`, no `scripts/`, no kit files, no
other example, no designation surface.

### Lane-level setup: the statements were CHECKED before they were delegated

Before any worker started, the lane owner fixed all three theorem
statements and then EXECUTED THEM against the real machine — running
`runFunctionWithContextM` at fuel 100000 and comparing the returned
`Result` to the fixed postcondition:

- `stack`: 9 argument points (the corpus's own `(n, seed, k)` shapes)
  × 3 choice streams — all MATCH; plus the true uint64 wrap region
  (`seed = 2^64−1` at `n = 1,3,8`; `2^64−2` at `n = k = 8`) — all MATCH.
- `queue`: the same, on its own 9 shapes — all MATCH.
- `matmul`: `seed = 0, 1, 5, 7, 2^63−1, 2^64−1, 2^64−2, 2^64−6` × 2
  streams — all MATCH.

- **JC (execute the statement before delegating it):** a delegated proof
  needs its statement fixed before it starts, and the cheapest way to
  know a fixed statement is TRUE is to run it. Principle: `#eval` before
  you `decide`, applied one level up — to the theorem rather than to a
  tactic. Cost ~15 minutes; benefit: three workers each knew that a goal
  which would not close was a proof-structure problem and never a reason
  to weaken the postcondition. This is now the lane's recommended
  practice for any delegated headline.

### THE LANE'S HEADLINE FINDING — `append` is a nondeterminism site, and
### the first brief was WRONG about it

The lane owner's first briefs told the `stack` and `queue` workers that
`∀ ch` was VACUOUS for their examples and handed them a concrete table
of backing addresses and capacities to unroll against. **Both claims
were false, and they were caught by probing rather than by reading.**

What the machine actually does (probe, `stack` harness):

| ch | total steps | `nextAddr` | choices consumed | final `s` header |
|---|---|---|---|---|
| `[]` | 1282 | 77 | 0 | base 48, len 8, cap 8 |
| `[0]*6` | 1282 | 77 | 2 | base 48, len 8, cap 8 |
| `[299]*6` | 1282 | **76** | **1** | base **19**, len 8, cap **15** |
| `[3]*6` | 1282 | 77 | 2 | base **69**, len 8, cap **17** |

- A SPILLING `append` consumes ONE choice; an append that fits its
  capacity consumes none. So even the NUMBER of choices a run consumes
  is choice-dependent — an early large capacity suppresses a later
  spill.
- The realized capacity is an ENVELOPE, not a number:
  `[newLen, max 32 (2 · appendGrowthCap oldCap newLen)]`
  (`GoLean/GoCore/Ops.lean`), because gc's realized capacity depends on
  element size through size-class rounding and the compiler's 32-byte
  stack buffer — mechanisms an element-size-free growth formula cannot
  see. The corpus row `slices/append-spill-size-class` pins this at
  `stage=membership` with 300 enumerated members.
- **The step count is choice-INVARIANT** (1282 at every stream): an
  `append` is ONE machine step whether it spills or not. This is what
  lets a single fuel bound cover every stream.
- The heap layout is NOT choice-invariant: backing address, capacity and
  allocation front all move.

Cross-checked against the reference implementation: the default-stream
capacity sequence for `[]uint64` is `0 → 4 → 8 → 16`, which a `go run`
probe reproduces EXACTLY. So the machine's default point is gc's, and
the envelope around it is the honest latitude, not a divergence.

- **JC (correct the workers mid-flight rather than let it land):** the
  correction went out the moment the probe came back, before either
  worker had written a push-loop segment. Principle: the two bounds —
  a proof pinned to one member of an envelope is a true statement about
  that member and a FALSE claim about `∀ ch`, and `∀ ch` is what the
  headline says. The cost of catching it late is the whole proof.
- **What it changed:** the push/enqueue-loop invariant had to become
  capacity- AND address-GENERIC (existential backing address and
  capacity, never named), and the "`∀ ch` is vacuous" honesty clause —
  which the brief had pre-written — became its opposite. `stack` and
  `queue` are **the gallery's first non-map examples with a non-vacuous
  choice quantifier**; until now only `wordcount` and `histogram`
  consumed choices, both via map-range order.
- **Standing consequence for another lane:** `rle` (RunLength) is the
  only other wave example that calls `append` (twice). Whoever proves it
  inherits this finding and must NOT write "∀ ch is vacuous". No LANDED
  gallery entry is affected — checked by grepping every example's
  `main.go`: only `stack`, `queue` and `rle` call `append` at all.

---

## Unit G1.7 — `stack` [lane A2, delegated] — **COMPLETE**

Proved by a delegated Fable worker against the brief above (plus the
mid-flight correction); reviewed, re-verified and integrated by the lane
owner. All eight COMPLETE-checklist items met (1–2 landed by the wave).

### Checklist status

3. **Headline** — DONE. `stack_ok` in the ROOT
   `Examples/SliceStack.lean`. `∃pushed-∃N-∀fuel-∀ch` over
   `runFunctionWithContextM`, returned values only. Axioms
   `[propext, Classical.choice, Quot.sound]` — the trio — **re-probed by
   the LANE OWNER from a fresh `#print axioms` run**, not inherited from
   the worker's report, and `#guard_msgs`-pinned with 10 others in the
   new shard `proofs/Audit/SliceStack.lean` (wired from `Audit.lean`'s
   import list + shard roll-call). `stack_readout` is the D1 twin;
   `push_pin`/`pop_pin`/`size_pin` are the added SUBJECT `rfl` pins.
4. **Fuel bound** — DONE. SHIPPED `N = 257·n + 254`, quoted as a BOUND.
   MEASURED and separately PROVED as an equality inside the module:
   `242 + 130·n + 127·min(k,n) + 12·[n < k]` — probe-verified at
   `n = 0…8` with `k` under, at and over `n`, and confirmed
   choice-invariant at three streams. The two coincide exactly when
   `k > n` (the bound is ATTAINED on the drain-everything rows) and the
   bound is loose by `127·(n − k) + 12` when `k ≤ n`. Both stated;
   neither presented as the other.
5. **Deletion test** — RUN by the LANE OWNER, not inherited. The worker
   reported it "by inspection" and as "all three explicit hypotheses";
   `lean_minimal_hypotheses` on `stack_ok` reports **all FOUR explicit
   binder groups LOAD-BEARING** — `(n seed k)`, `hcap`, `hseed`, `hk`.
   The shard's wording was corrected to the run's actual verdict, with
   the honest sub-finding the run exposed: `hseed` is NOT consumed
   inside the internal `st_run` (the machine wraps every stored value
   regardless) and bites only at the entry normalization.
   **Sharpened at the lane finish** (probe, not inspection): the four
   binder groups are load-bearing in three DIFFERENT senses, and the
   single word hid the difference. `hcap` is a TOTALITY bound — at
   `n = 9` the subject panics (`index out of range [8] with length 8`),
   so the `= .ok …` conjunct fails outright. `hk` is a TRUTH bound —
   at `n = 3, seed = 5, k = 2^64` the machine normalizes `k` to `0`,
   pops nothing and returns size `3`, against a postcondition that
   reads `k` as the Nat `2^64`: a witnessed counterexample. `hseed` is
   a PROOF-STRUCTURE bound only — at `n = 3, seed = 2^64, k = 2` the
   machine still MATCHES the postcondition, because the entry
   normalization and `stFam` both reduce mod 2^64 and move together.
   Shard and gallery both corrected. **Every one of those three probe
   claims was independently RE-RUN by the lane owner before it shipped**
   — `n = 9` panics with that message verbatim (`MATCH` at `n = 8`),
   `k = 2^64` is a witnessed MISMATCH, `seed = 2^64` still MATCHes — on
   the standing rule that an empirical assertion in an
   object-of-agreement document is a claim like any other. Principle:
   `lean_minimal_hypotheses` answers "does the proof consume it", which
   is NOT "is the claim false without it" — reporting the first in the
   vocabulary of the second is the same class of error as the
   `emptyPops` fabrication below, and it is one `#eval` away from being
   checked.
6. **Gallery entry** — DONE. Subject + harness verbatim;
   `scripts/render-gallery` exit 0 (**82 blocks, was 76**). Doc counts
   updated and COUNTED, not recalled: `Twelve` → `Thirteen Go programs`
   (13 `##` example sections, verified by counting), the
   not-designated paragraph now names six entries and its own shard
   path, and the *What "∀ choices" means* section was REWRITTEN — it had
   said "ten of the twelve … their runs consume no choices", which is
   still ten but now of thirteen, and it now separates the two ways the
   quantifier does work (map order; `append` capacity).
7. **`scripts/proof-costs`** — measured at the lane tip (below).
8. **`scripts/ci`** — run at the lane's commit boundary (proofs-only;
   the lane's own full differential record was recorded first — see
   *Gate state* below).

### THE CORRECTION THE LANE OWNER MADE TO A LANDED CLAIM

The worker's shard and module docstring both described the third
returned value as "the empty-pop count `n − k`, truncating at zero
exactly as the Go's `emptyPops` counter (which only increments when a
pop finds the stack empty) does".

**There is no `emptyPops` counter.** Grepped: the identifier does not
occur in `Corpus/coverage/exec/examples/stack/main.go`, nor anywhere in
`Corpus/`. The Go returns `size(s)` — `uint64(len(s))` — the REMAINING
stack size, and its pop loop runs `min(k, n)` times, so it never pops an
empty stack at all. The sentence was a fabricated mechanism attached to
a correct formula.

Fixed in both files: the value is named as the remaining stack size, and
the Nat-subtraction truncation is explained by what the Go actually does
(pops `min(k,n)`, returns `len`). Recorded because of what it is: **the
formula was right and the story about it was invented**, which is the
failure mode a docstring review catches and a green build never will.
Principle: the lane owner owns every claim that reaches the gallery;
delegation moves work, not accountability.

### What makes this entry worth reading

- **It is the `∀ ch` entry.** See the lane finding above: the first
  non-map example where the choice quantifier is not decoration, and the
  first proof in the gallery that is generic in a heap layout rather
  than pinned to one.
- **The spec is two list operations.** LIFO is `pushed.reverse.take k`
  and the remainder is Nat `n - k`; `List.take` supplies the `min` for
  free, so no `min` appears in the statement. The sibling `queue` entry
  is the same sentence with `.reverse` deleted — the pair is the
  teaching point, which is why the wave built both.

### KIT GAPS (unit G1.7) — recorded, NOT fixed here

- **GAP-APPEND (new class) — nothing in the kit covers a GROWING
  slice.** Shipped as marked gap-witness: `st_append_inplace` (the
  `len < cap` path: in-place `List.set` on the backing, no choice
  consumed) and `st_append_spill` (the `len = cap` path: one choice
  consumed via `Choices.consume (appendSpillWidth …)`, fresh backing at
  `σ.nextAddr`, capacity the envelope member the stream picks).
  *Shape wanted:* both facts in `SliceMem`, parameterized over the
  element `IntKind`, with the spill fact stated so the consumer never
  names the realized capacity. Consumers: stack (landed), **queue**
  (same lane, same two facts re-derived independently), **rle**
  (chartered, calls `append` twice). Three consumers — over the §12 bar
  on arrival.
- **GAP-RESLICE (new class) — slice expression on a SLICE base.**
  `st_sliceExpr_slice`: `s[:len(s)-1]` where the operand is already a
  slice value. The kit's slice-expression fact covers only an
  ARRAY-POINTER base. (This is the same gap `dedup` recorded from the
  other side as `applyStrictOp_sliceExpr_slice`; this is its second
  independent consumer.)
  *Shape wanted:* the slice-base form beside the existing pointer-base
  one. Consumers: dedup (landed), stack (landed), queue (same lane).
- **GAP-FRAME (small) — `stepFn_return_frame`**, the conditioned
  `.returning`-at-`.frame` exit with result-cell read-back, re-derived
  locally. Consumers: stack, queue.
- **GAP-P2b (4th consumer)** — the affine family `seed + i` again
  (`stFam`). Already recorded by dotprod/kadane; referenced, not
  re-reported.

### Costs

| artifact | lines | build | peak |
|---|---|---|---|
| `Examples/SliceStack.lean` (stub 226 → full) | 6919 | 152.2 s | **2540 MiB** |
| `Audit/SliceStack.lean` | 146 | — | — |
| `docs/verified-examples.md` (entry) | +139 | — | — |

**Measured first-hand by the lane owner** (`/usr/bin/time -v` on a
capped `lake env lean` of the module): `wall=152.17s peak=2601296kB`,
i.e. **2540 MiB = 2.48 GiB**.

**CORRECTION — this is UNDER the ~2.5 GiB bar, not over it.** Both the
worker's report and this log's first draft said "2.6 GiB" and called it
a breach. That was a UNIT SLIP: `/usr/bin/time`'s `%M` is in KiB, so
`2601296 kB` is 2601296/1024 = 2540 MiB, and the bar is 2.5 GiB =
2560 MiB. Reading the same number as decimal GB (2601296/10^6 ≈ 2.6)
produced a "GiB" figure that was neither the right unit nor the right
verdict. Recorded rather than silently fixed, because the direction of
the error is the interesting part: **it invented a breach that did not
happen** — the same "easy sentence, never checked" failure as the other
three corrections in this lane, and the only one where the uncorrected
version was PESSIMISTIC. A number in the record is a claim whichever way
it points.

Honest standing: at 2540 MiB `stack` is the **closest any gallery entry
has come to the bar** (previous high-water: `kadane` 2291 MiB, `dedup`
2262 MiB), with ~20 MiB of headroom, and it is the largest example
module by a wide margin among the pre-lane entries (6,919 lines against `dedup`'s
2,921) — the direct cost of the capacity-generic push loop plus the
nine-way exit analysis. Its sibling `queue` is larger still (8,498
lines) and peaks LOWER (2313 MiB), so the driver is segment shape, not
size. The next slice-growth example starts from here, and it does not
have much room.

---

## Lane A2, finisher session — what the finisher checked, and one near-miss

The lane's coordinator ended without committing, leaving eight
uncommitted paths and a precise handoff. This section records what the
finisher VERIFIED rather than inherited, and one process error worth
more than the time it cost.

### Re-verified from scratch, not inherited

Every number below was produced by the finisher against the real
machine or a real build, at the lane tip:

- **`stack` axioms** — `lake build Audit.SliceStack` green, so all
  eleven `#guard_msgs #print axioms` pins in the shard were re-checked
  by the elaborator at this tip. Nothing about the trio is quoted from
  the worker's report.
- **`stack` fuel** — probe: `242 + 130·n + 127·min(k,n) + 12·[n<k]`
  confirmed at (n,k) = (0,0) 242, (0,3) 254, (1,0) 372, (1,1) 489,
  (1,3) 501, (3,2) 866, (3,3) 983, (3,5) 995, (8,0) 1282, (8,4) 1790,
  (8,8) 2298, (8,9) 2310. The shipped bound `257·n + 254` is exactly
  attained at `k > n` (2310 at n=8) — as claimed.
- **`stack` choice-invariance** — 1790 steps at `n=8, k=4` under `[]`,
  `[299]*6` and `[3]*6`; `nextAddr` 97/96/97, so the layout moves and
  the count does not. As claimed.
- **`stack` postcondition** — MATCH at nine argument points including
  `seed = 2^64−1`, `2^64−2` and `2^63−1`.
- **`stack` deletion test** — re-run as a PROBE and the verdict
  sharpened; see the correction folded into unit G1.7 item 5 above.
- **`queue` postcondition** (the not-yet-proved headline, as a
  prediction) — MATCH at eleven argument points including the wrap
  region, across four choice streams; step formula
  `242 + 130·n + 117·min(k,n) + 12·[n<k]` confirmed at thirteen (n,k)
  points, choice-invariant at 1750 steps for `n=8,k=4` while the
  consumed-choice count moves (1 vs 2) and `nextAddr` moves (96 vs 97).
  The bound `247·n + 254` is exactly attained at `k > n` (2230 at n=8,
  k=9). Handed to the worker as ground truth so a goal that would not
  close could never be mistaken for a wrong statement.
- **`matmul` postcondition** — MATCH at eight seeds (`0, 1, 5, 7,
  2^63−1, 2^64−1, 2^64−2, 2^64−6`) and three choice streams, against a
  `matSpec` re-implemented INDEPENDENTLY in the probe rather than
  imported from the module — so the check does not inherit the
  module's own spec. **Minimum successful fuel is exactly 5247** at
  seeds `0`, `5`, `2^63−1`, which is the shard's "bound and
  measurement coincide" claim, confirmed.

### THE NEAR-MISS — a stale declaration listing is not the file

The finisher read `Examples/MatMul.lean` early in the session, listed
its declarations, and concluded (correctly, AT THAT MOMENT) that the
module had a machine layer and no headline. On that basis — plus the
measured elaboration cost — it trimmed the module back to a cheap
groundwork form and wrote an honest-gap record.

**The file had grown by 375 lines in between, and those lines were
`matmul_ok` and `matmul_readout`.** The trim deleted a completed
headline. It was recovered intact because the WIP had been snapshotted
to `refs/snapshots/gc-proofs-a/matmul-machine-layer` FIRST, one command
before the destructive edit, and the recovery was exact
(`git show <ref>:MatMul.lean`).

- **JC (snapshot before you cut, always):** the snapshot was taken out
  of routine caution about discarding someone's uncommitted work, not
  because anything looked wrong. It converted an unrecoverable mistake
  into a two-minute one. This is the crash rule from CLAUDE.md applied
  to a non-crash: any destructive edit to work you did not write gets a
  ref first.
- **The actual lesson is narrower and sharper:** a `grep` of
  declarations is a snapshot of a file at a time, and it does not
  expire visibly. Re-`stat` (or re-grep) the file immediately before
  acting on a structural conclusion about it, especially in a worktree
  where another agent may be writing. The `mtime` was right there and
  said 08:36; it was read and not connected to the 2,000-line listing
  taken at 08:20.

---

## Unit G1.9 — `matmul` [lane A2] — **HONEST GAP, NOT LANDED**

The example is NOT in the gallery and its headline is NOT in the built
tree. What follows is the precise state, the blocker, and the pickup
plan, because the work is real and the reason it is not landed is a
cost measurement rather than a missing idea.

### What exists (preserved, recoverable, not built)

`refs/snapshots/gc-proofs-a/matmul-machine-layer` holds two files:

```
git show refs/snapshots/gc-proofs-a/matmul-machine-layer:MatMul.lean
git show refs/snapshots/gc-proofs-a/matmul-machine-layer:Audit-MatMul.lean
```

* **`MatMul.lean`, 2,375 lines** — the harness and both subject `Func`s
  with their three `rfl` lowering pins; the statement vocabulary
  (`mmGet`, `matSpec`, `mmArr3`); the machine vocabulary (`mmSet` —
  the machine's OBSERVED nested-array store composite — `mmNorm`,
  `mmAcc`, `unn`); the entry equation; the three `enterFrame` facts;
  the whole segment layer (both `seedMat` frames unrolled, all nine
  `matMul` accumulator cells, the final readback); the arithmetic
  bridges `un_idem` / `unn_collapse` / `unorm_mul_nat` /
  `mm_a_final` / `mm_c_final`; the 82-segment assembly; and
  **`matmul_ok` + `matmul_readout` in full, with no `sorry`**.
* **`Audit-MatMul.lean`, 126 lines** — the finished per-example shard
  with the honesty clauses and the `#guard_msgs` axiom pins.

### THE BLOCKER — it has never been elaborated, not once

**No build has ever checked this module.** Three independent whole-file
elaborations were started and none returned:

| run | how | outcome |
|---|---|---|
| `lake env lean MatMul.lean` | 2026-08-15 08:36 | still running at **57 min**, cut |
| `lake build` (full package) | 2026-08-15 08:22 | still running at **66 min**, cut |
| `lake build GoLeanProofs.Examples.MatMul` | 2026-08-15 09:46 | still running at **1 h 54 m**, cut by the finisher |

Supporting cost measurements from the unit's own profiling scratch:
single `with_unfolding_all rfl` segments over the 291-step `seedMat`
inner loops timed at **61 s / 129 s / 307 s / 310 s / 326 s** across
five reformulations, and one profiling probe hit the **16 M-heartbeat
`whnf` ceiling outright** at `MatMul.lean:709`.

So the honest status is not "it fails" and not "it works" — it is
**UNCHECKED**, and an unchecked proof is not a proof. Nothing about
`matmul_ok` may be claimed anywhere until a build closes it, which is
why the gallery section and the Audit shard were both withdrawn rather
than shipped: a gallery entry quoting an axiom pin that no build has
ever evaluated would be the worst artifact this campaign could produce.

### What IS known about matmul, independently

The finisher probed the real machine with a `matSpec` re-implemented in
the probe (NOT imported from the module, so the check does not inherit
the module's own spec):

* the postcondition MATCHES at seeds `0, 1, 5, 7, 2^63−1, 2^64−1,
  2^64−2, 2^64−6`, and at three choice streams;
* **the minimum fuel at which the run succeeds is exactly 5247** at
  seeds `0`, `5`, `2^63−1` — the shard's "bound and measurement
  coincide" claim, confirmed;
* `∀ ch` is genuinely vacuous here (no `append`, no map range).

So the STATEMENT is almost certainly true. That is evidence about the
subject, not about the proof.

### What DID land: cheap groundwork

`Examples/MatMul.lean` in the built tree is a **336-line groundwork
root** that builds in **963 ms**: the harness transcription plus BOTH
subject `Func`s (`seedMat`, `matMul`) in readable compositional form,
all three tied to the pinned lowering by `rfl`
(`matmulHarnessRFunc_pin`, `seedMat_pin`, `matMul_pin`), and the
statement vocabulary a headline will be written in. That is the corpus
half of the checklist, one step past where the guardrails wave left it,
and it costs the gate nothing. Its docstring says GROUNDWORK ONLY in
its first line and points at the snapshot ref.

### Pickup plan (the blocker is segment SHAPE, not a missing fact)

1. Do not re-derive anything: every fact needed for the headline is in
   the snapshot, including the assembly and the arithmetic bridges.
2. Attack the `seedMat` inner-loop segments first — they are the
   measured hotspots. The lever is splitting them at the store
   boundaries (each 291-step iteration covers nine `m[i][j]` stores) or
   replacing raw `with_unfolding_all rfl` with a CONDITIONED store
   lemma stated over a symbolic heap, the way the `stack` and `queue`
   modules do it. `rfl` over a 291-step window with a concrete nested
   array is the pathological shape.
3. Re-measure per segment with `--profile` BEFORE reassembling; the
   whole-file run is not a debugging loop at this cost.
4. Only then re-land the shard, and only then write the gallery entry.

- **JC (cost is a landing criterion, not a footnote):** a module that
  cannot be elaborated cannot be gated, and a module that takes an hour
  taxes every future `scripts/ci` in the campaign for as long as it
  lives. The lane's ~2.5 GiB memory bar has a wall-time sibling and this
  unit is what discovered it. G1 stands at twenty headlines; buying a
  twenty-first at that price — with the proof still unchecked — would
  have been a bad trade.

---

## Unit G1.8 — `queue` [lane A2, delegated ×2] — **COMPLETE**

The lane's second slice-growth entry and the `stack` mirror. Started by
one delegated worker (enqueue phase, the `append` facts, the dequeue
callee), finished by a second after the coordinator ended; integrated,
re-verified and claimed by the lane finisher. All eight
COMPLETE-checklist items met.

### Checklist status

3. **Headline** — DONE. `queue_ok` in the ROOT `Examples/SliceQueue.lean`,
   `∃enqueued-∃N-∀fuel-∀ch` over `runFunctionWithContextM`, returned
   values only, with `queue_readout` as the D1 twin derived through
   `harness_readout_of_total`. Axioms `[propext, Classical.choice,
   Quot.sound]` — the trio — checked IN BUILD by the shard's
   `#guard_msgs` pins at this tip, not quoted from a worker report.
4. **Fuel bound** — DONE. SHIPPED `N = 247·n + 254` as a BOUND. MEASURED
   and separately PROVED as the exact count of `q_run`:
   `242 + 130·n + 117·min(k,n) + 12·[n < k]`. The finisher confirmed the
   measurement independently at thirteen `(n,k)` points before the work
   was delegated — (0,0) 242, (0,3) 254, (1,0) 372, (1,1) 489, (1,3) 501,
   (2,1) 619, (3,2) 866, (3,3) 983, (3,5) 995, (8,0) 1282, (8,4) 1750,
   (8,8) 2218, (8,9) 2230 — and the shipped bound is exactly attained on
   the `k > n` rows (2230 at `n = 8, k = 9`), loose by `117·(n−k) + 12`
   when `k ≤ n`. The per-iteration asymmetry with `stack` is real and has
   a cause: 130 steps per enqueue against **117** per dequeue, because
   `q[1:]` re-slices a header instead of copying.
5. **Deletion test** — RUN by the lane owner, as a MACHINE PROBE rather
   than `lean_minimal_hypotheses`, and the three named hypotheses come
   back load-bearing in three DIFFERENT senses (the distinction the
   stack entry's correction introduced, now applied from the start):
   `hcap` TOTALITY (`n = 9` panics, `index out of range [8] with length
   8`), `hk` TRUTH (at `k = 2^64` the machine returns
   `dequeued = [0,…,0]`, size `3` — a witnessed counterexample, printed),
   `hseed` PROOF-STRUCTURE only (at `seed = 2^64` the machine still
   matches; entry normalization and `qFam` reduce mod 2^64 together).
6. **Gallery entry** — DONE. Subject + harness verbatim;
   `scripts/render-gallery` exit 0 (**88 blocks, was 82**). Doc counts
   updated and COUNTED, not recalled: `Thirteen` → `Fourteen Go
   programs` (14 `##` example sections, counted), "ten of the thirteen"
   → "ten of the fourteen … for the other four", the `append`-capacity
   paragraph now names both `stack` and `queue` and states that the
   dequeue half is choice-free, and the not-designated paragraph names
   seven entries with `queue`'s probe-style deletion test spelled out.
7. **`scripts/proof-costs`** — RECORDED at the lane tip:
   `GoLeanProofs.Examples.SliceQueue` **137.00 s / 2313 MiB**,
   `Audit.SliceQueue` 0.81 s / 1454 MiB. **Under the ~2.5 GiB bar**
   (2313 MiB = 2.26 GiB). Independently, the module's own build line
   reports 134 s and a 2.16 GiB process-tree peak, the same measurement
   from the other side.
   **Correction to this bullet's first draft**, which said `SliceStack`
   "is 1,568 lines SMALLER and went OVER it at 2.6 GiB". The SIZE half
   is right (`SliceStack` 6,919 lines vs this module's 8,498 — smaller
   by 1,579); the BAR half is wrong. `SliceStack`'s first-hand
   measurement is 2540 MiB = 2.48 GiB, UNDER the 2560 MiB bar; "2.6
   GiB" was a KiB-vs-decimal-GB unit slip. See unit G1.7's Costs
   section.
   The point the bullet was reaching for survives, and is sharper the
   right way round: the SMALLER module (`stack`, 6,919 lines) peaks
   HIGHER (2540 MiB) than the LARGER one (`queue`, 8,498 lines, 2313
   MiB). Line count is not the driver; segment shape is.
   Recorded with a wince: this correction's own first draft claimed the
   size comparison had the sign backwards, which it did not — an error
   introduced while fixing an error, caught by running `wc -l` instead
   of trusting the sentence. Same failure mode, one level up.
8. **`scripts/ci`** — PASS at the lane commit (see *Gate state*).

### What makes this entry worth reading

- **It is the mirror, and the mirror is one word.** `enqueued.take k`
  against `pushed.reverse.take k`. Everything else in the two
  statements — the zero-padded fixed-cap arrays, the Nat `n - k`, the
  absence of any `min` — is identical. The pair is the teaching point
  the wave was built for, and it only works because both were proved
  against the same machine with the same statement shape.
- **The two halves consume nondeterminism differently.** `enqueue` is
  `append`, so the enqueue loop is capacity- and address-generic and the
  stream is consumed; `dequeue` is `q[1:]`, which only advances a header
  offset, so the dequeue loop is entirely choice-free and the stream
  rides through unchanged. One example, both answers.
- **`∀ ch` is not decoration and the numbers say so**: at `n = 8, k = 4`
  the run is 1750 steps under every stream, while `nextAddr` lands at 97
  or 96 and the run consumes 2 choices or 1 — an early large capacity
  suppresses a later spill.

### KIT GAPS (unit G1.8) — recorded, NOT fixed here

- **GAP-APPEND — third and fourth consumers.** `queue` re-derived
  `qappend_inplace` / `qappend_spill` independently of `stack`'s
  `st_append_inplace` / `st_append_spill`, plus
  `buildAppendBacking_u64` and `sliceVisibleValues_u64`. The gap is
  unchanged in shape (both facts in `SliceMem`, parameterized over the
  element `IntKind`, the spill fact stated so the consumer never names
  the realized capacity); what this unit adds is the evidence that the
  re-derivation cost is paid per example, and `rle` will pay it a third
  time.
- **GAP-RESLICE — second consumer, landed name.**
  `applyStrictOp_sliceExpr_slice` (`q[1:len(q)]` on a slice base: same
  backing, offset+1). `dedup` recorded it from the other side and
  `stack` from a third; this is the form the kit should carry.
- **GAP-FRAME — `stepFn_return_frame`**, re-derived locally again.
  Consumers: stack, queue.
- **NEW: the four dequeue-phase GLUE COMBINATORS** —
  `stepFnIter_splice_pop`, `stepFnIter_drain3`, `stepFnIter_block_pop`
  and `stepFn_block`. These are pure `stepFnIter` plumbing over
  `.seqn`/`.block` spines with no queue content at all, and `stack`'s
  exit analysis wants exactly the same shapes. **Two consumers on
  arrival — over the §12 promotion bar — RECORDED, NOT LIFTED**, per the
  lane's standing rule that a consolidation slice lifts them, not the
  example that discovers them.

### Costs

| artifact | lines | wall | peak |
|---|---|---|---|
| `Examples/SliceQueue.lean` (5,297 → full) | 8,498 | **137.00 s** | **2313 MiB** |
| `Audit/SliceQueue.lean` | 176 | 0.81 s | 1454 MiB |
| `docs/verified-examples.md` (entry) | +155 | — | — |

`SliceQueue` displaces `SliceStack` (6,919 lines) as the biggest module
in the gallery, and the two together are the cost of proving through a
growing slice twice. That is the concrete argument for GAP-APPEND: the
next `append` example should not be the third module to pay it.

---

## Lane A2 — GATE STATE and lane summary

### Gate state at the lane tip

`scripts/ci` **PASS** (no escape hatch, no `GOLEAN_ALLOW_NO_DIFF`):

- escape-hatch preflight, meta-layer hatches, proofs-file audit
  coverage, bug-index, feature-coverage, lane-validation fixtures,
  imported-goose verbatim, surface purity, statement-TCB closure,
  import-direction — all ok;
- **core build warning-free**; **proofs + Audit gate ok** (the
  `#guard_msgs #print axioms` pins for both new headline pairs are
  checked IN BUILD, not by report);
- verdi compat gate ok; import-goose fixtures ok; **golden lowering ok**
  (proof subject = decoded(frontend(source)) — the link that makes the
  `rfl` subject pins mean something); imported-goose R2 pins ok;
- frontend unit tests ok; **eval tests 136 ok**;
- **negative baseline diff: no regression, 311/311** — this lane had NO
  recorded negative run at all (a fresh worktree), which is a FAIL by
  the standing rule; rather than reach for `GOLEAN_ALLOW_NO_DIFF=1` the
  finisher RECORDED one (`scripts/coverage-negative`, 311 cases, all
  PASS). A hatch was available and was not used, because recording the
  run cost eleven seconds and buys a real check;
- **differential baseline diff FULL: 1791/1791, no regression.**

`scripts/render-gallery` exit 0, **88 verbatim blocks** (82 before this
lane's queue entry; the stack entry took it from 76 to 82).

### What this lane shipped

| unit | example | outcome |
|---|---|---|
| G1.7 | `stack` | **COMPLETE** — headline + twin, gallery entry, shard |
| G1.8 | `queue` | **COMPLETE** — headline + twin, gallery entry, shard |
| G1.9 | `matmul` | **HONEST GAP** — groundwork landed, headline withdrawn unverified (see the unit) |

Two headlines, two gallery entries, two axiom shards. The gallery is at
**fourteen example sections**; `stack` and `queue` are its first
non-map examples with a non-vacuous choice quantifier, and they are
each other's teaching pair.

### Three claim corrections the finisher made to already-written text

Recorded together because they are one failure mode with three faces —
**a sentence that was easy to write and had never been checked**:

1. **The `emptyPops` fabrication** (caught by the coordinator, kept
   here for the pattern): a correct formula with an invented mechanism
   attached. There is no such counter in the Go.
2. **"All four binder groups load-bearing"** stated in the vocabulary
   of truth when the tool answers a question about the proof. Split
   into totality / truth / proof-structure boundaries, per hypothesis,
   by probe — for `stack` retroactively and for `queue` from the start.
3. **A shard and a gallery entry for `matmul` that pinned axioms no
   build had ever evaluated.** Both were written in good faith against
   a headline that exists as text; neither may ship until a build
   closes it. Withdrawn.

- **JC (the standing rule this lane earned):** every number and every
  mechanism in a shard or gallery entry is either produced by a build
  at the tip, or by a probe the author ran, or it does not go in. "The
  worker reported it" is not a source; "it follows from how the machine
  works" is not a source. Both of those produced defects in this lane
  and neither produced one that a green build would have caught.
