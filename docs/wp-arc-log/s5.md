# WP arc — SLICE 5 log (emission + instantiation sugar; the storm lint)

Charter: `docs/2026-08-16_wp-arc-charter.md` §Slice 5 ("`go_iterate`/
`go_bail`/`go_rebase`/`go_run` instantiation helpers over the slice-1/2
lemmas; evaluator-backed segment emission; the storm lint … Scope
trimmed to what slices 1–4 leave standing"), DONE item 4: "shipped or
explicitly trimmed with reasons". Conventions per `INDEX.md`.

Entry state: branch `wp-arc` @ `d1607145` (slices 1–4 integrated),
worktree clean, `scripts/ci` PASS re-run at that tip before any edit
(`.tmp/s5_ci_baseline.out`: 24 ok / 0 fail, full differential
2073/2073 + negative 390 with the stale-at-HEAD note, RESULT: PASS).
Cap `GOLEAN_MEM_MAX=24G` throughout; 102 GiB free at entry.

The slice RESCOPED itself around S4.11's smart-unfolding discovery,
exactly as the brief anticipated: item 1 (the option probe) determines
the arc's honest cost story, item 2's lint gains a rule from it, and
item 3 shrank to one measured lift because slices 1–4 ate the rest.

## Judgment calls (one-line entries)

- JC-S5-1 (probe fidelity): the S4.3(b) scratch was NOT kept, so the
  fixture is RECONSTRUCTED from the log's description (50 × `a = a+1`
  over a binder) and lands at **752** steps, not 653 — the shape (a
  term-growing accumulation over a symbolic scalar, program-generic
  state) is what the cost class is, and the reconstruction is
  `#eval`/`rfl`-validated end to end (S4.12). Recorded as a
  reconstruction everywhere it is cited; the 653 figure is history.
- JC-S5-2 (lint home): a NEW `scripts/proof-lint` rather than an
  extension of `scripts/proof-costs` — the two answer different
  questions (a syntax census vs a per-module build-cost trend), have
  different runtimes (0.4 s vs a full build), and `proof-costs`'
  do-not-harden header is written about ITS numbers. Both are wired
  into `scripts/ci` as adjacent note-only steps.
- JC-S5-3 (lint honesty over lint coverage): rule L3 (the E-form)
  fires 552 times on a GREEN tree, because shipped sites pin their
  arguments by name instead of ascribing the `have`. Rather than tune
  the rule until the number looked good, L3 is demoted to a CENSUS —
  excluded from the headline count, its notes one flag away — and the
  reason is in the script's header. A lint whose biggest number is
  known noise teaches people to ignore the lint.
- JC-S5-4 (item 3 scope): all four named sugars (`go_iterate`,
  `go_bail`, `go_rebase`, `go_run`) are TRIMMED with reasons (S5.3);
  the one shape that survived the assessment is a two-line-to-one-line
  LEMMA, not a tactic, and it ships with 7 retrofitted sites. Building
  a tactic whose arguments are as long as the lines it replaces would
  have been ceremony, and the charter asked for a disposition, not a
  quota.
- JC-S5-5 (`derive_seg` mode (a)): out of scope by the brief, and the
  probe supplies the trim reason rather than just the instruction —
  mode (a) emits raw-`rfl` windows, and S4.12 cell B shows the raw
  route's cost problem is an OPTION, one line long. The territory
  mode (a) was chartered for is covered by the option (cheap raw
  windows) plus the evaluator transport (windows whose output you
  should not transcribe); a probe/quoter layer would now be building
  emission for a route that no longer storms.

## Unit S5.1 — ITEM 1: THE OPTION PROBE — `8cd1f3d1`

Deliverable: the S4.3(b) baseline re-run under
`set_option smartUnfolding false`, bounded, plus the arc's cost-story
reconciliation. **Full record + the 2×2 table + the per-window
guidance: `s4.md` §Unit S4.12** (written there because it re-reads
S4.3/S4.11's numbers and is where an auditor will look for them).

Headline, derivation-anchored (scratches `.tmp/s5_*.lean`, each run
`timeout`-bounded under `scripts/capped`, `GOLEAN_MEM_MAX=24G`):

- reconstructed 752-step accumulation window, raw
  `with_unfolding_all rfl`: **DNF in 620 s** at default elaboration →
  **0.94 s** with the option (575 ms tactic + 362 ms kernel). The
  option alone collapses the class.
- the evaluator's own full-tuple `rfl`: **1.03 s** default, **1.01 s**
  under the option — a wash against cell B, and INSENSITIVE to the
  option (the robustness claim, measured rather than argued).
- the transported/emission route: **2.6 s** at default, **DNF in
  420 s** under the option — the REVERSAL, reproduced at a 32-step
  shallow window too (64 ms → 13.7 s). Discriminating feature not
  isolated; recorded as an open question.
- `Examples/MatMul.lean`'s prefix through its transported trio: 5.56 s
  under the file's option vs 6.29 s without — the landed tree is
  measured-safe and S4.11's claim re-verifies at this tree.

VERDICT (the sentence the reconciliation opens with): on today's
corpus the option buys the raw-window collapse and the evaluator buys
nothing measurable beyond it for a window raw `rfl` can state; the
refinement theorem's value is structural (program-genericity by
construction, no hand-transcribed output, a ∀ρ-general fact per
window, elaborator-heuristic robustness) plus the one measured window
class where the option is the thing that breaks.

## Unit S5.2 — ITEM 2: THE STORM LINT — `8cd1f3d1`

`scripts/proof-lint` (new, 0.4 s, report-only, DO-NOT-HARDEN header in
the `scripts/proof-costs` voice) + a note-only `scripts/ci` step
beside the proof-cost trend. Five rules, each anchored to a MEASURED
pathology, four actionable + one census:

| rule | shape | measured pathology | hits at this tip |
|---|---|---|---|
| L1 | unqualified `.base ⟨…⟩` ×3+ in one statement argument | 502k `BEq.beq` unfoldings, 50-min storm → 12 s (StepKit rule 5) | 62 |
| L2 | full-heap `Heap.lookup (… ++ …)` in a signature | every instantiation re-traverses the front; two parks (rule 4) | 34 |
| L3 | unascribed `have :=` on a step lemma at a concrete-state site | ~2^N in front length, 52 GB at N=16 (rule 3, E-form) | 552 (census) |
| L4 | long `stepFnIter` window whose STATEMENT embeds the program | `wc_empty_run` 82 s / 50.8 GiB → 86 s / 1.9 GiB restated | 4 |
| L5 | long raw `with_unfolding_all rfl` in a file with neither the option nor a transport | **S4.12 cell A/B: DNF 620 s → 0.94 s** | 81 |

Counts from `scripts/proof-lint --summary` at this tip (258 files
scanned). L5 is the new rule the discovery bought; its header carries
the COUNTER-NOTE (cells E/F: the option inverted the transported
landing 64 ms → 13.7 s, and matmul's landed file is measured-safe), so
nobody reads L5 as "put the option on every file".

Honest limits, in the script's own header: every rule is a regex with
a crude statement/proof split; L1/L3 have a real false-positive class;
L4 reads statements, so `Examples/WordCount/EmptyRun.lean` — the
exemplar of L4's own FIX, whose statement stays byte-identical while
the proof went program-generic — is flagged and is not a defect. The
lint is a speedbump: investigate a note, never obey it. Nothing in the
tree was changed to satisfy it (gates are speedbumps; no hardening,
no laundering).

DIFF FLAGGED FOR THE ARC AUDIT (gate-shaped surface): `scripts/ci`
gains one step that only prints and appends a `note` line to
`RESULTS`; it never calls `bad()`, never sets `fail`, and its script
exits 0 except on an argument typo.

## Unit S5.3 — ITEM 3: INSTANTIATION SUGAR, assessed and trimmed —
this commit

The assessment, at this tree, with the consumer counts that decided
each call (`grep -rn` over `proofs/GoLeanProofs/Examples/`):

| sugar | consumers today | verdict | reason (one line) |
|---|---|---|---|
| `go_iterate`/`go_iterate_exit` | 28 `stepFnIter_iterate` + 7 `_exit` sites | **TRIMMED** | the site is ~12 lines of which ~9 are the `T`/`C` descriptor families — per-example content only a probe-driven emitter (`derive_seg`, out of scope) can write; a tactic would take the same 9 lines as arguments. |
| `go_bail` | 8 `stepFnIter_iterate_bail` sites (s1 lift 5 + the s1.5b `_rel` schema) | **TRIMMED** | same shape one level up: `T`/`C`/`I`/`Q`/`hstep` are the proof's actual content, and the s1.5b relational schema already removed the `strongRecOn` scaffolding a tactic would have generated. |
| `go_rebase` | 17 files touching the threshold/rebase layer | **TRIMMED — the lemma ate the tactic** | s1 lift 4 (`Frame/Threshold`) turned the ~200-line 11/16/21-way per-cell enumeration into `refine rebaseSimT (retired := […]) h` (worked instance: `Examples/SelectionSort/Frame.lean:149-156`). There is no enumeration left to generate. |
| `go_run` | 28 files using `harness_readout_of_total` | **TRIMMED** | the readout half is already ONE line (`exact ⟨…, harness_readout_of_total htot⟩`); the assembly half is per-example naming (entry-eq lemma, fuel polynomial, readback simp set) — a tactic's argument list would equal the code it replaces. |
| `derive_seg` mode (a) | — | **out of scope (brief)**, and now also unmotivated | JC-S5-5: the option covers cheap raw windows and the transport covers the ones worth emitting. |

**What the assessment DID find, and what shipped instead of a tactic.**
One idiom is genuinely repeated verbatim across the iterate/bail
instantiation sites: turning the loop guard `decide ((i : Int) <
(n : Int))` into `true`. Census at the lift:
`grep -rn "decide_eq_true (by exact_mod_cast" Examples/ | wc -l` →
**79** occurrences; ~50 in exactly the `Nat`→`Int` cast-on-both-sides
shape (`grep -B2` histogram: 26 `j/n`, 8 `m/n`, 6 `i/l.length`, plus
singletons), the rest either comparing a `Nat` cast against an `Int`
binder or needing `omega`, where `decide_eq_true h` already applies
and no lemma is missing.

Shipped: `GoLean.Surface.decide_natCast_lt_true` in `FuelMeasure`
(group 6, beside the schemas that need it), axiom pin **`[propext]`
alone** — fresh-probed (`.tmp/pinprobe13.lean`), deliberately NOT
copied from the neighbouring `[propext, Quot.sound]` pins (the S4.11
stale-pin lesson). Retrofits in the same commit, per the §12
active-abstraction rule: `Examples/DotProduct.lean` (4 sites),
`Examples/Kadane.lean` (3 sites).

MEASURED delta, stated plainly rather than flatteringly: the idiom is
2 source lines and becomes 1, so **−7 lines at 7 sites**
(DotProduct 2025 → 2021, Kadane 2473 → 2470), with ~50 more sites
available tree-wide (≈ −50 lines if ever swept — NOT swept here; a
50-file mechanical churn is not worth a rebuild wave in this slice).
The honest value is uniformity and discoverability — one named bridge
in the group-6 API instead of an idiom re-derived 79 times — not line
count, and the docstring says so. No cost claim: both modules rebuild
green (`lake build … DotProduct Kadane Audit.Kit`, 293 jobs, exit 0);
neither module had a prior `proof-costs` row to compare against.

## Slice-5 disposition summary (charter DONE item 4)

| item | disposition |
|---|---|
| the option probe (flagged S4.11 open question) | **SHIPPED** — closed, reconciliation in `s4.md` §S4.12, guidance per window |
| the storm lint | **SHIPPED** — `scripts/proof-lint`, 5 rules, ci note-only step |
| `go_iterate`/`go_bail`/`go_rebase`/`go_run` | **TRIMMED**, one reason each (S5.3 table) |
| `derive_seg` mode (a) | **TRIMMED** (brief scope + JC-S5-5) |
| the lift the assessment found instead | **SHIPPED** — `decide_natCast_lt_true` + 7 retrofits + pin |

Rules honoured: no statement changed anywhere; existing Kit pins
byte-identical (the only pin-block edit is an ADDITION);
no `sorry`/`admit`/`native_decide`/`axiom`; `scripts/ci` PASS per
commit (recorded in each commit message); GoCore untouched, so the
standing full differential record carries with its visible
stale-at-HEAD note.
