# WP arc — SLICE 6 log: the DISCOVERABILITY close-out (2026-08-18)

Charter: `docs/2026-08-16_wp-arc-charter.md` §Slice 6 — (1) the Kit
Guide, situation-indexed, "one page an agent reads FIRST"; (2) the
discoverability acceptance test, "a fresh agent, given ONLY the Kit
Guide + a new example's corpus half, produces a proof plan naming the
right kit forms without reading any example module … gaps found are
guide bugs, fixed before DONE". Conventions per `INDEX.md`.

Entry state: branch `wp-arc` @ `8b36bf15` (slices 1–5 integrated),
worktree clean, `GOLEAN_MEM_MAX=24G` throughout.

**Scope discipline honoured:** this is a DOCS-ONLY slice plus two
sub-agent dry-runs. No statement, no proof, no pin, no `Audit/Kit.lean`
edit, no `Corpus/`/`baselines/` touch. The whole code diff is `/-!`
docstring blocks in nine modules (the eight s3 pointer slots + one
added in `Sym/Refine.lean`).

## Judgment calls (one-line entries)

- **JC-S6-1 (guide altitude):** the guide is a ROUTING index, not a
  reference manual — every row answers "which form, what do I owe it,
  which file do I copy" and stops. The API-group inventory
  (`s3.md`) stays the exhaustive list; duplicating it here would give
  the tree two name registers to keep in sync. *Principle: one place
  per fact.*
- **JC-S6-2 (fixtures by FILE):** every fixture cite is a path with no
  line number, and every one was re-derived by grep at this tip rather
  than copied from a log — the cite-drift lesson (the R5 design report
  is the standing example of line cites that stopped resolving).
- **JC-S6-3 (the guide was written from the SOURCES, then tested):**
  the sections were drafted from `s3.md`'s inventory, `s4.md` §S4.12,
  `s5.md` and the module docstrings — deliberately NOT from reading the
  dry-run examples' proof modules first. Writing what the fixture does
  into the guide before testing on that fixture would have made the
  acceptance test measure nothing. Two of round 1's three bugs
  (§3/§4's missing precondition, §18's over-broad routing) would have
  been silently pre-fixed had I read `DedupAdjacent.lean` first.
- **JC-S6-4 (dry-run fidelity, stated as a limit not a claim):** each
  sub-agent was given the guide as an ABRIDGEMENT pasted inline, not
  the file. Verbatim in the abridgement: every routing-table row, every
  precondition, every form NAME, every fixture path, every measured
  number, §5's guidance table and §22's five rules. Compressed: the
  header paragraph and some section tables flattened to prose (§§7, 8,
  11, 12, 20 in round 1; also §6 in round 2). No form name and no
  routing rule was dropped. Inline paste was chosen over "read this one
  file" precisely to make leakage IMPOSSIBLE rather than merely
  instructed — and the transcript metadata confirms it worked:
  **`tool_uses = 0` for both agents**, so neither read any repo file.
- **JC-S6-5 (round-2 findings fixed without a third dry-run):** the
  charter's loop is "fix, then re-run on a SECOND example". Round 2 was
  that re-run; it verified all four round-1 fixes and surfaced two NEW
  bugs, which are fixed here. A third run is not chartered and is not
  claimed; the two round-2 fixes are additive rows with named,
  grep-verified fixtures, and they are flagged for the arc audit as the
  one part of the guide no dry-run has exercised.

---

## Unit S6.1 — DELIVERABLE 1: `docs/kit-guide.md` — this commit

**819 lines, 23 numbered sections + a routing table + the phase map.**

The twenty guide-section names were FIXED in `s3.md`'s API-group
inventory so the module docstrings and the guide would agree by
construction. All twenty are present as `##` headings, verbatim:

Entry · One step at a time · Heap at a symbolic split · Footprint ·
Values in memory: slices · Values in memory: maps · Values: strings ·
Integer normal forms + bounds · Setup families (∀-input data) ·
Append / growth · Swap / count surgery · Counted loop · Two-exit loop ·
Map count loop · Map range loop · Loop-local allocation → threshold
frame · Recursion / call span · Composition · Termination · Readout.

**Three sections beyond the twenty**, each with its reason:

| § | name | why it is not in the s3 inventory |
|---|---|---|
| 5 | Segments — raw `rfl`, the option, or the evaluator | slice 4 ran on the parallel `wp-eval` lane, so `Sym/*` had no row in the s3 inventory. This section carries S4.12's per-window guidance table, which the brief names load-bearing. |
| 22 | The disciplines | StepKit rules 1–5 + the five lint rules, at guide altitude. StepKit's `## THE FIVE RULES` remains the single copy; this section cites and does not restate the derivations. |
| 23 | Honest limits | the parked list + the fail-closed fragments + what the dry-runs surfaced. A guide that only says what EXISTS wastes a reader's hour on what does not. |

Section census against the charter's twelve named situations —
all twelve covered, each by a named section: entry (§1), counted loop
(§13), two-exit loop (§14), loop-local allocation → threshold frame
(§17), map count (§15), map range (§16), append/growth (§11),
recursion/call span (§18), footprint (§4), composition (§19), readout
(§21), bounds (§9, incl. the bound-vs-measured discipline paragraph the
brief asked for). Plus the evaluator (§5) and the sugar disposition
(§23's trimmed-tactics bullet).

Every row carries: the kit form, the hypothesis shape / what you owe
it, the named fixture FILE, and the applicable lint rules where they
bite (§3 → L1/L2; §5 → L4/L5; §22 → all five).

**Fixture derivations** (all re-run at this tip, `proofs/GoLeanProofs/`,
so the guide cites consumers that exist rather than consumers a log
once recorded):

```
grep -rl '<name>_' Examples/            # suffixed families
grep -rln '\b<name>\b' Examples/        # exact names
```
Consumer counts quoted in the guide and their commands:
`stepFnIter_iterate` → 20 files; `derive_entry_eq` → 28 invocation
sites; `harness_readout_of_total` → 26 files; `stepFnIter_chain` → 78
occurrences in `DedupAdjacent.lean`; `CompletesIn` → `Fib.lean` only;
`symEvalWindow` → `MatMul.lean` only; `smartUnfolding false` →
`Examples/MatMul.lean` + `Audit/MatMul.lean` only.

**One honest datum recorded in §23 rather than smoothed over:**
`SliceMem.sliceVal` and `SliceMem.normalize_of_range_unsigned` are
named NOWHERE else in `proofs/` (`grep -rln` → their own module only).
The vocabulary defs are re-spelled per example because headline
statements name them and no kit name may enter a headline closure
(§12b). A guide that said "use `sliceVal`" would have been wrong.

## Unit S6.2 — DELIVERABLE 2: the docstring cross-pointers — this
## commit

s3 left every kit module's pointer slot NAMED but marked `Future`.
Nine modules now carry a live pointer:

| module | pointer |
|---|---|
| `StepKit` | One step at a time · Heap at a symbolic split · Footprint · Append / growth, **+ §22** (the guide's restatement of THE FIVE RULES, which stay anchored here) |
| `SliceMem` | Values in memory: slices · Integer normal forms + bounds · Setup families · Append / growth · Swap / count surgery |
| `StringMem` | Values: strings |
| `MapMem` | Values in memory: maps · Map count loop (model/fold half) · Map range loop (the pick step) |
| `MapLoops` | Map count loop (incl. the five identifier parameters) · Map range loop |
| `FuelMeasure` | Composition · Counted loop · Two-exit loop · Recursion / call span · Termination · Readout · Entry (`runConfig` half), **+ the §14 routing rule between group 6's three schemas** |
| `Frame/Threshold` | Loop-local allocation → threshold frame |
| `EntryEq` | Entry (both forms, the string arm, the fail-closed refusals) |
| `Sym/Refine` | **NEW slot** — §5 Segments, with the one-line routing rule |

**One factual correction made while filling the slots:** `MapMem`'s
slot named sections "**Map counting**" and "**Map range**", which are
not among the twenty fixed names (the inventory's own union resolves
them into "Map count loop" / "Map range loop" — MapMem owns their
model/pick halves). Both now name real sections, so every pointer in
the tree resolves.

`grep -rn "kit-guide" proofs/` → 9 hits, 0 containing "Future".

---

# DELIVERABLE 3 — THE DRY-RUN ACCEPTANCE

**Method.** One fresh general-purpose sub-agent per round, Opus-class,
no arc context, given exactly: the guide (abridged per JC-S6-4), one
example's corpus half (`main.go` + `cases.tsv`, verbatim from
`Corpus/coverage/exec/examples/<id>/`), and the instruction *"produce a
proof PLAN naming the kit forms you would use per phase; do not prove
anything; do not read any file"*. Both agents complied: **`tool_uses = 0`
in both transcripts**, so no example module was read and no kit source
was consulted. Round 1's example was chosen so its shipped proof
exercises the counted loop, the entry macro and the readout; round 2's
so its shipped proof exercises the footprint, the call span and the
routes round 1's fixes had just changed — i.e. the second run tests the
fixes in the OPPOSITE direction, which is the only way a fix that
merely swapped one over-recommendation for another would show up.

Ground truth for both evaluations is grep at this tip, not the logs.

---

## ROUND 1 — `Corpus/coverage/exec/examples/dedup` → `Examples/DedupAdjacent.lean`

### The plan, as returned (verbatim)

> ## Proof plan — machine-level correctness of `dedup_harness_r`, ∀ n ≤ 8, ∀ seed
>
> ### Routing-rule precondition checks (done first, since they pick branches)
>
> 1. **§1 `derive_entry_eq` fragment.** Precondition: scalar-integer parameters, and result defaults limited to scalars / arrays *of* scalars / nil slices+maps / strings. Params are `n, seed : uint64` ✓. Results are `[8]uint64, [8]uint64, uint64` — single-level arrays of scalars, **not** nested aggregates. **Branch taken: the macro, not the hand-written `MatMul` route.** (`MatMul`'s refusal is `[3][3]uint64`, a *nested* default; `[dedupCapN]uint64` is one level down from that and inside the fragment.) Within §1 I take the **six-ident program-generic form**, because the segments must be stated over an abstract `σ` (§22 rule 1).
> 2. **§13 uniformity.** Holds for the three harness loops (setup, copy-in, copy-out: branch-free, choice-free, no allocation). **Fails for the `dedupAdjacent` body loop** — `if k == 0 || s[i] != s[k-1]` makes the taken and skipped iterations cost different step counts and produces different successor heaps. → §14, and within §14 **skip straight to `_bail_rel`** per the guide's own "when to skip" rule (variable cost + existential successor state).
> 3. **§5 window route.** The evaluator is unavailable for the subject loop regardless of cost: its fragment excludes forking, path conditions, and addresses computed from symbolic values, and this loop has all three (`s[k]` with symbolic `k`, a data-dependent branch). **Branch taken: raw `with_unfolding_all rfl` windows, program-generic.** Since no window in the file is transported, §5's REVERSAL hazard ("a file that mixes both") does not apply, so `set_option smartUnfolding false` is safe file-level if any window measures long (L5).
> 4. **§17 Frame/Threshold.** Precondition: the loop body allocates. It does not — the only allocation is the single `make([]uint64, n)` before the loops, and `s[:k]` re-slices the existing backing. **Skip §17 entirely** ("do not carry a footprint you do not need").
> 5. **Not applicable:** §7 maps, §15/§16 map loops, §8 strings, §11 append (there is no `append`; `s[:k]` is §6's `applyStrictOp_sliceExpr_slice`, not a spill), §12's `swapList` half.
>
> ### Phase 1 — Pure model (spec layer, no machine)
>
> * **Forms:** §10 `familyF` (+ `familyFZ_range`) for the setup data `s[i] = seed + i/2`; §12's point-update algebra `getD_set_self`, `getD_set_ne`; §10 `prefixPad` (+ `prefixPad_familyF_set`) for the `pre` array (copy-out of a *family* prefix) and §10 `takePad` for the `post` array (copy-out of *computed* data).
> * **Why:** `seed + i/2` is an arbitrary index function, so `familyMod` (which is `seed + i % k`) does not fit and `familyF` is the general one. The two output arrays differ in kind — one pads a family, one pads a computed prefix — which is exactly the `prefixPad`/`takePad` split.
> * **You owe:** a local `def` for the family plus its `_length`/`_range`/`_succ`/`_set`/`_getD` lemmas delegating to `familyF_*`; a pure `dedupAdjacent` list function and its correctness lemmas (length `= (n+1)/2`, elements, adjacent-distinctness); the key arithmetic fact that `seed + i/2` for `i < 8` takes exactly 4 pairwise-distinct values **mod 2^64** (see limits — this is not the in-range story §9 supports).
> * **Fixture:** `Examples/DedupAdjacent.lean` (the guide names it as *the* pattern for a family shape the kit does not name); `Examples/MinMax/Core.lean` and `Examples/SelectionSort/Post.lean` for `takePad`; `Examples/TwoSum/Pure.lean` for the pure-layer shape.
>
> ### Phase 2 — Entry
>
> * **Form:** `derive_entry_eq <thm> <prog> <func> <stateName> <contName> <base>` — the **six-ident** program-generic form, `base = ddProg`.
> * **You owe:** the lowered-program constant, the `dedup_harness_r` `Func` constant, the empty-heap program state `ddProg`, and the emitted names; then a structural `rfl` bridging the emitted record update `{ ddProg with heap := …, nextAddr := … }` to the abstract-`σ` spelling the segments use.
> * **Why:** never hand-write the entry dance; the macro computes the layout (params at `0…1`, the three result cells at `2…4` at their `defaultValue`s) and self-checks at a concrete probe point before emitting.
> * **Fixture:** `Examples/DedupAdjacent.lean`; `Examples/TwoSum/Machine.lean` for the six-ident spelling.
>
> ### Phase 3 — Executable value facts (the hypothesis supply for every window)
>
> * **Forms — §6:** `sliceCells`, `sliceVal`; `applyStrictOp_len_slice` (`len(s)`, `len(r)`); `applyStrictOp_indexGet_slice` (`s[i]`, `s[k-1]`, `r[i]`); `applyStrictOp_sliceExpr_slice` for `s[:k]` (result `⟨some b, 0+0, k, cap⟩` under `0 ≤ k ≤ cap`); `storeTarget_slice_u64` for `s[k] = s[i]`; **`storeTarget_arrayLocal_u64` + `normalizeValueForTy_arr_u64`** for `pre[i]` and `post[i]`, which are array-typed *locals*, not slices.
> * **Forms — §9:** `applyStrictOp_add_u64` (`seed + i/2`), `div_u64` (`i/2`), `sub_int` (`k-1`), `eqCmp_int` (`k == 0`), `neqCmp_int` (`s[i] != s[k-1]`), `lessCmp_int` (loop guards), `convert_u64` (`uint64(len(r))`); `unorm_of_range`, `unorm_nat_of_lt`, `unorm_add_nat`, `intKind_normalize_idem`; `Surface.decide_natCast_lt_true` to turn each guard `decide ((i:Int) < (n:Int))` into `true`.
> * **Forms — §3/§4:** `lookup_append_left`/`_right`, `set_append_left`/`_right`, `lookup_set_self`, `lookup_set_other`, `set_set`, and **`set_self_of_lookup`** for the taken branch while `k == i` (the write `s[k] = s[i]` is then a no-op — a named form exists, use it rather than a bespoke argument). One `FreshFrom.push` for the single `make` backing; that is the entire footprint burden.
> * **Why:** these are what discharge §2's conditioned hypotheses; they carry no stepping and no fuel.
> * **Fixtures:** `Examples/DedupAdjacent.lean`, `Examples/DotProduct.lean`, `Examples/BinSearch.lean`; `Examples/WordCount/RangeGeneric.lean` for the D-relative heap spelling.
>
> ### Phase 4 — Step windows
>
> * **Forms — §2:** `stepFn_makeSlice_u64_step` (owe the length fact for `make([]uint64, n)`), `stepFn_strict_apply`, `stepFn_store_step`, `stepFn_var`, `stepFn_seqn_splice` / `stepFn_seq_pop` / `stepFn_block`, `stepFnIter_one`, plus `loadMany_one`/`loadMany_two`, `storeTarget_addr`, `stepFn_init_seq`.
> * **Why:** every step that consults the heap or the program tables gets its own conditioned lemma over abstract `σ`; the rest is crossed by raw `with_unfolding_all rfl`. Per §22, for a first-order run the *only* step that genuinely consults `σ.functions` is `enterFrame` — so that is the single split point that keeps the whole file program-generic.
> * **You owe:** for each window, the executable facts from Phase 3, and — §22 rule 3 — the **full result type pinned on every `have`** at any application site mentioning a big concrete state (measured ~2^N in front length, so this is live, not theoretical).
> * **Fixtures:** `Examples/DedupAdjacent.lean`, `Examples/WordCount/EmptyRun.lean`.
>
> ### Phase 5 — The three uniform loops
>
> * **Form:** §13 `stepFnIter_iterate`, and `stepFnIter_iterate_exit` where I want the exit leg folded in — three instantiations: setup, copy-in to `pre`, copy-out to `post`.
> * **You owe:** the descriptor families `T i : ExecState`, `C i : Config` per loop (this is the proof's real content — no `go_iterate` tactic exists, on purpose), the single `hstep`, `hexit`, and the guard via `decide_natCast_lt_true`. Expect `c` around **57 / 53 / 55** (setup / copy-in / copy-out) — the guide cites exactly these three for this example; re-measure rather than assume.
> * **Note:** the copy-out loop's trip count is `len(r) = (n+1)/2`, not `n`, and it indexes through the re-sliced handle from `applyStrictOp_sliceExpr_slice`.
> * **Fixtures:** `Examples/DedupAdjacent.lean` (all three), `Examples/DotProduct.lean`, `Examples/Kadane.lean` (`_exit`).
>
> ### Phase 6 — The subject loop inside `dedupAdjacent`
>
> * **Form:** §14 `stepFnIter_iterate_bail_rel`.
> * **Why:** uniformity fails (branch-dependent cost) and the successor heap is existential (the store happens only on the taken branch), which is precisely the pair of conditions the guide says the constant-`c`/`max b e` form cannot reproduce.
> * **You owe:** a per-index state **predicate** `S i σ` tying the live cells to the pure compaction model of Phase 1 (the current `k`, the compacted prefix, the untouched tail), a single measure `B : Nat → Nat`, and per index the iterate branch supplying its own cost `c` plus the descent obligation `c + B (i+1) ≤ B i`.
> * **Fixture:** `Examples/TwoSum/Subject.lean` (variable row cost — the closest match); `Examples/BubbleSort/Outer.lean` for existential successor states.
> * **Alternative if `_rel` proves awkward:** the taken/skipped pattern here is *determined* (even `i` takes, odd `i` skips), so a pair-of-iterations schedule at `c = c_take + c_skip` over `⌊n/2⌋` rounds with a remainder leg would restore §13's uniformity.
>
> ### Phase 7 — The call
>
> * **Form:** §18 `stepFnIter_call_span` (with `StepKit.stepFn_call_enter` / `stepFn_return_frame` underneath).
> * **You owe:** `henter`, `hbody` — which is exactly Phase 6's theorem, stated at abstract `(h, na, ret-cont, caller-env)` — and `hexit`. You get `stepFnIter (1 + b + e)` from the `.retV … (.callArgsK …)` config.
> * **Why:** `r := dedupAdjacent(s)` is a genuine frame entry/exit; stating `hbody` over an abstract heap is what forces (and is repaid by) the D-relative discipline of Phase 3.
> * **Fixture:** `Examples/FibMemo/Rec.lean`, `Examples/Stein/Run.lean` (span shape only).
>
> ### Phase 8 — Composition and the fuel bound
>
> * **Forms:** §19 `stepFnIter_chain` (the workhorse), `stepFnIter_splice_pop` / `_drain3` / `_block_pop` for the statement-queue glue, `runConfig_of_stepFnIter`, `runConfig_mono`, `runFunctionWithContextM_mono`, `CompletesIn` + `CompletesIn.mono` + `completesIn_comp`.
> * **You owe:** left-associated chain nesting, chains ≤ 8 links, and `omega` over the per-phase bounds.
> * **Discipline (§9):** Phases 5 and 7 give exact counts, Phase 6 gives `∃ k ≤ B 0`. The composite is therefore a **BOUND**, and must be shipped as a bound, not dressed up as a count.
> * **Fixtures:** `Examples/DedupAdjacent.lean`, `Examples/DotProduct.lean`.
>
> ### Phase 9 — Readout
>
> * **Form:** §21 `harness_readout_of_total` (harness route — the headline goes through `runFunctionWithContextM`, not `execStmt`, so *not* `normal_readout_of_total`).
> * **Fixture:** `Examples/DedupAdjacent.lean` / `Examples/DotProduct.lean`.
>
> ### Phase 10 — Statement hygiene
>
> * No kit name may appear in the headline's closure (§12b), so the headline names **local, definitionally-equal copies** of the family / pad vocabulary rather than `familyF`/`takePad` (§23's last bullet). Run `scripts/proof-lint` and *investigate* L1–L5 notes rather than obeying them; `#eval` any `Bool` before asking `decide` for it.
>
> ---
>
> ## What the reference document does NOT cover for this program
>
> 1. **Wrapping arithmetic for arbitrary `seed`.** §9's integer group is oriented at in-range values. The theorem quantifies over *all* `seed`, so `seed + i/2` genuinely wraps, and the branch condition needs **injectivity of the family mod 2^64**. No named form does this.
> 2. **A spec vocabulary for compaction.** §12 supplies swap/count surgery for sorting and §6 names only `Sorted`. There is nothing for "adjacent-dedup", the surviving-prefix relation, or `k = ⌈n/2⌉`. The entire pure model of Phase 1 is bespoke.
> 3. **Aliasing / handle provenance.** `dedupAdjacent` returns a re-slice of its *argument's* backing, so `pre`, `s`, and `r` alias one region across the call boundary. No form states "this returned handle shares provenance with the caller's slice".
> 4. **A symbolically-lengthed heap front.** The backing has `n` cells with `n` symbolic (≤ 8). The guide gives no form for a symbolic-length backing region.
> 5. **Multi-result readout.** §1 handles multi-result *layout*, but every §21 readout fixture cited is single-scalar; the readout of a 3-tuple with two array components is unnamed glue.
> 6. **Array-local zero defaults.** The initial all-zero array value and its relation to the pad functions is not named.
> 7. **The `_bail_rel` vs pair-uniform choice** is a judgment call the guide does not adjudicate.

### The evaluation — plan vs. the SHIPPED proof

Ground truth by grep over `Examples/DedupAdjacent.lean` (2,844 lines)
at this tip.

| phase | plan named | the shipped proof | verdict |
|---|---|---|---|
| Entry | six-ident program-generic, checked that `[8]uint64` is INSIDE the macro's fragment (unlike MatMul's nested `[3][3]`); predicted params at `0…1`, results at `2…4` | `derive_entry_eq dd_entry_eq dedupLowered dedupHarnessRFunc ddSeed ddC0 ddProg`; the module's address map reads `0 = n, 1 = seed, 2 = $res0, 3 = $res1, 4 = $res2` | **PASS — exact, layout included** |
| Setup family | `familyF` via a local def delegating, *because* `familyMod` is `i % k` and this is `i / 2` | `ddFamily` local def; `ddFamily_length = familyF_length (· / 2) n seed`, and `_range`/`_succ`/`_set`/`_getD` likewise (5 `familyF_*` uses) | **PASS — exact, delegation pattern included** |
| Copy-in pad | `prefixPad` | `ddPre = prefixPad ddFamily 8 m seed`, with `prefixPad_length`/`_range`/`_full` | **PASS** |
| Copy-out pad | `takePad` | local `ddPost` of exactly `takePad`'s shape; `takePad` not consumed here | **ROUTING PASS / tree finding** — right form named; the example is an un-retrofitted copy |
| Value facts | index/len/sliceExpr_slice, `storeTarget_slice_u64`, **`storeTarget_arrayLocal_u64` + `normalizeValueForTy_arr_u64`**, `div_u64`, `unorm_*` | all present: `applyStrictOp_len_slice` ×6, `_indexGet_slice` ×5, `_sliceExpr_slice` ×4, `_div_u64` ×4, `storeTarget_slice_u64` ×2, `storeTarget_arrayLocal_u64` ×2, `normalizeValueForTy_arr_u64` ×2, `unorm_of_range` ×4, `inorm_nat_of_lt` ×8 | **PASS** — incl. the non-obvious array-local PAIR |
| Steps | makeSlice, strict_apply, store_step, var, seqn_splice, `stepFnIter_one` | `stepFn_strict_apply` ×18, `_store_step` ×7, `storeTarget_addr` ×3, `stepFnIter_one` ×30, `stepFn_var`, `_storeK_nil`, `_seqn_splice`, `_makeSlice_u64_step`, `_call_enter` ×1 each | **PASS** |
| Window route | raw `with_unfolding_all rfl`, program-generic; evaluator ruled OUT by its fragment | 43 raw `with_unfolding_all rfl`; no `smartUnfolding false`; no `Sym` | **PASS** |
| 3 uniform loops | `stepFnIter_iterate` ×3 at `c ≈ 57 / 53 / 55` | `stepFnIter_iterate (c := 57)`, `(c := 53)`, `(c := 55)` | **PASS — but the guide HANDED it those three numbers** in §13's fixture line; credit is the guide's, not the agent's derivation. Recorded so the result is not read as stronger than it is. |
| Subject loop | `stepFnIter_iterate_bail_rel`, justified by branch-dependent cost + existential successor heap | a HAND `induction μ` concluding `∃ k ≤ 98 · μ` (`sj_loopD`) — the `_rel` shape, hand-written | **ROUTING PASS + REAL FINDING** (below) |
| The call | `stepFnIter_call_span` + `stepFn_return_frame` | `stepFn_call_enter` ×1 and raw windows; **`stepFnIter_call_span` = 0, `stepFn_return_frame` = 0** | **MISS → GUIDE BUG 1** |
| Composition | chain + runConfig glue + **queue glue** + **`CompletesIn` algebra** | `stepFnIter_chain` ×78, `runConfig_of_stepFnIter` ×1, `runConfig_next_stop` ×1; **queue glue = 0, `CompletesIn` = 0** | **PARTIAL → GUIDE BUG 2** |
| §3/§4 | named seven heap-algebra forms + "one `FreshFrom.push`" | **ZERO** StepKit heap-algebra forms; `FreshFrom` = 0, `DeadFrom` appears once, in a docstring saying it is NOT needed | **MISS → GUIDE BUG 3** |
| Readout | `harness_readout_of_total`, explicitly rejecting the direct twin | `harness_readout_of_total` ×2 | **PASS — exact, right branch reason** |
| Not-applicable list | maps, map loops, strings, append, `swapList`, §17 all excluded with checked reasons | none of them appear | **PASS** |

**Score: 10 of 13 phases routed correctly; 3 guide bugs.**

### The three guide bugs, and the fixes

**GUIDE BUG 1 — §18 routed EVERY call to the call span.** The routing
row read "a function call, or recursion → §18", and §18's only
whole-call row is `stepFnIter_call_span`. But the span's `hbody` must
be stated at an abstract `(h, na, ret-cont, caller-env)` — real work
that pays only when the callee is entered more than once. A program
that calls a helper exactly once crosses the frame with
`stepFn_call_enter` and ordinary windows, and the frame EXIT falls out
of a raw window (dedup states neither the span nor `return_frame`).
A first-time reader would have built an abstract callee theorem it did
not need.
*FIX:* the routing table now has two rows (single call → §2; recursion
or several placements → §18), and §18 opens with an explicit
PRECONDITION naming `Examples/DedupAdjacent.lean` as the single-call
fixture and `FibMemo/Rec.lean` / `Stein/Run.lean` as the span cases.

**GUIDE BUG 2 — §19 listed narrow forms in a flat list.** The queue
glue (`stepFnIter_splice_pop`/`_drain3`/`_block_pop`) and the whole
`CompletesIn` algebra sat in the same table as `stepFnIter_chain` with
no applicability note, so the plan pulled both. In the tree the queue
glue is `SliceQueue.lean`-only and `CompletesIn` is `Fib.lean`-only.
*FIX:* both rows now carry their own applicability test in the row
itself (`CompletesIn` → only for a TERMINATION headline, see §20; the
glue → only for that continuation shape), and §19 states plainly that
**the everyday composition layer is exactly three names** —
`stepFnIter_chain`, `runConfig_of_stepFnIter`, `runConfig_next_stop` —
with dedup's own 78/1/1 counts as the worked case.

**GUIDE BUG 3 — §3 had NO applicability test at all** (the worst of
the three; a whole-section misroute). §4 carried a footprint
disclaimer, but §3 ("Heap at a symbolic split") said nothing about
when a heap is concrete enough that the section is unnecessary — so
the plan checked allocation at §17, correctly skipped the threshold
frame, and STILL pulled seven §3 forms and a `FreshFrom.push` for a
run in which every address is a fixed constant.
*FIX:* §3 opens with a PRECONDITION ("if every address in your run is
a fixed constant you need NOTHING from §3 or §4 — concrete fronts
reduce inside raw `rfl` windows and the lookups never become a proof
obligation") plus a NEGATIVE FIXTURE, `Examples/DedupAdjacent.lean`,
cited as the worked decision. §4's disclaimer is sharpened to the same
test and made explicit that a single constant-address pre-loop
allocation is just a longer concrete front — not even one
`FreshFrom.push`. A routing-table row was added for the situation
itself.

### Two non-bug findings, recorded not fixed

1. **`Examples/DedupAdjacent.lean` is an un-retrofitted
   `stepFnIter_iterate_bail_rel` consumer.** Its `sj_loopD` is a hand
   `Nat`-induction producing `∃ k ≤ 98 · μ` over an existential
   backing list — the exact shape s1.5b lifted (whose retrofits were
   twosum and bubble). Likewise its `ddPost` is `takePad`'s shape as a
   local def. Both are promotion-ledger candidates for a future slice;
   **NOT touched here** — slice 6 is docs-only and the charter freezes
   proof internals for this slice.
2. The plan's uncovered-list items 3, 4 and 5 (aliasing/provenance
   across a call; a symbolic-LENGTH concrete front; multi-result
   readout glue) are REAL and were verified against the shipped module
   (`dSliceV`/`rSliceV` both at `Loc.base ⟨6⟩`; the backing is `n`
   cells with `n` symbolic; the headline returns a triple). All three
   were added to §23.

---

## ROUND 2 — `Corpus/coverage/exec/examples/stein` → `Examples/Stein/Run.lean`

Re-run per the charter, on a SECOND example, against the FIXED guide.
Stein was chosen because its shipped proof exercises every route round
1's fixes changed, **in the opposite direction**: it is heavily
footprint-style (so §3/§4's new precondition must fail), it has one
callee at several placements (so §18's new precondition must take the
span branch), and its headline is a step-counted harness run (so §19's
new `CompletesIn` test must exclude it). A fix that merely swapped one
over-recommendation for another would show up here.

### The plan, as returned (verbatim)

> ## Routing checks first (preconditions the guide asks me to test)
>
> | rule | precondition | my program | branch taken |
> |---|---|---|---|
> | §3/§4 | "every address in my run is a fixed constant" | **FALSE.** `isEven` is called from inside all four loops; every entry allocates a frame parameter cell, and the number of entries is input-dependent (v2(a), v2(b), the outer-pass count). `nextAddr` therefore advances an input-dependent amount. | **Take §4** (footprint/`FreshFrom` over a whole abstract heap). Take only the thinnest slice of §3 — I never want to *describe* the retired frame cells, only keep them in the abstract region. Not the `DedupAdjacent` negative case. |
> | §18 | "a function called exactly once" | **Split.** `stein_harness → steinGCD`: exactly once → §2 branch. `steinGCD → isEven`: **four syntactic sites** and an unbounded number of dynamic entries → span branch. | `stepFn_call_enter` for the outer call; `stepFnIter_call_span` for `isEven`. The guide already names `Examples/Stein/Run.lean` as the "one callee at four sites" fixture — that is this program. |
> | §13 | "every iteration EXACTLY `c` steps, choice-free" | loop 2 ✔; loop 1 body ✔ but its *exit* has two shapes (short-circuit on `isEven(a)` false vs. `isEven(b)` false); loop 3 ✘ (variable inner-halving count, conditional swap, `break`). | `_iterate`/`_exit` for loop 2, `_bail` for loop 1, `_rel` for loop 3. |
> | §5 | which window route | Outputs are four scalars — nothing to transcribe. And the evaluator **quits at branches, control-feeding equality, and every program-table consult**; this program is branches and calls end to end. | **Raw `with_unfolding_all rfl`**, program-generic over abstract `σ`. `Sym.symEvalWindow_refines'` is structurally unavailable — do not reach for it. Add file-level `set_option smartUnfolding false` if any window exceeds ~40 steps (L5); safe here because there are **no** transported windows, so §5's reversal cannot bite. |
> | §19 | "CompletesIn only if your headline is a TERMINATION claim" | Headline is a step-counted harness run with an input-dependent bound. | Skip the `CompletesIn` algebra; use the everyday triple. |
> | §17 | "loop body ALLOCATES" | Technically yes (the `isEven` frame cell), but the retired cells hold **no addresses** (`uint64` only) and are dead at the loop head. | Default to absorbing the shift into the §13/§14 descriptor plus `FreshFrom.push`; hold §17 (`ρT`, `rebaseSimT`) as contingency only. Flagged in the limits list below. |
>
> ## Phase 0 — Pure layer (no kit)
>
> - **Forms:** none. This is example-side content in an `Examples/Stein/Pure.lean`-shaped file.
> - **What I owe:** the binary-GCD model and its three rewriting facts (`gcd(2a,2b) = 2·gcd(a,b)`; `gcd(2a,b) = gcd(a,b)` for odd `b`; `gcd(a,b) = gcd(a, b−a)`), the termination measure, the bound `shift ≤ 63` when `a,b ≠ 0`, and no-overflow for `a << shift`.
> - **Fixture:** the `*/Pure.lean` shape — `Examples/TwoSum/Pure.lean`, `Examples/WordCount/Pure.lean`.
>
> ## Phase 1 — Entry
>
> - **Form:** `derive_entry_eq … <base>` — the **SIX-ident program-generic** form.
> - **What I owe:** the lowered-program constant, the harness `Func` constant, my chosen emitted names, and `base`. Then a structural `rfl` bridge from the emitted record update to the `σ`-generic spelling my segments use.
> - **Fixture:** `Examples/Gcd.lean` / `Examples/Fib.lean` for the scalar-harness shape; `Examples/TwoSum/Machine.lean` or `Examples/DedupAdjacent.lean` for the six-ident spelling.
> - **Why:** two scalar `uint64` parameters and one scalar `uint64` result sit squarely inside the macro's fragment — no refusal risk. Six-ident because the run is long and every segment must be stated over abstract `σ` (§22 rule 1, §5's "never" row).
>
> ## Phase 2 — The `isEven` callee theorem (reusable)
>
> - **Forms:** `stepFnIter_call_span`; internally `StepKit.stepFn_call_enter`, `stepFn_return_frame`, `stepFn_var`, `applyStrictOp_mod_u64`, `applyStrictOp_eqCmp_int`.
> - **What I owe:** the three span hypotheses — `henter`, `hbody` stated at an **abstract `(h, na, ret-cont, caller-env)`** (this is the real work and the whole point), and `hexit`. Plus `FreshFrom.push` for the one cell the entry allocates.
> - **Fixture:** `Examples/Stein/Run.lean`; `Examples/FibMemo/Rec.lean` for span+footprint composition.
> - **Why:** §18's precondition is met in the affirmative — four placements, each re-entered at a different `na` on every loop iteration. A per-placement window would have to be re-proved at every iteration's address; the abstract-`na` body theorem is proved once and instantiated everywhere.
>
> ## Phase 3 — Prologue case split (`a == 0`, `b == 0`)
>
> - **Forms:** `stepFn_var`, `applyStrictOp_eqCmp_int`, `stepFn_block` / `stepFn_seq_pop` / `stepFn_seqn_splice`, `stepFn_return_frame`; joined with `stepFnIter_chain`.
> - **Why:** corpus rows `zero-zero`, `a-zero`, `zero-b` pin exactly these three exits.
>
> ## Phase 4 — Footprint scaffolding (carried through every later phase)
>
> - **Forms:** `FreshFrom` with `.push`, `.mono`, `.set`, `.lt_of_lookup`; `DeadFrom` only if I end up wanting a concrete front for the harness's own result cell.
> - **What I owe:** an invariant "everything at or above `na` is absent", threaded as a hypothesis through each loop schema's state family, with `.push` discharging each `isEven` entry and `.mono` at the loop head where the frame retires.
> - **Fixture:** `Examples/Stein/Run.lean`, `Examples/FibMemo/Rec.lean`, `Examples/Sieve/Machine.lean`.
> - **Why:** the §3/§4 precondition failed (see the routing table).
>
> ## Phase 5 — Loop 1, `for isEven(a) && isEven(b)`
>
> - **Form:** `stepFnIter_iterate_bail` (§14), with the *bail* disjunct degenerate.
> - **What I owe:** terminal predicate `Q`, invariant `I` (both operands even, `shift = i`, footprint at `na + k·i`), `hstep` whose iterate branch is exactly `c` steps, and `hexit` **within `e`**.
> - **Why:** the body *is* uniform, so `stepFnIter_iterate` would fit — but `&&` short-circuits, giving **two** exit shapes of different exact cost. `_exit` demands one exact `e`; `_bail`'s `hexit` is a *bound*, which absorbs both shapes under `max`. Shipping a bound as a bound is §9's discipline.
>
> ## Phase 6 — Loop 2, `for isEven(a) { a /= 2 }`
>
> - **Forms:** `stepFnIter_iterate_exit`, plus `decide_natCast_lt_true` if I index the loop by a `Nat` counter.
> - **What I owe:** descriptor families `T i`/`C i` (with `nextAddr = na + k·i`), `hstep` as ONE hypothesis, and `hexit` — here genuinely single-shaped. Trip count `n = v2(a)` from Phase 0.
> - **Why:** §13's uniformity precondition holds exactly.
>
> ## Phase 7 — Loop 3, the outer `for { … break }` (with its nested halving loop)
>
> - **Form:** `stepFnIter_iterate_bail_rel`.
> - **What I owe:** a per-index state **predicate** `S i σ`, a single measure `B : Nat → Nat` from Phase 0's descent, and per index either the bail branch (the `b == 0` `break`) or the iterate branch with its own cost and descent obligation.
> - **Nested inner loop:** each iterate branch supplies **its own** `c`, and I build that `c` inside the branch with `stepFnIter_iterate_exit` for `for isEven(b) { b /= 2 }` at that pass's trip count, chained with `stepFnIter_chain`.
> - **Fixture:** `Examples/TwoSum/Subject.lean`, `Examples/BubbleSort/Outer.lean`.
> - **Explicitly NOT used:** §12 (`swapList`, …). The routing row "the loop swaps two elements" points at *list/slice* surgery; `a, b = b, a` here is a multi-assign of two scalar **locals**.
>
> ## Phase 8 — Tail: `a << shift`, return, frame exit
>
> - **Forms:** `stepFn_var`, `stepFn_return_frame`, `unorm_of_range` / `unorm_nat_of_lt` / `intKind_normalize_idem`.
> - **What I owe:** the strict-op fact for shift-left (**not named by the kit** — see limits), `shift ≤ 63` and the no-wrap bound from Phase 0.
> - **Why:** §18's single-call branch says the exit usually falls out of a raw `rfl` window; here the return happens at a *symbolic* heap, so I plan for the explicit `stepFn_return_frame`.
>
> ## Phase 9 — Composition
>
> - **Forms:** exactly the everyday triple — `stepFnIter_chain`, `runConfig_of_stepFnIter`, `runConfig_next_stop`; `runConfig_mono`; then `omega`.
> - **Why:** none of the queue-shaped glue matches this continuation shape, and `CompletesIn` is not owed because the headline is a step-counted harness run, not a bare termination claim (§19's stated precondition).
>
> ## Phase 10 — Readout
>
> - **Form:** `harness_readout_of_total`. This is the harness route, not the direct `execStmt` route, so `normal_readout_of_total` is the wrong twin.
>
> ---
>
> ## What the reference document does NOT cover for this program
>
> 1. **Shift-left on `uint64`.** §9's strict-op roster is closed — there is **no** `applyStrictOp_shl_u64`. `a << shift` is the theorem's final operation; I must write the fact per-example, or lift it.
> 2. **Short-circuit `&&`.** No form names the two-shape exit of `isEven(a) && isEven(b)`.
> 3. **`for { … break }` with no syntactic bound.** Every loop schema is indexed by an `n` or a measure `B`; the kit names nothing that *produces* the outer bound.
> 4. **The number theory.** No arithmetic/gcd rows exist anywhere in the guide.
> 5. **The retired-frame region is a symbolic-LENGTH front.** §23 names this gap verbatim. My mitigation — never describe them, keep them above the `FreshFrom` bound — is a discipline, not a named form.
> 6. **§17's blanket rule vs. this loop.** §17 says a loop whose body allocates needs **both** §17 and §13/§14, but its apparatus is built for cells that *hold addresses*. Here every retired cell is `CellFixed.of_locFree`. The guide gives no row for "the allocation is a scalar frame cell dead at the loop head".
> 7. **Multi-value assignment (`a, b = b, a`).** No §12 row applies, and §2's step roster has no `multi_assign` entry.
> 8. **Composing an inner counted loop inside a `_rel` iterate branch.** No fixture or row shows the nesting.
> 9. **A reusable callee theorem whose caller is itself mid-loop.** Nothing states how the span's `na`-genericity interacts with a `_rel` state predicate's *existential* `na'`.

### The evaluation — plan vs. the SHIPPED proof

Ground truth by grep over `Examples/Stein/Run.lean` + `Stein.lean` at
this tip.

**All four round-1 fixes VERIFIED, each firing in the opposite
direction from the case that produced it:**

| the round-1 fix | round-2 branch taken | the shipped proof | verdict |
|---|---|---|---|
| §3/§4 precondition | **FALSE** → take the footprint; "not the `DedupAdjacent` negative case" | `FreshFrom` ×42, `lookup_append_left` ×55, `lookup_set_other` ×34, `lookup_append_right` ×28, `lookup_cons_self` ×27, `set_singleton_self` ×11, `set_append_right` ×11, `set_fresh` ×7 | **PASS — the fix does not over-suppress** |
| §18 precondition | SPLIT: harness→`steinGCD` once (`call_enter`); `steinGCD`→`isEven` at several placements (span) | `stepFnIter_call_span` used once, inside the reusable span `st_ieSeq`, which is instantiated at **9 sites**; `stepFn_return_frame` ×3 | **PASS — and the split diagnosis is the shipped design** |
| §19 `CompletesIn` test | skip it | `CompletesIn` = 0 occurrences in the whole example | **PASS** |
| §19 queue-glue test | skip it | `stepFnIter_splice_pop`/`_drain3`/`_block_pop` = 0; `stepFnIter_chain` ×266, `runConfig_of_stepFnIter` ×1, `runConfig_next_stop` ×1 — exactly the everyday triple | **PASS** |

Other phases:

| phase | plan | shipped | verdict |
|---|---|---|---|
| §5 route | raw `rfl`, evaluator structurally unavailable (quits at branches) | raw throughout; no `smartUnfolding false`; no `Sym` import | **PASS** |
| Readout | `harness_readout_of_total`, rejecting the direct twin | `harness_readout_of_total` ×2 | **PASS** |
| Bound discipline | "ship a bound as a bound" | the module's own docstring: *"The bound is a BOUND, not a measurement: `steinFuel a b = 600 + 480·(a + b)`"* | **PASS — independent agreement** |
| §12 exclusion | scalar multi-assign is not list surgery | no `swapList` anywhere | **PASS** |
| §17 | held as contingency; noted the retired cells are `CellFixed.of_locFree` | no `Frame/Threshold` use | **PASS** — and it named a real kit form for the right reason |
| shift-left | flagged as NOT in the kit | `applyStrictOp_shl` is a **private local** in `Stein/Run.lean`, alongside private `_mod2`/`_div2`/`_add1`/`_subNat`; `grep -c 'shl\|shiftLeft'` over `StepKit`/`SliceMem`/`StringMem` → 0/0/0 | **PASS — exactly right** |
| Entry form | SIX-ident program-generic | `derive_entry_eq stein_entry_eq steinLowered steinHarnessFuncRun stSeed stC0` — the **FIVE-ident flat form** | **MISS → GUIDE BUG 4** |
| four loops | `_bail` / `_iterate_exit` / `_bail_rel` + nested `_iterate_exit` | **four hand `Nat.strongRecOn` inductions; ZERO kit loop schemas in the example** | **MISS → GUIDE BUG 5** |

**Score: 11 of 13 checked routes correct; the four fixes verified; 2 new guide bugs.**

### The two round-2 guide bugs, and the fixes

**GUIDE BUG 4 — §1 over-recommended the program-generic form.** The
second Entry row read "the same, and your segments are stated over an
abstract `σ` (they should be)", which reads as *always prefer
six-ident*. Stein's segments ARE state-abstract — they take
`(σ : ExecState)` and return it untouched, or take `(h : Heap) (na : Nat)`
with `FreshFrom` — and it uses the FLAT five-ident form, as do
`Gcd.lean` and `Fib.lean`. The six-ident form buys exactly one thing:
it preserves the record-update structure that a `xSt σ H na =
{ σ with heap := H, nextAddr := na }` segment vocabulary is stated
over. A footprint-style module has no record update to preserve.
*FIX:* the row's condition is now the SEGMENT SPELLING, not a quality
judgement, and a "which of the first two rows is you?" paragraph
follows, naming `Stein/Run.lean`, `Gcd.lean` and `Fib.lean` as
flat-form modules and stating that "program-generic is not a quality
ranking; it is a match to your segment spelling".

**GUIDE BUG 5 — no row for the MEASURE-DRIVEN loop class.** §13 and
§14 are both indexed by an `i` running to a known `n`; §20's measure
rule yields `CompletesIn`, a termination claim. Stein's four loops
(`for isEven(a) && isEven(b)`, `for isEven(a)`, the inner strip, the
subtract loop) have no counter at all — their trip counts are measures
of the data — and the shipped answer in every case is a hand
`Nat.strongRecOn` producing `∃ k ≤ B μ`. The guide named no class for
this and the plan therefore forced all four into counter-indexed
schemas.
*FIX:* §14 gains **THE THIRD CLASS** section and the routing table a
row. It states the class, the landed practice (hand strong induction
on the measure), the fixtures (`Stein/Run.lean` ×4,
`DedupAdjacent.lean`'s subject loop), the honest note that
`stepFnIter_iterate_bail_rel`'s `B : Nat → Nat` *is* a measure and is
the form these should reduce to — **with no landed measure-driven
consumer yet**, since its two retrofits are counter-indexed — so a new
example tries `_rel` first and RECORDS the outcome; and the warning not
to reach for §20, whose output is the wrong kind of claim.

Round 2's uncovered-list items 1, 2 and 7 (no shift/bitwise op facts;
short-circuit `&&` exits; multi-value assignment) were verified and
added to §23. Items 3, 4, 6, 8, 9 are honest observations about
example-side content or unmeasured interactions and were left as the
plan stated them — the guide already says the pure layer is bespoke,
and inventing rows for interactions nobody has run would be the
over-specialization the doctrine warns about.

**Not fixed, recorded:** the round-2 fixes have not themselves been
dry-run tested (the charter's loop is one re-run and this was it).
They are additive rows with grep-verified fixtures and no routing rule
was removed, but they are the part of the guide with the least
evidence behind it — flagged here for the arc audit.

---

## SLICE 6 CLOSE-OUT

Charter DONE item 5 — *"the Kit Guide exists; the dry-run acceptance
ran and its findings are fixed"*:

| item | state |
|---|---|
| the Kit Guide | **SHIPPED** — `docs/kit-guide.md`, 819 lines, 23 sections, all twenty s3-fixed section names present, every row with form + hypothesis + FILE fixture + lint |
| cross-pointers | **SHIPPED** — 9 modules, 0 `Future` slots, 1 factual correction (MapMem's two non-resolving names) |
| the dry-run acceptance | **RAN TWICE** — 2 fresh agents, `tool_uses = 0` each, both plans + both evaluations recorded verbatim above |
| findings fixed | **5 guide bugs found, 5 fixed** (3 in round 1, verified by round 2; 2 in round 2, fixed and flagged as untested) |

Honest summary of what the acceptance measured. Round 1 routed 10 of
13 phases correctly from the guide alone, including two things it
could not have guessed — the array-local store PAIR and the
`familyF`-delegation pattern — and one it could (§13's fixture line
handed it `c = 57/53/55`). Its three misses were all the SAME defect
class: **a section that lists forms without stating when the section
does not apply.** All three fixes are preconditions, not new forms.
Round 2, on a structurally opposite example, took every fixed branch
the other way and found two further gaps of the same class (a
recommendation stated as a preference rather than a condition; a whole
loop class with no row). The class is now the guide's main known risk
and is the thing to re-test when the next real example is built — the
charter's stated field measurement.

`scripts/ci` PASS at this commit (`GOLEAN_MEM_MAX=24G`; docs +
docstring-only diff, so the standing full differential record carries
with its visible stale-at-HEAD note).
