# S2 — lift wave 2 (new shapes; consumers 2–5 each, all landed)

Charter section: `docs/2026-08-16_wp-arc-charter.md` §Slice 2. Owner:
the single wp-arc lane (worktree `.claude/worktrees/wp-arc`, one
writer). Format rules: `INDEX.md` in this directory. Conventions
carried from s1 (one commit per lift family, ≥2 landed consumers
retrofitted same-commit, P6 rollback grep-verified, fresh-probed
`Audit/Kit.lean` pins, measured deltas here, `scripts/ci` PASS per
commit).

The chartered items (charter §Slice 2 + the resume order): 1 the
footprint pack; 2 the call-span combinator; 3 GAP-APPEND; 4
`StringMem`; 5 growing-heap front support; 6 the `derive_entry_eq`
string arm; 7 GAP-C1b (`MapLoops` statement-constant
parameterization).

---

## Unit S2.1 — the FOOTPRINT PACK → StepKit — this commit

- STATUS: done. StepKit +194 lines in two sections:
  (a) **the lookup/set battery** (after `lookup_singleton_self`):
  `lookup_set_self`, `lookup_set_other`, `lookup_cons_self`,
  `set_cons_ne`, `set_cons_self`, `set_set`, `set_comm`
  (WITH the presence hypothesis — the docstring records that the
  obvious unconditional form is FALSE on assoc-list heaps, which is
  the reason it ships in the kit), `set_self_of_lookup`;
  (b) **the freshness algebra** (after `DeadFrom.push2`):
  `DeadFrom.{mono, push3, set, lt_of_lookup}` + `abbrev FreshFrom :=
  DeadFrom` with six zero-proof `FreshFrom.*` views
  (mono/push/push2/push3/set/lt_of_lookup).
- JC (home): StepKit, not a new `Footprint.lean` — StepKit already
  owns `DeadFrom` and the P11 append/set battery; a separate module
  would split the heap algebra across two homes.
- JC (the one non-obvious call): `FreshFrom` and `DeadFrom` are ONE
  formula read two ways (dead tail vs whole footprint heap) — the kit
  carries the predicate ONCE (`FreshFrom` is an `abbrev`, algebra
  stated on `DeadFrom`, `FreshFrom.*` views are zero-proof
  delegations), rather than shipping a second identical predicate.
  This is what let the fibmemo/stein STATEMENTS (which spell
  `FreshFrom h na`) survive textually unchanged with the local defs
  deleted — the name now resolves through `open GoLean.Surface` to a
  defeq-identical predicate.
- The two SIGNATURE DISCIPLINES added to StepKit's module docstring
  as rules 4/5 (D-relative lookup hypotheses, never full-heap;
  qualified `Loc.base` in big positional arguments), each with its
  measured-storm citation verified against
  `docs/gallery-campaign-log/g1.md` §wordfreq unit
  "Elaboration-storm findings" (finding 1: isDefEq storms, two honest
  parks; finding 2: 502k `BEq.beq` unfoldings, 50 min → 12 s).
- CONSUMERS RETROFITTED (P6, same commit — the five chartered copies
  plus three the sweep found): FibMemo/Rec (whole pack deleted:
  FreshFrom def + mono/push/set, lookup_set_other/self, set_cons_ne,
  lookup_cons_self, set_cons_self, set_self_of_lookup, set_set,
  set_comm — −110/+8); Stein/Run (the whole PRIVATE pack incl.
  push3 + lt_of_lookup — −80/+6); Sieve/Machine (five private
  mirrors — −40/+5); WordFreq (Scan2's `lookup_set_self` +
  `DeadFrom.push3`, Scan's `DeadFrom.set_below` → kit
  `DeadFrom.set` with 4 Scan3 call sites re-pointed, two
  explicit-arg call sites in Range/Scan3 rewritten to named-arg
  form); PLUS Histogram/HarnessR's private `lookup_set_self`,
  SliceStack's cons trio, and rle's `lookup_set_ne_local` (kept as a
  one-line delegation onto the core's `Heap.lookup_set_ne` — its
  beq-shaped hypothesis is a view, not a copy). TwoSum's precursor:
  already on kit `DeadFrom` (no surviving local lemmas — recorded,
  nothing to delete).
- RECORDED, not lifted: WordFreq's `DeadFrom.push4/push5/push6`
  (arities the kit does not carry; single-consumer each, §12a).
- P6 ROLLBACK, grep-verified at this commit:
  `grep -rn "def FreshFrom|theorem FreshFrom.|theorem
  lookup_set_self|theorem lookup_set_other|theorem
  set_cons_self|theorem set_cons_ne|theorem lookup_cons_self|theorem
  set_set|theorem set_comm|theorem set_self_of_lookup"
  proofs/GoLeanProofs/Examples/` → ZERO hits (exit 1).
- MEASURED DELTAS (`git diff --numstat` at this commit): consumers
  −307/+40 (net −267 across 10 example files); kit +194 (StepKit);
  pins +39. Full proofs build 445 jobs green, first build after the
  retrofits.
- PINS: +18 (`Audit/Kit.lean` 195 → 213), transcribed verbatim from
  `.tmp/pinprobe8.lean`.
- Gate: `scripts/ci` PASS at this commit.

---

## Unit S2.2 — the CALL-SPAN combinator — this commit

- STATUS: done. Kit: `FuelMeasure.stepFnIter_call_span` (+27) — a
  whole call as entry (one conditioned `enterFrame` step via
  `stepFn_call_enter`) → callee BODY (one hypothesis, to the frame's
  `.returning`) → EXIT segment (one hypothesis, from that
  `.returning`); the three callee facts are the hypotheses, exactly
  the P-H schema. Plus `StepKit.loadMany_one`/`loadMany_two` (+17) —
  the conditioned result loads that feed `stepFn_return_frame`
  (promoted from the SliceQueue/SliceStack copies).
- CONSUMERS RETROFITTED (P6, same commit):
  - the frame-exit STEP copies deleted: fibmemo's
    `fm_frame_exit_step` and stein's `st_frame_exit_step` (both were
    "the kit has no frame-exit step" GAP-WITNESSes) — their three
    call sites now apply `stepFn_return_frame` + `loadMany_one`
    directly; stein's header note updated (it recorded the lift as
    ripe — it now records it done).
  - the loadMany copies deleted: queue's `loadMany_one`/`loadMany_two`
    (call sites resolve through `open GoLean.Surface`), stack's
    `st_loadMany1`/`st_loadMany2` (three call sites renamed).
  - the SPAN consumed at two landed sites: fibmemo `fmCall_base`
    (entry `have` deleted; assembly = one `stepFnIter_call_span`) and
    stein `st_ieCall` (the 20-link chain regrouped into
    prefix + span(entry, body-chain, exit-chain); the flat fuel `show`
    dropped — the regrouped literal sum is defeq to 32).
- HONEST NON-RETROFITS (recorded with reason): fibmemo
  `fmCall_hit`/`fmCall_build` and stein's `steinGCD` harness span keep
  their inline `stepFn_call_enter` chains — converting them deletes
  ZERO lines (pure regrouping of working 100-line proofs; the
  brick-wp churn-without-capability bar). The schema's ≥2-consumer
  bar is met by `fmCall_base` + `st_ieCall`.
- P6 ROLLBACK, grep-verified: `grep -rn "theorem loadMany_one|theorem
  loadMany_two|theorem st_loadMany|theorem fm_frame_exit_step|theorem
  st_frame_exit_step" proofs/GoLeanProofs/Examples/` → zero hits
  (exit 1).
- MEASURED DELTAS (`git diff --numstat` at this commit): consumers
  −98/+53 (net −45 across fibmemo/stein/queue/stack); kit +44
  (FuelMeasure +27, StepKit +17); pins +9. Full proofs build 445 jobs
  green.
- PINS: +3 (`Audit/Kit.lean` 213 → 216), verbatim from
  `.tmp/pinprobe9.lean`.
- Gate: `scripts/ci` PASS at this commit.
