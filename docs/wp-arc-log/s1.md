# S1 — lift wave 1 (pure lifts; every shape pre-drafted in the campaign ledger)

Charter section: `docs/2026-08-16_wp-arc-charter.md` §Slice 1. Owner:
the single s1 lane (worktree `.claude/worktrees/wp-arc`, one writer).
Format rules: `INDEX.md` in this directory.

The six lift families, in dependency order (charter order; each unit =
one commit; each commit carries the generic statement, ≥2 landed
consumers retrofitted, the P6 rollback grep-verified, fresh-probe
`Audit/Kit.lean` pins for new public surface, and measured deltas
here):

1. op-fact/`unorm` families + kind-generic `normalize_of_range` + the
   C4 `intKind_normalize_idem` lift-out-of-HeapBridge
2. `SliceMem.familyF`/`familyOf` + signed variants + `takePad`
   (= the drafted `prefixPadL`)
3. `swapList` surgery + count algebra
4. `Frame/Threshold.lean` (the five-site frame/rebase layer)
5. `FuelMeasure.stepFnIter_iterate_bail`
6. GAP-RESLICE + `stepFn_return_frame` + the queue glue combinators

Baseline at arc entry (derivation: `git log --oneline -1` = `a4e14f03`;
`scripts/capped lake build` 44 jobs + proofs 444 jobs, both green,
2026-08-16): tree clean, kit pins 116
(`grep -c '^#guard_msgs in #print axioms ' proofs/Audit/Kit.lean` → 116).

---

## Unit S1.0 — arc log init — this commit

- STATUS: done. `docs/wp-arc-log/{INDEX.md,s1.md}` created; the
  conventions line inherits the campaign's (INDEX).
- Ledger sources re-read in full before any lift: the charter, the R5
  design report (`docs/2026-08-16_wp-library-design.md` §1/§3 + the
  operator cross-correction note), `g1.md` (all drafts + gotchas),
  `g0.md` (the rw-ascribed-rfl and `unusedSimpArgs` notes, item (d)),
  the trip report (lessons + addendum).
- The four named gotchas, located and carried into every unit below —
  (a) `rw [(rfl : a = b)]` is unreliable — use `have h : a = b := rfl`
  (`g0.md` §G0.3c JC); (b) `linter.unusedSimpArgs` false-flags
  load-bearing simp args — suppress at site with reason (`g0.md`
  §G0.3b NOTE); (c) `stepFnIter_chain` nesting must match `+`'s LEFT
  associativity or the failure is a whnf heartbeat timeout (`g1.md`
  §Unit G1.3b PITFALL); (d) the storm signature rules — D-relative
  lookup hypotheses, qualified `Loc.base` in big positional arguments,
  ≤8-link chains, full type ascription on kit-lemma `have`s (`g1.md`
  §Unit G1.wordfreq route notes; StepKit docstring).

---

## Unit S1.1 — lift family 1: op-facts + `unorm` normal forms + kind-generic `normalize_of_range` + the C4 lift — this commit

- STATUS: done. `SliceMem` +14 public lemmas (SliceMem.lean +183/−2):
  the normal-form additions `unorm_nat`, `unorm_mul_nat`,
  `intKind_normalize_idem` (C4), the kind-generic pair
  `normalize_of_range_unsigned`/`normalize_of_range_signed`, and the
  completed integer executable-op family `applyStrictOp_{mul,div,add}_u64`,
  `applyStrictOp_sub_int`, `applyStrictOp_{eq,neq}Cmp_int`,
  `applyStrictOp_atMostCmp`, `applyStrictOp_not`,
  `applyStrictOp_convert_u64` — every statement byte-identical to a
  landed example copy (the A1/A2 one-family rule, g1.md §Unit G1.3b).
- CONSUMERS RETROFITTED (P6, same commit; per-family counts):
  `mul_u64` powmod + sieve; `div_u64` powmod + dedup + rle; `add_u64`
  sieve; `eqCmp/neqCmp/sub/convert` dedup (+ queue's LATENT convert
  copy deleted outright); `atMostCmp`/`not` sieve; `unorm_nat` SIX
  copies (dotprod, bubble, fib private, minmax `unorm_nat_wrap`,
  isort + selsort `unorm_nat_mod`) deleted with call sites re-pointed;
  `unorm_mul_nat` dotprod; `normalize_of_range_signed` kadane
  (`inorm64_of_range`, bits := 63) + strrev + wordfreq
  (`i32norm_of_range`, bits := 31); `intKind_normalize_idem`
  HeapBridge (delegates; its only consumer `storeLoc_int_cell` is
  internal, checked) + fibmemo's `unorm_idem` deleted (call site
  consumes the kit form; the C4 ruling text stays on record in the
  design doc §(a) and g1.md §Unit G1.7).
- P6 ROLLBACK, grep-verified at this commit: the only surviving
  example-local declarations of the family are NINE zero-proof-line
  delegations under pinned/roll-called names (powmod ×2, dotprod ×1,
  dedup div, rle div, kadane inorm64, strrev + wordfreq i32norm,
  HeapBridge idem); every other copy deleted
  (`grep -rn "^theorem applyStrictOp_(mul|div|add)_u64|..." Examples/`
  — the command and its 9-line output are in the commit's build log;
  re-runnable).
- MEASURED DELTAS (derivation: `git diff --numstat` at this commit):
  consumer modules −256/+97 (net −159); kit +183/−2; pins +36/−1.
  Elaboration: SliceMem 3.3 s (was ~3.5 s class — unchanged);
  retrofitted modules rebuilt inside their previous cost class (full
  proofs build 444 jobs green).
- PINS: +14 in `Audit/Kit.lean` (116 → 130,
  `grep -c '^#guard_msgs in #print axioms ' proofs/Audit/Kit.lean`),
  transcribed verbatim from a fresh probe (`.tmp/pinprobe1.lean`).
- JC: `intKind_normalize_idem` lifted into `SliceMem` (not `StepKit`)
  — it is a machine-integer NORMAL-FORM fact and SliceMem already owns
  that family; SliceMem's closure is Iris-free, which is what the C4
  ruling requires.
- JC: the kind-generic signed form is stated at width `bits + 1`
  (`k.bits? = some (bits + 1)`, hypotheses `-(2^bits) ≤ v < 2^bits`)
  — no degenerate-width side condition, and instantiation stays
  `rfl`-shaped (`(bits := 63) rfl rfl h0 h1`); a single if-typed
  statement (the ledger's literal `normalize_of_range (k) (hlo) (hhi)`
  sketch) would cost every instantiation site an `if`-reduction.
- JC (pin-freeze mechanics, the one non-obvious call):
  `DotProduct.unorm_mul_nat` is PINNED at `[propext, Quot.sound]` but
  the kit proof is `[propext]`; the delegation carries a commented
  `have _ := unorm_add_nat 0 0` anchor so the FROZEN shard stays
  byte-identical (charter: existing pins byte-identical at every
  commit) rather than re-pinning to the smaller set. Honesty note: the
  anchor is visible in the source with this reasoning, not hidden.
- rle's audit shard ROLL-CALLS `RunLength.applyStrictOp_div_u64`, so
  that name keeps its original three-hypothesis signature as a
  delegation (the kit form drops the unused `b < 2^64`; absorbed at
  the delegation boundary).

---

## Unit S1.2 — lift family 2: `familyZ`/`padZ` + `familyF` + `familyOf` + `takePad` — this commit

- STATUS: done. `SliceMem` +30 public lemmas over 5 new vocabulary
  defs (SliceMem.lean +278/−0): the fully generic `familyZ (g : Nat →
  Int)` map-over-range family (length/mem/succ/set/getD) with its
  zero-padded prefix `padZ` (zero/length/set/set_any/range — `set_any`
  IS GAP-P2c's two-store mid-list helper); the drafted
  `familyF (f : Nat → Nat)` uint64 shape (six facts +
  `prefixPad_familyF_set` + `familyMod_eq_familyF`, definitional);
  `iterStep`/`familyOf` (the iterated-step family, six facts); and
  `takePad` (zero/length/range/set/full — the drafted `prefixPadL` is
  this same shape, shipped under the `takePad` name).
- CONSUMERS RETROFITTED (P6, same commit): the SIX affine copies
  (GAP-P2b) — dotprod `dpFamA`, reverse `revFamily`, twosum
  `tsFamily`, stack `stFam`, queue `qFam`, minmax `mmFamily`
  (DESIGNATED def untouched; lemma PROOFS delegate) — all onto
  `familyF id`; rle `rleFamily` → `familyF (· / 3)` (GAP-P2d's sibling),
  dedup `ddFamily` → `familyF (· / 2)`; SortShared's LCG →
  `lcgStep_eq_iterStep` + `lcgFamily_eq_familyOf` (the familyOf
  instance, as chartered) with the six facts delegating through
  `familyZ`; kadane `kadFamily`/`kadPad`/`kadMid` (GAP-P2c, the signed
  family) → `familyZ`/`padZ`/`padZ_set_any`; minmax `setupList` and
  selsort `selPost` + bubble `bPost` (the copy-OUT prefixes) →
  `takePad`. Twelve consumer modules.
- P6 ROLLBACK, grep-verified: zero surviving copies of the standard
  family facts — the remaining `:= by` proofs among the family lemmas
  (grep shown in the build log: 16 declarations) are all genuinely
  example-specific composites (LCG range content, kadane's odd-index
  bounds and `set_even`, rle's `_const` regime lemmas, `qFam_take`,
  `selPost_full`/`mmFamily_ne_nil` etc.), none a copy of the lifted
  shapes. dpFamB (unwrapped `i+1`, not in the ledger's copy census)
  left as-is, recorded here.
- MEASURED DELTAS (derivation: `git diff --numstat` at this commit):
  consumer modules **−311/+174 (net −137 across 12 example files)**;
  kit +278; pins +70/−4. (**Corrected 2026-08-18**, pre-merge review
  finding B-4: published as −315/+174, net −141 across 13 example
  files. The four extra deleted lines are `proofs/Audit/Kit.lean`'s —
  already counted in "pins +70/−4" on this same line, so they were
  DOUBLE-COUNTED, and `Audit/Kit.lean` is not a 13th example file.
  Derivation: `git diff --numstat 49f4a835^ 49f4a835 --
  proofs/GoLeanProofs/Examples/` → 12 files, +174/−311. The s1 slice
  total moves with it: 2,483 → 2,479 deleted, net −1,551 → −1,547;
  `INDEX.md` §Audit corrections carries both.) Full proofs build 444 jobs green; every
  retrofitted module rebuilt in its previous cost class.
- PINS: +30 in `Audit/Kit.lean` (130 → 160, `grep -c` as before),
  transcribed verbatim from `.tmp/pinprobe2.lean`.
- JC: `takePad_set` shipped at WEAKER hypotheses than the ledger draft
  (`m < l.length → m < cap` instead of `m < n → l.length = n → n ≤
  cap`) — bubble's landed `bPost_set` needs exactly the weak form and
  the drafted form is an `omega` corollary of it; parameterize by what
  the witnesses need.
- JC: `familyZ` added as the common base UNDER the two drafted lifts
  (familyF/familyOf facts delegate to it) — it is what the signed
  witnesses (kadane, dpFamB) actually vary, and it is the layer that
  keeps the kit itself copy-free; not wider than the witnesses
  (element function + padding only).

---

## Unit S1.3 — lift family 3: `swapList` surgery + count algebra — this commit

- STATUS: done. `SliceMem` +9 public lemmas (+101 lines): the selsort
  GAP-WITNESS block moved VERBATIM — `getD_set_self`, `getD_set_ne`,
  `count_set_add`, `swapList` (machine store order, `m = i` degenerate
  folded in) with `swapList_length`, `getD_swapList_{fst,snd,other}`,
  `count_swapList` (the permutation half of any sort invariant),
  `range_swapList`.
- CONSUMERS RETROFITTED (P6, same commit): selsort Pure (the whole
  block deleted, −100/+14; `swapList` + `count_swapList` survive as
  shard-roll-called/pinned zero-proof delegations); bubble Pure
  (−51/+6: the `getD_set_*` copies AND the whole adjacent-pair
  decomposition apparatus `adj_decomp`/`adj_swap_decomp` deleted —
  `bstepL`'s swap arm IS `swapList l (i-1) i`, so `bstepL_count` is
  now one `count_swapList` delegation).
- RECORDED, not retrofitted: isort's `bubbleState_swap` — the ledger
  calls it "the same surgery in structural clothing", and it is, but
  its proof content is `bubbleState`'s getElem characterization, not
  the set algebra; rewriting it through `swapList` deletes nothing
  (the brick-wp "churn without capability" non-promotion class). The
  lift's ≥2-consumer bar is met by selsort + bubble.
- P6 ROLLBACK, grep-verified (command + output in the build log): the
  only surviving declarations are selsort's two pinned delegations and
  sieve's PRIVATE `getD_set_self/ne` at `List Bool` — a different
  element type, owned by the PARKED element-kind-generic SliceMem item
  (charter §raft checkpoint), recorded here, not lifted.
- MEASURED DELTAS (`git diff --numstat`): consumers −151/+20 (net
  −131); kit +101; pins +25/−2. Full proofs build 444 jobs green.
- PINS: +9 in `Audit/Kit.lean` (160 → 169), verbatim from
  `.tmp/pinprobe3.lean`.

---

## Unit S1.4 — lift family 4: `Frame/Threshold.lean` (the five-site frame/rebase layer) — this commit

- STATUS: done — the biggest single item, taken fresh mid-slice as
  chartered. NEW kit module `proofs/GoLeanProofs/Frame/Threshold.lean`
  (441 lines): `ρT T d` + `lt`/`ge`/`shiftSpec_ρT`/`ρT_zero_app`;
  `bumpAt T r` + `renameLoc_ρT_bump` (the bump transport, drafted as
  `renameLoc_shiftAt_succ`); `retiredFrame` (the retired-cell list as
  a laid-out Heap) + its four lookup lemmas; `renameValue_id` (the
  unbounded sibling of `renameValue_locFree`, same induction) feeding
  `frameSim_seed` — a GENERIC zero-shift seed that DELETES the
  per-example `frameSim_zero` cell enumerations entirely; `CellFixed`
  (+ `of_locFree`); `rebaseSimT` with the front's per-cell obligations
  as ONE hypothesis (`hfront`, per the g1 drafts — the two ledger
  sketches agree and this is their shape) and the retired cells as a
  parameter; `transfer_segT` (verbatim from the five copies). Plus
  `Surface.lookup_append` (the match form) in StepKit, exactly where
  the selsort GAP-WITNESS note asked.
- ALL FIVE SITES RETROFITTED (P6, same commit): isort `ρsh` (T=4,
  retire 2, PassFrame −228/+43 + Canonical), isort `ρ11` (T=11,
  retire 2, Subject −227/+47), isort `ρ21` (T=21, retire 4, Count
  −309/+86), selsort `ρ16` (T=16, retire 3, Frame −366/+74), bubble
  `ρ16` (T=16, retire 3, Frame −344/+60). Every `ρ*` def is now a
  one-line `ρT` wrapper; every `shiftSpec_ρ*`, `bump*`,
  `renameLoc_bump*`, `fs_lookup_none*`, per-example `lookup_append`
  proof and `frameSim_zero*` enumeration deleted; every `rebaseSim*`
  is ONE `rebaseSimT` application + the fixed-cell enumeration (the
  residue the ledger predicted); every `transfer_seg*` a delegation.
  Shard-pinned names (`rebaseSim3` selsort, `rebaseSim16` bubble)
  survive with unchanged axiom sets (shards green in-build).
- Rebase conclusions now spell the frame as `retiredFrame`; the three
  outer inductions' explicit frame lists updated in the same commit
  (internal proof terms; no statement a reader sees changes).
- MEASURED DELTAS (`git diff --numstat`): consumers −1481/+317 (net
  −1164 — the single largest deletion of the slice); kit +441 (new
  module) +15 (StepKit) + pins +43. Full proofs build 445 jobs green;
  retrofitted modules rebuilt inside their previous cost classes.
- P6 grep (shown in build log): zero surviving copies of the
  shift/rebase machinery; the two grep survivors are wordfreq's
  unrelated pure `bumpW` and PassFrame's `lookup_append` DELEGATION
  (kept for its isort-internal consumers).
- **AUDIT CORRECTION (2026-08-18, pre-merge review finding B-1): the
  zero-survivor claim above was FALSE AS PUBLISHED.** One copy did
  survive this unit's sweep — `fs_lookup_none11` in
  `Examples/InsertionSort/Subject.lean` (the kit-superseded
  `FrameSim.lookup_img` wrapper, which the bullet at "every
  `fs_lookup_none*` … deleted" claims was removed). It was stranded,
  not used: `grep -rn fs_lookup_none11` over the tree returned exactly
  ONE hit, the declaration itself, so nothing downstream depended on
  it and the retrofit was semantically complete — only the deletion
  was not. The sweep's recorded grep (in the build log, not
  transcribed here) enumerated the machinery by name pattern and this
  stranded wrapper fell outside it; the same name-enumeration hole
  produced the s2 miss recorded in `s2.md` §S2.1. **The copy is now
  deleted** (audit-fix commit, this branch); full proofs build green
  after the deletion. Corrected claim: zero surviving copies of the
  shift/rebase machinery holds AT THE AUDIT-FIX TIP, not at this unit's
  commit. Nothing else in this unit's deltas or pins changes — the
  deleted theorem was never pinned.
- PINS: +18 (169 → 187), verbatim from `.tmp/pinprobe4.lean`.
- JC: the signed/generic seed needed `renameValue_id` (∀-identity ρ),
  which did not exist — written once in the kit by the same
  `renameValue.induct` pattern as `renameValue_locFree` rather than
  bounding every seed by `locSup` enumeration.
- JC: `rebaseSimT` takes `σOut`/`σIn` as separate states related by
  hypotheses (front agreement + high-emptiness + flat-heap) rather
  than requiring `σIn.heap = σOut.heap ++ retired` syntactically —
  the five landed sites differ in how their σIn is spelled, and the
  lookup-level hypotheses are what they all discharge by `rfl`.

---

## Unit S1.5 — lift family 5: `FuelMeasure.stepFnIter_iterate_bail` — this commit (CLOSED AT TWO RETROFITS + RECORDED DISPOSITIONS under the pause order)

- STATUS: schema landed; TWO of five consumers retrofitted (the
  charter's ≥2 bar met); the remaining three RECORDED below with exact
  pickup plans (the operator's orderly-pause order arrived mid-lift;
  the two clean retrofits were finished, the rest parked honestly).
- THE SCHEMA (`FuelMeasure.lean` +57): the drafted three-hypothesis
  shape (g1.md §Unit G1.3), extended per the R1-closure lesson with an
  invariant `I : Nat → Prop` (threads what the exit needs — palin's
  `PalinUpTo`) and a terminal PREDICATE `Q : Config → ExecState →
  Prop` (the two exits stop at different, existentially quantified
  states). `hstep` is ONE hypothesis (bail-or-iterate disjunction);
  bound `c·(n−i) + max b e` — both landed instantiations' constants
  are exactly `max b e` (70 = max 70 49; 79 = max 79 58).
- RETROFITTED: palin `ph_loopP` (HarnessR −31/+54 — the strongRecOn
  scaffold is one schema instantiation; statement byte-identical) and
  strrev `p_loop` (Palin.lean −32/+56, same). Both compiled on the
  first build. Net lines are ROUGHLY FLAT per consumer here — the
  honest accounting: the value is the SCHEMA carrying the induction
  (no hand strongRecOn, no fuel-arithmetic case split), as with
  GAP-R1's closure; successors write zero induction lines.
- PINS: +1 (`Audit/Kit.lean` 187 → 188, fresh probe
  `.tmp/pinprobe5.lean`).
- RECORDED, not retrofitted (pickup plans, in order of ease):
  1. twosum (`TwoSum/Subject.lean:1368`, the outer loop with early
     return): shape fits the schema (T := row states, I := the
     `tsAnsF` answer-function equation, Q := terminal with the found
     pair existential); the inner-loop miss runs stay `stepFnIter_iterate`.
     Next action: mirror the palin transformation; ~1 h.
  2. rle: its `Nat.strongRecOn` scaffolds were already absorbed into
     segment composites at `n ≤ 3` (no surviving two-exit induction to
     replace — check `grep strongRecOn Examples/RunLength/` = zero
     hits); record as NO-OP or find the residual scaffold; verify
     before claiming.
  3. bubble (`BubbleSort/Outer.lean:46`): the two-exit induction is
     INTERLEAVED with the frame layer (each iteration transfers +
     rebases, the successor state is existential through
     `transfer_seg16`), so the deterministic-`T` schema does not fit
     as-is; retrofit needs either a δ-abstract descriptor variant of
     the schema (mapPickLoop-style) or leaving the scaffold — assess
     against the brick-wp "churn without capability" bar before
     forcing it. Same consideration applies to selsort/isort outer
     loops (not in the five; noted for completeness).
- Gate: `scripts/ci` PASS at this commit (proofs-only; standing full
  record with stale-note).
- Correction to Unit S1.4's log entry: `Frame/Threshold.lean` is 477
  lines, not the 441 written there (the 441 was a pre-final count;
  derivation: `wc -l` at commit time printed 477 in the build log).

---

## Unit S1.5b — the bail stragglers: twosum + bubble retrofitted via a
## RELATIONAL/MEASURE-INDEXED schema; rle verified NO-OP — this commit

- STATUS: done. Lift family 5 CLOSED at 4 of 5 consumers retrofitted +
  one verified NO-OP (= all five dispositions final).
- THE FINDING (corrects the park record's pickup plan): twosum does
  NOT mirror palin. Its outer rows have VARIABLE cost
  (`ts_rowMiss` = `100 + 57·(n−t−1)` steps, shrinking per row), and
  the deterministic schema's constant-`c`/`max b e` bound cannot
  reproduce the frozen `(57n+106)·μ + 71` statement (the bail cost
  `163 + 57·(u−t−1)` exceeds the `+71` slack that `max b e` covered
  in palin). Bubble is as the record predicted: successor states are
  existential through `transfer_seg16`/`rebaseSim16`. ONE
  generalization covers both: `FuelMeasure.stepFnIter_iterate_bail_rel`
  (FuelMeasure +78/−26, net +52 with the corollary delegation) —
  states a per-index PREDICATE `S i σ` (the δ-descriptor
  variant), fuel a single measure `B : Nat → Nat` with the iterate
  branch supplying its cost `c` and the descent obligation
  `c + B (i+1) ≤ B i`. The deterministic schema is reproved as its
  special case (`S i σ := σ = T i ∧ I i`, `B i := c·(n−i) + max b e`)
  — statement byte-identical, kit carries the induction exactly once.
- RETROFITTED (statements byte-identical, both compiled on the FIRST
  build): twosum `ts_outerP` (TwoSum/Subject.lean −48/+84 — growing
  dead region `D' ++ tsLive`/`na'+2` and the `tsAnsF` answer stability
  enter as descriptor existentials; no recursive heap family needed)
  and bubble `bOuter_loop` (BubbleSort/Outer.lean −56/+88 — the
  descriptor carries list/shift/frame/FrameSim; e-index runs `e - j`).
  Frozen shard pins `Audit.TwoSum`/`Audit.BubbleSort` green at
  `[propext, Classical.choice, Quot.sound]`, unchanged.
- rle: VERIFIED NO-OP — `grep -rn "strongRecOn\|iterate_bail"
  proofs/GoLeanProofs/Examples/RunLength/` → zero hits (exit 1), as
  the park record predicted: its scaffolds were already absorbed into
  `n ≤ 3` segment composites. No surviving two-exit induction exists
  to replace; recorded, nothing edited.
- MEASURED DELTAS (`git diff --numstat` at this commit): consumers
  −104/+172 (net +68 — the honest accounting, as with the palin/strrev
  retrofits: the schema DELETES the hand `strongRecOn` inductions and
  fuel-arithmetic case splits; the descriptor plumbing costs lines.
  The value is structural — successors write zero induction lines, and
  the frame-interleaved shape now has a kit form); kit +78/−26
  (schema added, constant-schema induction replaced by delegation);
  pins +5/−0.
- PINS: +1 (`Audit/Kit.lean` 188 → 189), transcribed verbatim from a
  fresh probe (`.tmp/pinprobe6.lean`): `stepFnIter_iterate_bail_rel`
  at `[propext, Quot.sound]`; the probe also re-checked the delegated
  `stepFnIter_iterate_bail` — still `[propext, Quot.sound]`, frozen
  pin untouched (gotcha 1 did not fire; no anchor needed).
- JC: the generalization went into the KIT rather than forcing the
  deterministic shape onto twosum with a weakened bound — the charter
  freezes statements, so the schema had to meet the frozen bounds, not
  vice versa; `_rel` has two landed consumers of its own (the §12 bar)
  plus the in-kit corollary.
- JC: bubble's retrofit passes the brick-wp "churn without capability"
  bar (the park record's stated concern): it is NOT churn — it removed
  the last frame-interleaved `strongRecOn` and demonstrated the
  descriptor form the selsort/isort outer loops (outside the five) can
  reuse.

---

## Unit S1.6 — lift family 6: GAP-RESLICE + `stepFn_return_frame` +
## the queue glue combinators — this commit (CLOSES SLICE 1)

- STATUS: done. Three homes per the park-record plan:
  (a) `SliceMem.applyStrictOp_sliceExpr_slice` — the GENERAL
  `s[lo:hi]`-at-slice-base form (`lo ≤ hi ≤ cap`, input validates at
  `len ≤ cap`; result `⟨some b, off+lo, hi−lo, cap−lo⟩`), unifying the
  three landed copies: dedup's `s[0:kv]` (lo = 0, len = cap = n),
  stack's `s[0:d]` (lo = 0, capacity kept), queue's MOVING-OFFSET
  `q[1:len]` (lo = 1). (b) `Surface.stepFn_return_frame` +
  `Surface.stepFn_block` → StepKit's P1 family (single-step
  conditioned facts). (c) the three `stepFnIter_*` glue composites
  (`splice_pop`, `drain3`, `block_pop`) → FuelMeasure beside
  `stepFnIter_chain`.
- CONSUMERS RETROFITTED (P6, same commit): dedup + queue
  `applyStrictOp_sliceExpr_slice` and stack `st_sliceExpr_slice` +
  `stepFn_return_frame` survive as PINNED zero-proof delegations
  (shard roll-calls at `Audit/{DedupAdjacent,SliceQueue,SliceStack}`;
  statements byte-identical, pins unchanged); queue's UNPINNED
  `stepFn_return_frame` + four glue locals DELETED OUTRIGHT — the
  ~40 call sites resolve unchanged through `open GoLean.Surface`
  (marker comments left at both deletion sites); stack's private
  `st_checkSliceBounds` (its only consumer was the reslice proof)
  deleted with the proof.
- Glue-combinator consumer grading, recorded as the ledger graded it:
  ONE landed consumer (queue) + stack LATENT; at-bar family-wise via
  the commit's other members (reslice 3 landed, return_frame 2 landed
  + fibmemo/stein's span shape), exactly the park-record disposition.
- P6 ROLLBACK, grep-verified (`grep -rn "theorem
  applyStrictOp_sliceExpr_slice|theorem st_sliceExpr_slice|theorem
  stepFn_return_frame|theorem stepFn_block|theorem
  stepFnIter_(splice_pop|drain3|block_pop)" proofs/GoLeanProofs/` —
  output in this commit's build log): kit homes + exactly the FOUR
  pinned delegations; zero other survivors.
- JC (the one structural call): the `stepFnIter_*` composites went to
  `FuelMeasure`, NOT StepKit — they compose `stepFnIter_chain`, and
  StepKit does not import FuelMeasure; instead FuelMeasure gained
  `import GoLeanProofs.StepKit` (no cycle; both are `GoLean.Surface`;
  the split principle — single-step conditioned facts in StepKit,
  iteration glue in FuelMeasure — is recorded in StepKit's P1
  docstring).
- MEASURED DELTAS (`git diff --numstat` at this commit): consumers
  −113/+42 (net −71: dedup −13/+8, queue −76/+17, stack −24/+17); kit
  +103/−2 (SliceMem +27, StepKit +35/−2, FuelMeasure +41); pins
  +17/−0.
- PINS: +6 (`Audit/Kit.lean` 189 → 195, `grep -c` as before),
  transcribed verbatim from a fresh probe (`.tmp/pinprobe7.lean`), all
  at `[propext, Quot.sound]`.
- Gate: `scripts/ci` PASS at this commit; SliceStack rebuilt at 149 s
  (its standing heavy-module cost class), SliceQueue in-class.

---

## SLICE 1 CLOSE-OUT (at unit S1.6)

All six chartered lift families LANDED; the lift-5 stragglers resolved
in S1.5b (4 retrofits + 1 verified NO-OP across the five consumers).
P6 sweeps re-verified at this tip (the S1.1–S1.4 grep commands re-run;
same survivors as recorded — pinned/roll-called delegations only).
Slice totals (derivation: per-unit `git diff --numstat` figures above;
deleted 256+315+151+1481+63+104+113 = 2,483; inserted
97+174+20+317+110+172+42 = 932): **2,483 landed consumer lines
deleted, net −1,551**; kit pins 116 → 195 (+79). Every summary number
re-derivable from the unit entries. Slice 2 (the new-shape wave)
starts next per the charter.

---

## PARK RECORD — orderly pause (operator order, 2026-08-16)

The operator paused the arc for compute reasons mid-slice. Stop point
chosen per the order: lift 5 was within reach of a green commit and
was closed at its ≥2-consumer bar (unit S1.5); lift 6 was NOT started.
Tree CLEAN at the park commit; nothing stashed, no WIP files (the only
untracked artifacts are `.tmp/pinprobe*.lean`, the pin-derivation
scratch probes cited by the unit entries).

### Per-lift disposition

| lift | status | next action |
|---|---|---|
| 1 op-facts/unorm/normalize_of_range/C4 | LANDED (`cdea1684`) | — |
| 2 familyZ/padZ + familyF + familyOf + takePad | LANDED (`49f4a835`) | — |
| 3 swapList + count algebra | LANDED (`78c97edb`) | — |
| 4 Frame/Threshold.lean (five sites) | LANDED (`3f3e2100`) | — |
| 5 stepFnIter_iterate_bail | LANDED at 2/5 retrofits (`4d95059f`) | pickup plans for twosum/rle/bubble in unit S1.5 (twosum first, ~1 h, mirrors the palin transformation) |
| 6 GAP-RESLICE + stepFn_return_frame + queue glue | NOT STARTED | (a) `applyStrictOp_sliceExpr_slice` → SliceMem, GENERAL `s[lo:hi]`-at-slice-base form covering dedup's `s[0:kv]` (DedupAdjacent.lean:698, base offset 0) AND queue's `q[1:len]` (SliceQueue.lean:1542, moving offset) — result `⟨some b, off+lo, hi−lo, cap−lo⟩` with `lo ≤ hi ≤ cap`, both locals become delegations; stack's third copy: find via `grep -n sliceExpr Examples/SliceStack.lean`. (b) `stepFn_return_frame` → StepKit: queue's is at SliceQueue.lean (§ after the re-slice fact, GAP-FRAME marker); stack re-derives it — grep `return_frame\|returning` in SliceStack. (c) the four queue glue combinators (`stepFn_block`, `stepFnIter_splice_pop`, `stepFnIter_drain3`, `stepFnIter_block_pop`, SliceQueue.lean ~§3472 at the landing commit) → StepKit P1 family; ledger grading: ONE landed consumer + stack LATENT — move them, delegate queue's names, record stack as latent (the §12 bar is met family-wise by the commit's other members; or record the combinators as at-bar-via-latent exactly as the ledger does). Each with fresh pins + P6 grep. |

(SHAs transcribed from `git log --format="%h %s" -6` at the park
tip, re-run after a first draft of this table mis-keyed lift 2's —
the summary-layer lesson 15 reflex, applied to itself.)

### Gotchas discovered this session (beyond the four briefed ones)

1. Pin-freeze mechanics: a delegation can SHRINK a pinned name's axiom
   set and break the frozen shard — anchor the historical dependency
   visibly (`have _ := unorm_add_nat 0 0`, DotProduct.unorm_mul_nat)
   rather than re-pinning (unit S1.1 JC).
2. `ρ*`-wrapper simp sets: every `simp [renameLoc, ρ16]`-style site
   needs `ρT` added once the local shift is a kit wrapper (five sites
   found by build failure — grep `simp \[renameLoc, ρ` on any future
   threshold retrofit).
3. `retiredFrame`'s consecutive addresses (`b+1+1`) are NOT defeq to
   the examples' `17+d`/`18+d` spellings — pass `retiredFrame` in the
   outer inductions' frame arguments instead of explicit lists (three
   sites; the frame is existential so no statement moves).
4. `rw` inside an unreduced `match some c with ...` scrutinee fails
   ("did not find occurrence") — `show` the reduced form first
   (rebaseSimT and the frameSim_seed proofs; same class as the
   g0 rw-ascription gotcha).
5. Implicit-`T` elaboration: `ρT_ge (d := ...) (a := ...) (by omega)`
   inside a `show ... from by simp [...]` leaves the `by omega` with
   an unresolved `?T` — pass `(T := T)` explicitly at such sites.
6. `proofs/.tmp/` is swept by the proofs-file audit coverage gate —
   scratch probes go in repo-root `.tmp/`, never under `proofs/`.

### Gate state at the park commit

`scripts/ci` run at this tip (the park commit itself is docs-only over
the green lift-5 tip): recorded below after the run — see the commit
message for PASS confirmation; the standing full differential record
(1853/1853 + 311/311 negative, recorded at `a4e14f03` in this
worktree) carries with the visible stale-at-HEAD note, correct for a
proofs-only lane.

### Resume point

Resume = lift 6 (disposition row above), then the s1 close-out:
re-grep the P6 sweeps at the tip, refresh INDEX totals, checkpoint 2,
then slice 2 per the charter. The three lift-5 stragglers can ride
with lift 6's commit or a small S1.5b.
