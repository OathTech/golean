# Dossier U-4 — overlapping copy/append aliasing coverage ((d) UNKNOWN — now analyzed)

G3 register item 21. Inventory §6 U-4. Proposal only — batched for
user ruling; the corpus-case list below is an INPUT for the
corpus-owning lane, nothing is added here.

## 1. The question

`copy` (and append's copy phase) with overlapping src/dst is
spec-DEFINED — no latitude — but the differential coverage validating
the machine's multi-cell one-step arms against the forced point was
recorded as owed. The unknown was whether an aliasing shape exists
where the machine's arm diverges.

## 2. Evidence

### SPEC (go1.26.5, §Appending to and copying slices)

> "For both functions, the result is independent of whether the
> memory referenced by the arguments overlaps."

FORCED (class (c)) — confirmed; the entry is a coverage debt, not
latitude.

### GC probe (`docs/evidence/2026-08-15-dossier-u4/`, verbatim)

Five shapes — forward overlap (`1 1 2 3 4`, not the naive in-place
loop's `1 1 1 1 1`), backward overlap, self-copy identity, in-place
aliasing append (`1 1 2 3`), spill self-append (`7 8 9 7 8 9`) — all
realize as-if-intermediate.

### Machine arm (read-only verification)

`copySlice` (Machine.lean:756–819 region): loads ALL source values
into an intermediate array BEFORE storing any destination cell — the
as-if-intermediate semantics BY CONSTRUCTION; the naive-cascade
answer is unreachable. The same load-all-then-store shape governs the
append spill path. CONCLUSION on the unknown: no divergence candidate
exists in the arm's sequential semantics; the debt is purely
test-coverage, not a suspected bug.

### Scope boundary respected

U-5 (the same arms' GRANULARITY under concurrency — single apply
step) is a SEPARATE, concurrency-only register item, out of this
campaign's scope; nothing here discharges the granularity-ledger
re-audit.

## 3. Proposed disposition

**CONFIRM FORCED (no latitude); hand the coverage debt to the
corpus lane as a concrete case list.** Proposed cases (corpus-lane
INPUT — the probe program is directly convertible):

1. `slices/copy-overlap/forward` — `copy(a[1:], a[:4])`, readout the
   full array (discriminates as-if vs naive cascade).
2. `slices/copy-overlap/backward` — `copy(b[:4], b[1:])`.
3. `slices/copy-overlap/self` — `copy(c, c)`.
4. `slices/append-alias/in-place` — `append(d[:1], d...)` under
   sufficient cap (discriminates pre-append source snapshot).
5. `slices/append-alias/spill` — `append(f, f...)` at exact cap.

- ARGUMENT: the cheapest possible closure of a recorded TODO — the
  cases are written, oracle outputs known, and the machine arm is
  verified as-if by reading; expected result is five greens, after
  which U-4 is CLOSED rather than owed.
- COUNTER-ARGUMENT: none of substance — the only question is lane
  priority, which is the corpus owner's call.
- WHAT WOULD CHANGE THE ANSWER: any of the five cases going red
  (would reveal a store-path subtlety the read missed — exactly what
  the differential is for).

## 4. Evidence gaps

None for the sequential question. The concurrency-side granularity
re-audit stays owed under U-5 (out of scope here, recorded).
