# W3 charter (2026-08-27) — the function specs, under the adopted invariant

Contract: `docs/2026-08-27_w25-invariant-design.md` (ADOPTED — see its
status header; the three open points resolved: ghost-acks, separate
lens readers, T1 payload scope). Plan item: W3 of
`docs/2026-08-27_clean-proof-plan.md`. Lane: `w1-prover` worktree
(one writer). Every unit charter opens with its quantifier-audit
line; every new mechanism carries a LINEAGE line; no statement
contains a step count, round count, or fixture identity outside
declared reflection certificates — scaffolding is labeled at birth.

## Wave structure ([AGENT])

The serialization resource is the INTERFACE LAYER (the invariant
module, the ghost extension, the reader extension) — it lands first
and alone; handler specs then consume it, file-disjoint.

### Wave 3.0 — the interface wave (sequential, one worker)

- **U3.0a — the ghost-acks extension**: extend the abstract Ghost
  with an acks map mirroring votes/victories; `certified`
  instantiated as ∃-quorum-of-recorded-acks; re-verify the two
  retained interface witnesses still discharge (they are the
  non-vacuity gauges). Quantifier line: advances no end-theorem
  quantifier itself — interface for the ack/Match clause (C4) and
  the Match-evidence unit.
- **U3.0b — the reader extension**: separate lens readers for
  leaderOf / byIndex / got / per-node cursors (C3's vocabulary);
  each reader with its renaming-congruence lemma (the `_ren` family
  pattern in RenCongr.lean). No AbsTwinV0 changes.
- **U3.0c — the invariant module** (`Specs/RaftPilot/Invariant.lean`
  or sibling): `I` as a Lean predicate per the note — Base, Pair
  (∃-placement ∃-ghost relational form + shell-sync), CheckerCorr
  (∃ event histories + fold-state equations), NetCorr (four
  clauses), Hygiene, Stream, the Electing/Elected phase split.
  DEFINITIONS ONLY plus definedness sanity lemmas; preservation is
  the handler waves' job. Every clause stated in reader vocabulary
  + placement data, never cell enumerations.

### Wave 3.1 — handler CallSpecs (file-disjoint after 3.0)

The reachable-handler census (from the driver's four-branch body +
the library's dispatch): stepCandidate/stepLeader/stepFollower
branches actually reachable in the T1 fragment, becomeFollower
(HAVE, W1), becomeCandidate, becomeLeader, campaign/hup path,
handleAppendEntries, handleHeartbeat (if reachable), poll/
recordVote, maybeCommit, appendEntry, bcastAppend/sendAppend,
send/mstore paths, and the newTwin-prefix library functions (init
stage B's need). EACH unit: the CallSpec (∀-state over
reader-predicate pre, ∀-stream, call-span shape), the I-clause
conclusions it owes (per the note's consumption contract), the
emissions-preserve-NetCorr conclusion with that handler's emission
census, and the guard-silence conclusions its census routes assign.
Workers verify the exact reachable set against the wire before
building — the list above is a charter estimate, not a census.

### Wave 3.2 — the named hard units (sequential, Fable)

- **U3.2a — the phase transition**: the winning MsgVoteResp
  delivery ESTABLISHES Elected (H₀ from the noop-first-propose,
  (ldr,tm) fixed by C4's population clause).
- **U3.2b — the Match-evidence unit**: instantiate `certified` from
  ghost-acks at the leader's commit-advance; discharge
  leaderCommitOk.
- **U3.2c — the harvest-quiescence measure**: the 64-round guard
  proved silent via a lexicographic drain measure — the hardest
  totality obligation; charter it with an explicit park condition
  (if the measure refutes, park with the record; do not weaken the
  guard census).
- **U3.2d — the checker reshape**: the checker's body spec over the
  projections (harvest arc4d's projLOf/projBy/encGS), concluding
  C3's fold-state equations + guard silences.
- **U3.2e — the driver body spec**: case analysis over the
  invariant-constrained net population at the pick, symbolic in the
  net list; concludes I-preservation for the full body. (The W4
  loop instance consumes exactly this.)
- **U3.2f — init stage B**: the newTwin prefix via the 3.1 library
  CallSpecs; joins `initSetup_establishes` to conclude I at the
  loop head (the base case, C1-complete).

## Gates and honesty

- Per-wave checkpoint in `docs/w3-prover-log.md` (≤5 units between
  checkpoints); gate (`scripts/ci`, capped, build-lock protocol)
  at wave boundaries; branch stays `w1-prover`.
- Successors re-verify predecessors' top claims; bounds ship as
  bounds; every summary number derivation-anchored.
- Park-not-weaken: any refuted costing parks the unit with a
  record and surfaces in the log — never a silent narrowing of a
  spec's conclusions.
- [AGENT] calls logged in the prover log; anything trust-adjacent
  flagged for the landing audit.

## AMENDMENT 1 (2026-08-27, [AGENT]) — the census lands; Wave 3.1 partitioned

`docs/2026-08-27_w31-reachability-census.md` replaces the charter's
handler estimate. Consequences, binding on the waves:

**Invariant addenda (fold into U3.0c / the note at the next touch):**
- **The term-bound clause** (new, C1/C4-adjacent): every node term
  and every net-message term ∈ {0, tm} with tm the single campaign's
  term — justified by the driver's one opCampaign (program text) +
  no ticks + no MsgTimeoutNow (C4 population). This is what kills
  raft.Step's entire m.Term < r.Term block (census §2.2.1) and
  grounds R3's canVote analysis.
- **Probe-state vocabulary in Pair**: the reject arm is REACHABLE
  (census R1/§2.2.2) — the leader's per-follower Progress data
  (Match/Next/State incl. StateProbe, inflights) is part of C2's
  concrete side, with the consistency facts (Match ≤ log length,
  Next ≥ Match+1 ≥ 2) that keep the snapshot family dead (census
  §2.4 chain) and U3's assertions unreachable.
- **U3 = spec obligations**: the census's assertion branches
  (commitTo/appliedTo range panics, send's term-stamping panics,
  self-addressed send) are NOT pruned by reachability — each is
  proved unreachable as a conclusion of the covering spec (the
  NeverFaults half). Listed per cluster below.
- **U4 recorded**: the D-11 jitter draw runs (reset →
  resetRandomizedElectionTimeout) but its value is unobservable in
  T1 — the choice-stream clause (C5) must thread these dead draws
  (spans consume a draw; suffix monotonicity absorbs it).

**The Wave 3.1 partition (spec clusters, not one-unit-per-function;
~172 reachable functions per census §4.1):**

| unit | cluster | contents (census refs) | consumed by |
|---|---|---|---|
| U3.1-F | shared log/util layer | raftLog term/entries/slice (U1 costed reachable)/firstIndex/lastIndex/matchTerm/zeroTermOnOutOfBounds + unstable read family + mustCheckOutOfBounds (U3: range obligations) | C/D/E |
| U3.1-B | the harvest engine | HasReady/Ready/readyWithoutAccept/acceptReady/Advance + storage Append/SetHardState + the two StorageResp local steps (stableTo/appliedTo) + unstable accept family | driver body, U3.2f |
| U3.1-C | election cluster | Campaign/hup/campaign/becomeCandidate/poll/becomeLeader + Step prelude (term-bound kills the low-term block) + MsgVote grant/reject (R3) + becomeFollower both arms (R2) + isUpToDate + RecordVote/TallyVotes/VoteResult | phase transition U3.2a |
| U3.1-D | replication cluster | Propose/stepLeader-MsgProp/appendEntry/bcastAppend/maybeSendAppend/send (U3: panic sites) + stepFollower/handleAppendEntries all three exits (R1) + maybeAppend/append/findConflict/commitTo/truncateAndAppend | S23 carrier, checker |
| U3.1-E | ack/commit cluster | stepLeader-MsgAppResp BOTH arms (reject: MaybeDecrTo/BecomeProbe/findConflictByTerm; accept: MaybeUpdate/FreeLE/maybeCommit/CanBumpCommit/bcastAppend) + Inflights + CommittedIndex | Match-evidence U3.2b |
| U3.1-A | init cluster | newRaft/validate/confchange-init chain (14+1 fns, §2.5)/MemoryStorage init/NewRawNode | init stage B U3.2f |

Order: F → B → {C, D} → E → A (F most-consumed first; C and D
file-disjoint after B; A alongside). U2 (duplicate-ack probe
disjunct) is UNDECIDED — the E worker covers the arm anyway (specs
are ∀-state over the invariant; per-run reachability is irrelevant).
The census is an [AGENT] instrument: each worker re-verifies the
census claims its unit builds on against the wire.
