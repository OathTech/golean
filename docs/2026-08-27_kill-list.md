# The kill-list (2026-08-27) — evidence-based, awaiting [USER] approval

Criteria ([USER]): keep only what serves (a) the clean proof path
(`docs/2026-08-27_proof-structure-explained.md`) or (b) general
reasoning capability. Bias to kill; green tests are not a defense;
enumerative infrastructure dies irrespective of consequences.
Evidence: the quantification census (2026-08-27; per-module verdicts
RULE/TRAJECTORY/LITERAL/READER/MIXED with importer graphs). Nothing
below executes without approval.

**Mechanism**: `archive/fixed-trajectory-era` = a branch ref at
pre-kill main + a short tracked `docs/ARCHIVE.md` (what lived there,
the design lessons, the pointer). History preserves everything; the
kill is deletions on a working branch → normal ceremony → main.

## K-A: the literal data — DELETE (59 modules, 1,184,100 lines)

All 57 generated `*Lit*` modules under Specs/Raft (census class c,
zero theorems, `GENERATED — DO NOT EDIT`) + `TwinCheckpoints` +
`TwinCheckpointsF`. Importers: only their own equation siblings (K-B)
and the aggregators. Nothing on the proof path reads a literal.

## K-B: the trajectory theorems — DELETE (≈66 modules, ≈17,600 lines)

Census class b: every `*Equation`/`*Fixture`/`*Steps*`/`*Resite`/
`*31` handler chain; the four `Round*Eq*/Equation/Lemma` chains;
`RingEq*`/`RingEquation`; `HhEquation` + `HandlerEq`/`HandlerEqSym`
pilots; `SeedPin`/`SeedWitness`; ALL witnesses (`*Witness*` incl.
`RingWitness`, `ShapeWitness`, `DriverNetWitness`,
`BecomeFollowerWitness`, `RoundInductionWitness`,
`NativeS1Witness`/`NativeS23Witness`, ChoiceCanon's witness);
`StaticCells`/`StaticCellsExt`; `LensInst`; `TwinPrelude`
(`twin_prelude_eq`); `StateWire` (the state-reflection elaborators —
sole consumers are the killed checkpoints). Rationale: fixed
trajectories/fixture anchors; the doctrine bans them from proofs and
the plan replaces their roles (init spec, function specs, loop rule).

## K-C: mixed modules — SPLIT (keep the rule half, delete the rest)

| module | keep | delete |
|---|---|---|
| RoundStatement | `AbsTwinV0` + `absTwinRead` + helper readers (the pairing vocabulary) → move to a new `Specs/Raft/AbsTwinRead.lean` | `RoundFam`/`RoundLemmaShape`/glue replays/readouts |
| RoundInduction | — | ALL (the list-form induction = the unrolling shape; superseded by the loop rule; archive-lesson pointer) |
| AllocEq / AllocEqWave1 | the `*_ren` renaming-congruence lemmas AbsStateV2 consumes → fold into AbsStateV2 or a small `RenCongr.lean` | the `_alloc` fixture equations + witnesses |
| DriverNet | the ∀-spans (`rebuildLoop_span`, `liveCountLoop_span`, generic strict-op rules) + the `*_pinned` syntax certificates (legitimate reflection) | — (witness module dies in K-B) |
| NativeCheckerBridge | the fold models + `s1/s23_viol_delta` + interfaces + the four verbatim guard shape-pins | the fire/clean witnesses |
| AbsStateV2 | readers + `_ren` lemmas | `absV2_witness_L4` |

## K-D: infrastructure of the dead — DELETE

- The corpus: `GoLeanProofsCorpus.lean`, `AuditCorpus.lean`, both
  lake targets, `scripts/ci`'s two-closure coverage step (reverts to
  one closure + the existing allowlist), the corpus landmark record.
- Audit pins of killed theorems: `Audit/{Ring, RoundMa, RoundMar,
  RoundVote, RoundVr, RoundIndWitness, RoundInduction}` deleted;
  `Audit/ChoiceInv` pruned to the ChoiceCanon/Inv statement-layer
  pins (seed pins out); `Audit/FrameShape` deleted (its subjects
  die); `Audit/DriverNet` pruned (span pins stay); `Audit/FastEval`
  pruned (`twin_prelude_eq` out; the transfer-theorem pins stay);
  `Audit/CheckerBridge` pruned (witness pins out).
- `tools/campaign/` — the emitters, generators, doctor/prune
  tooling, probes, census scripts: enumerative infrastructure,
  ALL to archive ("tagged to get killed" per the directive).
- `Frame/Shape*` (7 modules, ≈6,000 lines incl. the 2,013-line
  `ShapeStrict` copy): the splice-clause completeness design —
  layout-dependent, refuted at its one chartered use; its only
  consumers die in K-B. DELETE; the layout-free heap-quotient frame
  is the plan's replacement if/when demanded.

## K-E: kept, explicitly, with the reason

- **Trusted base**: GoCore, the frontend, the differential corpus +
  scripts, `FastEval` + transfer/bridge theorems + `fastreplay`
  (certified replay; the differential instrument) — (b).
- **The rule set**: `Frame/` core (Sim/StepSim/Transfer/Relocate/
  Rename/…, minus Shape*), `Sym/` (Mirror/Conc/Refine/TableExt +
  the four transports), `Lens`, `SliceWalk`/`MapLoops`/`StepKit`/
  `FuelMeasure`, the Iris WP layer (Lang/Ghost/Lifting/Laws/*/
  Surface/Adequacy/SurfaceExit) — (b), all ∀-shaped per the census.
- **`Frame/ChoiceCanon` + `ChoiceInv`** (minus witnesses/seed pins):
  general choice-erasure machinery; the statement-former is clean;
  now OFF the critical path (the spec route discharges ∀-stream
  directly) — kept as (b), the optional localization instrument.
- **The abstract layer**: the six native-chain modules +
  NativeCheckerBridge's rule half — (a), the top of the path,
  fully ∀.
- **The pairing vocabulary**: AbsStateV2 + absTwinRead — (a).
- **The statement layer**: WirePin, TwinProgram, RaftAgreement — (a),
  trusted.
- **Branches (unlanded, kept as refs, not merged)**: `campaign-arc4d`
  (the span theorems = wrong-shape evidence + the reusable
  projections `projLOf`/`projBy`/`encGS` and commutations — harvest
  source for the checker specs); the killed workers' partials
  (`campaign-wave-a`, `campaign-ce`) — harvest candidates (wave-a's
  kill commits overlap this list; ce's SpanIso start) noted in
  ARCHIVE.md.

## Net effect

`proofs/` shrinks by ≈1.20M lines (the Raft scope's 97.9% literal +
1.5% trajectory); the default build loses its heaviest modules and
the corpus apparatus; the gate simplifies (one closure). HOLES,
acknowledged and accepted: no seed/base-case theorem, no round-level
results, no non-vacuity witnesses — all superseded by plan items
(init spec, loop theorem, total-statement shape). Some Audit files
and the aggregator shrink accordingly; the statement-TCB gate,
comparator-judge, and the differential are untouched.
