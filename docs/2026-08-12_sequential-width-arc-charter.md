# The sequential-width arc — charter (2026-08-12)

Status: DRAFT for user blessing; QUEUED behind the verified-examples
arc. Governing doctrine: the two bounds; the latitude inventory's
census and priorities are the work list.

## Objective

Answer the WIDTH question for sequential GoLean: make the machine
permissive enough for what Go might do that gc doesn't, at every
sequential latitude point the inventory names — per the doctrine's
default (envelopes argued from the upper-bound evidence classes), with
deterministic re-pins acceptable only as recorded interims.

## Work list (from the inventory, priority order)

1. The three eval-order observed-∉-modeled candidates: inter-target
   operand order, early-store-across-phase, hidden-dep init order
   (interim option: the fail-closed frontend detector, recorded).
2. `[]byte(s)` capacity on gc's escaping path (known outside the model —
   the cheapest genuine observed-∉-modeled fix; append-spill mold).
3. The unrecorded int-width pin: recorded at site with its obligation
   (width envelope is NOT in scope — a 32-bit lane is a future arc).
4. The register's remaining sequential entries per the census, each
   fixed, enveloped, or interim-recorded with reasons.

Each item: probe-backed both bounds, red-first where behavior moves,
membership-lane validation for envelopes, full differential discipline,
re-certification where schedule/choice trees grow.

## The capstone — the re-proof pass

The verified-examples corpus re-proved over the widened machine. Broken
proofs = reasoning-layer gaps under honest width; falsified specs =
specs that had silently baked a pin in (corrected, with the lesson
recorded in the gallery). The broken/falsified list is the arc's most
valuable finding set, not a failure.

## Boundaries

Sequential only — the concurrency re-envelope (the send-then-spin
wedge, C2/C3) remains queued behind this arc; touching the concurrent
machine parks. Must-park otherwise as standard (designation, doctrine,
compat/**, merges/pushes).

## Exit criterion

The inventory's sequential items dispositioned (fixed / enveloped /
interim-recorded); the register updated to the post-arc truth; the
re-proof pass complete with its findings recorded; gates green
(--diff/--slow as touched). Deferral with an honest log is success.
