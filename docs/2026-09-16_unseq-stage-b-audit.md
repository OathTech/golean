# Adversarial audit — Stage B `unseq` scheduler (candidate `core/unseq-scheduler-b-0916` @ `ba8767da`)

**VERDICT: FIX-FIRST** — no wrong answer for any Go program, no unsound proof, no coherence gap; but three FAIL-OPEN paths in the trusted core (`GoLean/GoCore/`) where a hand-built graph that violates the design's own §1/§3.4 contract is executed silently instead of refused by name (F1 silent zero from a SKIPPED producer at completion; F2 a non-frozen slice-variable anchor reproduces the reference's FORBIDDEN hybrid; F3 a non-`$` cell shadows a source local). Each fix is small and merge-invariant-shaped (a rule premise + the arm + its lemma). Whether F1/F2 may instead be RECORDED as Stage C decoder obligations is the user's call (PENDING [USER], §F1/§F2 dispositions).

[AGENT] Auditor, branch `review/unseq-stage-b-0916`, worktree `.claude/worktrees/audit-unseq-stage-b`, checked out at the candidate tip `ba8767da` (main `32398203` + `306fb3ef` core, `c770c59c` tests/lemmas/wiring, `ba8767da` records). Ordered by [USER] Mike 2026-09-16, verbatim, relayed by the [AGENT] coordinator — cite as relayed: «Great, send off an auditor as proposed». Nothing in the candidate or on `main` was edited; scratch lives under `.tmp/audit/` (untracked) and its small sources/outputs are tracked under `docs/evidence/2026-09-16_unseq-stage-b-audit/` (88 KiB, within caps). Every lake/lean command ran through `scripts/capped`; every result below is judged by its CAPTURED exit code.

Bootstrap (all EXIT=0 unless stated): `.lake` plain-copied from the candidate worktree at identical source (`CP_EXIT=0`); `scripts/setup-deps` failed (no network, EXIT=1) → `scripts/setup-deps --from /home/dev/projects/golean/deps` EXIT=0 (goose/raft/go pinned); `scripts/capped lake build` EXIT=0 («Build completed successfully (206 jobs)»); pre-fix binary copied read-only from the primary at `32398203` (sha256 `44c8ed60…`), candidate binary sha256 `62adb8ec…`; `go version go1.26.5 linux/amd64` = `baselines/go-oracle-pin`.

## Findings (severity order: WRONG-ANSWER > UNSOUND-PROOF/COHERENCE-GAP > FAIL-OPEN > RECORDS-CLAIM > SCOPE > PERFORMANCE > NIT)

No WRONG-ANSWER, no UNSOUND-PROOF, no COHERENCE-GAP finding. The first class present is FAIL-OPEN.

### F1 — FAIL-OPEN: a VALUE binder produced by a SKIPPED occurrence is consumed at completion as the zero value, silently (phase-2 store and `thenB`)

- What I did: scratch `AuditA.lean` A4/A5 (evidence `AuditA.lean.txt`, `AuditA.out.txt`). Graph: `R_z`, guard `G` (`$z`, when=false → skip when `z=true`), region {`E_h` (invoke, binds `$h : int`), `C_or`}, target `T` (`.var "out"`), **stores `[("$t", "$h")]`** — the store's value binder is produced ONLY inside the region. A5: no stores, `thenB = println($h)`.
- What happened: `z=true` → `ok: A4 … exact set of 1 member(s)` = output `out 0` (the source variable `out`, initialised 7, is overwritten with the cell's ZERO); control `z=false` → `h ran / out 42`. A5: output `h 0`. No refusal, no name. Contrast A8 (`AuditJ.lean`): a store through a TARGET binder from a skipped region IS refused by name («target binder '$t' has not been produced») — the two sorts are treated asymmetrically.
- Why it matters: design §1 G «a value use of a region-confined binder outside its region is REJECTED — `c` is the only join» and review R2 «Reject a use of a value confined to a skipped branch unless it passes through a valid join». The machine enforces this dynamically for occurrence BODIES (`skippedDep?`, `Unseq.lean:213`) but not for the completion's consumers: `unseqStorePlan` (`Machine.lean:1756`) does `loadLoc` on the cell regardless of the producer's status, and `Step.unseqComplete` (`Machine.lean:5035`) / `stepUnseqNext`'s case (i) (`StepFn.lean:172–173`) have no «every store value produced» premise. Charter doctrine: «unclassified case → an explicit refusal that NAMES ITS CAUSE, never a silent default». The zero-initialised cell is exactly the «absorbing fallback» the doctrine forbids. Reachability today: hand-built graphs only (no decoder emits `Stmt.unseq`); the design assigns the STATIC check to Stage C's decoder («invalid branch join»), but the machine is the trusted surface and already carries the dynamic check for bodies.
- Disposition PROPOSED: fix in a fix round — add `g.stores.all (fun (_, v) => g.produced st v)` as a premise of `Step.unseqComplete` and the matching refusal by name in `stepUnseqNext` (one arm, one lemma line in `stepUnseqNext_sound`/`_consumption_*`/`step_complete*`; `unseq_complete_settled` gains a conjunct). For `thenB`'s direct reads (A5) the machine has no `Stmt` name walk; record that as the Stage C decoder check («`thenB` mentions only cells whose producer is outside every region or is a completion») in the handoff §5 list, which today does not name it. Alternative — PENDING [USER]: accept both as recorded decoder obligations and merge as-is.

### F2 — FAIL-OPEN: the atom grammar admits a NON-frozen target plan (`.ref` of a slice VARIABLE as the chain anchor); it reproduces the reference's FORBIDDEN hybrid

- What I did: `AuditA.lean` C1 — the R4 program (`old := a; a[0] += mut()`, `mut` rebinds `a` to `b`) with the target plan spelled `.target "$t" (.addr (.indexAddr (.ref "a") (.intLit 0)))` — `.ref` is an admitted atom (`unseqAtom`, `Machine.lean:1672`); the candidate's own R4 test spells it `(.var "$hdr")` (`Tests/UnseqScheduler.lean:457`).
- What happened: `ok: C1 … exact set of 2 member(s)` = {`old 10 20 / a 11 200`, `old 10 20 / a 101 200`}. The first is the hybrid `outcomes.txt` FORBIDS for R4 («forbidden hybrid absent: ('old 10 20', 'a 11 200')»): the load read `A[0]=10` through `a`'s header at load time, the store wrote `B[0]=11` through `a`'s REBOUND header at store time — one identity for the read, another for the store. Control C1b (`.var "a"`: the header read at the plan step) gives R4's exact set.
- Why it matters: `resolveChain` (`Machine.lean:1615`) → `indexTargetLoc` (`:227`) on an `.addr loc` anchor whose cell holds a `.slice` LOADS THE CURRENT HEADER at resolve time — the R4 re-read the review named. The candidate's claim (handoff §2 «a `TargetRef` whose anchor is the frozen header VALUE never re-reads the variable (R4)») is true only for the spelling the tests chose; the machine offers no guard against the other spelling, and `unseqReadTarget` (`:1701`) / `unseqStorePlan` will replay it twice against different headers. The design puts freezing on the adapter (§3.4), but the machine can detect the non-frozen shape dynamically and cheaply. `.ref` of an ARRAY variable is a legitimate frozen anchor (arrays do not rebind), so a blanket refusal of `.ref` is wrong; the discriminator is the cell's content at plan time.
- Disposition PROPOSED: fix in a fix round — in `unseqTargetPlan` (`Machine.lean:1724`) after `completeTargetRef`, refuse by name a `.chain (.addr loc) _ (.index :: _)` whose `loadLoc s loc` is a `.slice` («unseq: target plan anchors a slice VARIABLE — the header is not frozen; freeze the header value»); one premise, one `locSup` lemma unchanged (refusals produce no step). Add C1 as a negative test. Alternative — PENDING [USER]: record «never emit `&a` as the anchor of a slice element plan» as a Stage C decoder check and merge as-is (handoff §5 does not list it today).

### F3 — FAIL-OPEN (low): a cell whose name is not `$`-prefixed shadows a source local of the same name for the rest of the enclosing block

- What I did: `AuditA.lean` B4 — cell `a : int`, occurrence `.eval "a" (.intLit 42)`, source local `a = 1`; `thenB = println("then a", a)`, then `println("after a", a)` after the sweep.
- What happened: `ok: B4 … output "then a 42\nafter a 42"` — the source `a` is unreachable for the rest of the block; no refusal. B5 (cell `a` whose head reads `a`) is refused by name («no ready occurrence» — a self-dependency), so the shadowing is only silent when the head does not mention the shadowed name.
- Why it matters: `wellFormed?` (`Unseq.lean:254`) already treats `$` as the frontend's slot RESERVATION for the «unknown slot» check (`:278`) but does not require cells or target binders to carry it, so the `.initialization` idiom (ENTER extends the source scope in place, `StepFn.lean:132–142`) lets a hand-built graph rebind a source name. Same fail-closed gap class as F1: a shape the design excludes, executed instead of refused.
- Disposition PROPOSED: fix in a fix round — `wellFormed?` refuses a cell or target binder not starting with `$` («binder '{n}' is not a reserved `$` slot name»); the tests already use `$` names throughout (one line, no proof impact since refusals produce no step).

### R1 — RECORDS-CLAIM: T1–T6 are inversion lemmas over the rules' own premises; they do not constrain `ready` against the §1 edge table

- What I did: read `GoLean/GoCore/UnseqSound.lean:48–207`. T1 `unseq_pick_ready` is `cases h with | unseqPick hdep hj => List.mem_of_getElem? hj` — the conclusion IS the rule's premise; T2 = `ready_active`; T4 = `unseqComplete`'s premises; T5/T6 = frame-field stability under the five `.next→.next` rules; T3 = `panicUnwind` on an `exprGlue` frame (ONE step over the unchanged state). All 22 exported theorems depend on the classical trio only (`axioms.txt`, EXIT=0; `check-unseq-scheduler` reproduced: «24 required theorems; 14 376 declarations …; classical trio only», EXIT=0).
- Why it matters: the handoff §3 lists T1 as «every edge respected at the pick: value dependencies produced, order prerequisites discharged, region enabled». That is true BY DEFINITION of `readyAt` (`Unseq.lean:190`) — the theorem adds nothing about whether `readyAt` encodes §1's two edge sorts correctly; that weight is carried entirely by the reference-set tests (which do pass, exactly, on 47 checks plus this audit's 12 new graphs). T3's «no later effect of this sweep» is a multi-step claim; the lemma is the one-step frame drop (the handoff's OWED list does say so). None is vacuous: the hypotheses are satisfiable (every rule fires in the tests). A wrong `ready` (e.g. one ignoring `after`) would leave T1–T6 provable unchanged — that is the gap the owed multi-step composition does NOT close either; only the exact-set tests and Stage C's check (b) do.
- Disposition PROPOSED: record — reword handoff §3 T1 to «a pick selects an element of `UnseqGraph.ready` (the inversion of `unseqPick`)» and add one sentence that `ready`'s fidelity to §1 is test-borne (exact sets) pending a machine-checked statement of «trace»; no code change.

### R2 — RECORDS-CLAIM: the reference enumerator under-refuses relative to §1 G on a use confined to a LATER-skipped region; the machine refuses (machine is the design-conformant one)

- What I did: A3 in both `enum_audit.py` and `AuditA.lean`: `X` (in region G2) value-depends on `E_h` (in region G1); both guards skip. Reference: completes, `sink true true` (X is never pending in `enumerate.py:94–130`, so its `deps ∈ skipped` check never runs). Machine: «refused by name (confined to a skipped region)» — `skippedDep?` fires on the ACTIVE `X` as soon as `E_h` is skipped (`Unseq.lean:213–226`; region members start ACTIVE, `initStatus`).
- Why it matters: §1 G's rule is STATIC («a value use of a region-confined binder outside its region is REJECTED»); the machine's earlier dynamic refusal matches it, the reference's laxity does not. Not a machine defect; a divergence between «reference» and «machine» that the evidence README's «machine = reference» framing should name, since check (b) in Stage C compares the two sets.
- Disposition PROPOSED: record in the spike README / handoff (one line); optionally tighten `enumerate.py`'s check to walk `flatten(occs)` rather than `pending` (scratch, not done here).

### R3 — RECORDS-CLAIM: the handoff counts «eleven `Step` rules»; there are ten

- `docs/2026-09-16_unseq-stage-b-handoff.md:35` lists `unseqEnter, unseqPick, unseqComplete, unseqRunEval/Invoke/Load/Target/Guard, unseqValue, unseqStmtDone` = 10; `grep -cE '^\s*\| unseq[A-Za-z]+ \{' Machine.lean` = 11 because it counts the LEGACY `unseqProbe` (`Machine.lean:4987`). The evidence README and the coordinator's brief repeat «eleven».
- Disposition: record (fix the count in the handoff; a NIT-grade records error, listed here because the rule inventory is the coherence claim's denominator).

### R4 — RECORDS-CLAIM: the design note §3.7 names rules (`unseqTargetDone`, `unseqMalformed`) that the candidate does not have; the handoff supersedes it without saying so

- `docs/2026-09-16_evaluation-order-model-v2.md:215–216`: «with `unseqTargetDone` and `unseqMalformed` added to the `Step` rules». The candidate has `unseqRunTarget` (one step, no separate done rule) and NO malformed rule (by design: `Machine.lean` comment «(iii) … has NO rule (a named malformed-graph refusal in `stepFn`)»). The choice is correct (a refusal is not a `Step`), but the design of record now disagrees with the implementation and neither document notes the change.
- Disposition: record — one line in the handoff §2 table («rules: 10, no malformed rule — refusals are not steps; §3.7's two names withdrawn»).

### R5 — RECORDS-CLAIM: the design's headline lowering `x := e ↦ thenB = .initialization x; x = $u` is the stated reason for the source-scope cell idiom, yet no candidate test exercises `.initialization` inside `thenB`

- What I did: `grep -n initialization Tests/UnseqScheduler.lean` → none. `AuditK.lean` K2 (mid-block, two sweeps, `x` and `y` declared by `thenB`, read after) and K3 (in a 2-iteration loop body): `ok: K2 … "x 3\nx y 3 12\n"`, `ok: K3 … "x 3\nx 4\n"` — it works (the `.seq rest env' k'` tail carries the extended env; `seqCont` merges same-env sequences, `Machine.lean:2843`).
- Why it matters: the handoff §2 justifies the [AGENT] representation choice «cells declared into the SOURCE scope» by this lowering alone; the exit evidence should include it (review row C «source declarations survive completion» is Stage C's, but the idiom's precondition is Stage B's).
- Disposition: record + add K2/K3 to `Tests/UnseqScheduler.lean` in the fix round (test only).

### R6 — RECORDS-CLAIM: the evidence tails omit the drift block; «no other row moved» was not verifiable from tracked evidence — the audit re-ran the gate (see §Baseline below for the captured result)

- `docs/evidence/2026-09-16_unseq-stage-b/gate1-tail.txt` and `gate2-tail.txt` carry the summary block only («FAIL baseline diff (DRIFT — see above)»); the drift lines live in the lane worktree's untracked `.tmp/ci-*.log`. `gate2-slow-row.txt` carries the one row. The README's «No other row moved» is therefore an untracked claim.
- Disposition: record — future gate tails should include the drift block (a dozen lines); this audit's own run is the independent confirmation.

### S1 — SCOPE: clean

Every hunk is in Stage B's remit. `CLI.lean` +11 = a docstring row for the accountant list; `ChoiceTrace.lean` = `siteName`/`allSites`/`unseqNextFacts` (required by the `allSites` completeness check and the depth guard's tracer); `Race.lean` +52 = the run-step footprint (required: the pool/CLI path the corpus uses reports `stepAccesses`); `AdmissionIndices.lean` +12 = the type-index walk (the constructor-catch-all discipline); `MultiStreams.lean`/`EnumDedupCheck.lean`/`EnumDedupSound.lean`/`EnumDedup.lean` = the refusal-by-name arms for the new site. No decoder/frontend change (`NativeToIR.lean`, `tools/nativefrontend` untouched — confirmed by `git diff --stat`); no E13 change (K2 below); no B7-style signature rewrite. Legacy `unseqProbe`/`probeK`/`unseqPanic` untouched.

### P1 — PERFORMANCE: budgets reproduced exactly; binder cells are never reclaimed

- Route-β table reproduced (`budgets-reproduced.tsv`): paths = 2^N for N = 1…8 in both variants; step counts IDENTICAL to the README's (printing 205/539/1207/2543/5215/10559/21247/42623; silent 181/467/1039/2183/4471/9047/18199/36503); members 2^N printing, 1 silent; in-process wall N=8: 3 015 ms printing / 2 563 ms silent (README 3 080 / 2 778); `time -v` max RSS 745–749 MB (README 754–764 MB); all EXIT=0. Recursion witness reproduced: `leaves=10000 sites=9414 steps=889276 maxDepth=16` (README identical); `check-unseq-scheduler` 1:13 wall, 1.68 GB max RSS, EXIT=0.
- Note: every ENTER `alloc`s one heap cell per binder and nothing frees them (C4 is the reclamation lane; the handoff says so). Not a Stage B defect; the loop budgets above are small. Recorded as the input to Stage D.

### N1 — NIT: the source-scope idiom leaks cells past a bare `if` branch (as `.initialization` does)

- `AuditA.lean` B3b: `.ifThenElse true (.unseq …) (.seqn #[])` as a statement of a block, then `println($op)` after the `if` → `leaked 5`. The branch runs under the enclosing `.seq` frame with the SAME env, so ENTER extends the enclosing block's scope. Identical to `.initialization`'s behaviour in that position; the frontend emits `.block` for Go blocks, so no Go program reaches it. B3a (bare `while` body) is refused by name («GoCore unseq outside a statement sequence») and B3d (nested `.block`) scopes correctly. Record only.

### N2 — NIT: the whole-sweep migration boundary (design §3.7) is not machine-enforced

- `AuditJ.lean` K1: a `.unseqProbe` statement and a `.unseq` statement in one function, AND a `.unseqProbe` inside `thenB`, are accepted (set = the probe's own two members). The boundary is an emitter rule («one sweep → one `unseq` node or the legacy lowering, never a mixture»); the machine cannot see sweep boundaries. Record that the check is Stage C's emitter/decoder's, not the core's.

### N3 — NIT: `wellFormed?` does not check the guard completion cell's TYPE

- `AuditK.lean` K4: completion cell `$cor : int`; the skip's `storeLoc … (.bool (!w))` is refused («stuck: expected int value, got GoLean.GoValue.bool true») — fail-closed, but at run time with a generic message rather than at ENTER by name. One line in `wellFormed?` («guard '{o.name}' completion '{out}' is not a bool cell»).

## The audit items, one by one

- (a) `ready` vs the two edge sorts (`Unseq.lean:157–204`): value deps = `mentions.filter isCell ++ targetMentions`, satisfied by `produced` = producer DONE; order prereqs = `after`, satisfied by `discharged` = DONE **or SKIPPED**; region = guard DONE. A1 (order prereq on the skipped region EVENT, not the completion): machine {`guard k / result true 7`} = reference (both z). Value dep on a skipped producer inside a body → refused (R2a INVALID, candidate test). A skipped branch never «produces» through `produced`, BUT its zero-initialised cell is readable at completion — F1. A3 — R2.
- (b) The idiom copies `.initialization`'s arm exactly (`StepFn.lean:310–318` vs `:132–144`): same `kenv = env` check, same `.internal`/`.stuck` texts. Enforced by refusal (B3a), not assumed. Two sweeps with identical cell names shadow (B1, `LocalEnv.declare` prepends — `State.lean:136`); the leak past a bare `if` is N1; a nested `.block` scopes (B3d); the loop re-enters fresh (W5, K3). Shadowing a SOURCE local — F3. Recursion: per-activation (candidate test + I4).
- (c) Atoms (`unseqAtom`, `Machine.lean:1672`): `.var`, `.ref`, int/bool constants; a non-atom operand is refused by name (C2). Heads and invoke arguments are NOT normal-form-checked: C4 (`a[b[0]] / d` in one `eval`; `a[0]` inside an invoke argument) runs — this is the design's stated decoder obligation (§3.1 «hidden read in a pure node»), listed in handoff §5; recorded, no finding. Frozen identity: F2 (`.ref` slice-variable anchor). Pointer redirection (candidate `ptr` test) and cell mutation (`cell`) pass; map plans refused by name. Phase-2 checks stay in phase 2: C3 (`a[0], a[9] = 1, 2` on `len 1`) — the deferred print shows `a0 1` landed before `index out of range [9] with length 1` fired at the second store.
- (d) Positional tags: the diff's renames are all consistent with the stated map (+1 at ≥66, +2 at ≥137, +3 at ≥152): `69→70, 77→78, 78→79, 79→80, 80→81, 83→84, 86→87, 87→88, …, 143→145, 145→147, 148→150, 149→151, 153→156, 154→157`; the three NEW cases sit at 66/138/154 and are proved by the arm lemmas (`stepUnseqEnter_sound`, `stepUnseqValue_sound`, `stepUnseqNext_sound`, `MachineSound.lean:18–119`). Structurally, a mis-attached tag cannot yield an unsound proof of `stepFn_sound`: its statement is unchanged and every case must close the goal for the arm the tag actually names — a wrong attachment breaks the build, it does not prove a different theorem. The build is green (`lake build` EXIT=0, no `sorry`). I did NOT re-run a `fun_cases` probe (cost); listed under «not checked».
- (e) `seqConsumption`'s arm (`Machine.lean:4244`) reports `(.unseqNext, |ready|)` iff `2 ≤ |ready|`, else `none`; `consumesUnseqNext` is conservatively `true` at every `.pick` (documented). Exactness against the actual pop is proved per step (`stepUnseqNext_consumption_some/none`, `MachineSound.lean:214–297`) and checked at run time on EVERY path of every test by the explorer's sentinel accountant (`CLI.lean:1366` fails the enumeration on drift; `poolConsumption` delegates to `seqConsumption` on the cell path, `Multi.lean:1568/1601`) — 47 candidate checks + 12 audit graphs, no drift. Singleton/empty: J1 (forced 4-chain, tape `[3,3,3]` untouched), C1/C2 (candidate), J2 (exactly one width-2 pick consumes exactly one entry at the pick: `[1,7,8] → B first, leftover [7,8]`; `[0,7,8] → A first, leftover [7,8]`; `[]` → canonical). Dedup: `innerVecs → none`, `poolThreadOblivious → false`, `refusalReason` names the site (diff read); not exercised at run time here (the tests use the default explorer).
- (f) Coherence: for each of the 10 rules I matched the `stepFn` arm's successful result (read side by side, `StepFn.lean:132–226` vs `Machine.lean:5021–5083`): ENTER (same `kenv = env`, `wellFormed? = none`, `allocDecls`), PICK (`skippedDep? = none`, `ready[pick]? = some i`; `j` free in the rule = `consumeAt`'s pick in the arm, realised by tape `[j]` — `consumeAt_fst_singleton`), COMPLETE (`skippedDep? = none`, `allSettled`, `unseqStorePlan`), RUN ×5, VALUE, STMT-DONE. No rule is more permissive than its arm or vice versa; case (iii) has no rule and the arm refuses. `step_complete` and `step_complete_any_wf_aux` have a named case per rule (`MachineSound.lean:485–529`, `540–571`). `step_preserves_wf_loc` names 8 cases; `unseqPick`/`unseqStmtDone` fall to the generic closer (state and frame payload unchanged except a status list, loc-free). What StateWf constrains: the frame's `locSup` covers graph, `thenB`, the TARGET table (frozen anchors are `.addr loc` — bounded), env and tail (`StateWf.lean:388–392`); statuses/phase are loc-free. It does NOT state `st.length = g.occs.length` (T6 takes it as a hypothesis; maintained by `initStatus`/`set`/`skipRegion_length`) — an invariant the owed multi-step composition will need in the wf predicate; recorded, not a defect.
- (g) Totality: the diff's added lines contain no `partial`/`sorry`/`admit`/`axiom`/`native_decide`/`unsafe`/`implemented_by` (grep over `GoLean` and `Tests` hunks: only the audit's own error-message string). All recursion structural (`Expr.names` mutual, `unseqLookupTarget`/`unseqAtoms`/`unseqStorePlan` on lists) or bounded (`skipRegion` = `foldl` over `range |occs|`). `#print axioms` on all 22 exported theorems: `[propext, Classical.choice, Quot.sound]` or a subset (`axioms.txt`, EXIT=0).
- (h) R1 above. The owed multi-step gap: a wrong scheduler that respects the rules' shapes but has a wrong `ready` passes T1–T6; what catches it is the exact-set comparison (Stage B's) and check (b) (Stage C's). A scheduler bug that violates the frame's record across a body's internal steps would be caught by neither T-lemma today (they are stated over the scheduler's own steps only) — exactly the handoff's owed item; no evidence here of such a bug (12 new graphs exact).
- (i) Table below. (j) Table below.
- (k) The mixture is accepted (N2). E13 rows: six functions of `Corpus/coverage/exec/builtins/e13-sibling-panic-order` (`indexMiddle`, `assertMiddle`, `twoIndexLeftCall`, `indexLeftCallArgPanics`, `indexLeftCall`, `assertVsMutatingCall`), one wire (`go run ./tools/nativefrontend`, 52 `unseq-probe` nodes), `coverage-observations --max-width 2 --max-sites 8 --cap 64 --work-cap 200000 --expect-status panic` on the pre-fix and the candidate binary: BYTE-IDENTICAL outputs and exit codes for all six (`e13-prefix-vs-candidate.txt`).
- (l) Structural argument: no wire file, no decoder change → no corpus program can contain `Stmt.unseq`; the only shared-code changes visible to corpus programs are the new `ChoiceSite` constructor (enumeration lists), `consumesUnseqNext` (false on every non-`unseqK` configuration), and the `Cont`/`Stmt` catch-all walks (`locSup`, `eqbF`, indices) — none alters an existing arm. Confirmed by (k). The audit's own `scripts/capped scripts/ci --diff` under the box-wide lock: see §Baseline.
- (m) P1 above.
- (n) Proved-vs-owed list = the 24 `exports` of `Tests/UnseqSchedulerAudit.lean` exactly; census row (`docs/2026-08-11_latitude-inventory.md:121`) and doctrine mirror entry match `State.lean`'s site docstring and `seqConsumption`'s bound; evidence README exit codes (gate 1 EXIT=1, gate 2 EXIT=1, unseq gate EXIT=0) match the tails (and the gate step reproduced here); every [AGENT] choice in handoff §2 has an alternative named (11 rows). Records errors: R3, R4; records gaps: R1, R5, R6.
- (o) S1 above.
- (p) F1–F3, N3, and: `unseqEnter` uses `.internal` for the foreign-scope shape exactly as `.initialization` does — consistent, not a finding.

## Table (i) — new graphs, machine vs reference (all exact; `AuditI.out.txt`, `enum_audit.out.txt`)

| graph | reference set (`enum_audit.py`) | machine set | leaves / sites / steps |
|---|---|---|---|
| I1 three unordered events, B panics `boom B` | 5 panic members by prefix: {`B`, `A B`, `C B`, `A C B`, `C A B`} | = (exact 5) | 7 / 5 / 312 |
| I2 `v := y + b2i(z ‖ h()) + k()`, h: y+=10, k: y+=100, z=false | {1, 11, 111} | = | 63 / 56 / 5561 |
| I2 … z=true | {1, 101} | = | 35 / 30 / 2498 |
| I3 `a[b[0]] += f()`, f rewrites `b[0]:=1`, `a[1]:=50`, returns 1000 (index operand a checked read) | {`a 1010 50`, `a 10 1050`} | = | 10 / 8 / 887 |
| I4 `sum(n)=g(n)+sum(n-1)`, g prints, E1 g→rec, n=3 | {`g 3 g 2 g 1`, 6} | = | 1000 / 926 / 86 812 |
| I4b same, g UNORDERED vs the recursive call (stress shape) | 4 outputs {`g1 g2 g3`, `g2 g1 g3`, `g3 g1 g2`, `g3 g2 g1`} × 6 | = (exact 4) | 3375 / 3108 / 307 914 |
| A1 `sink(z ‖ h(), k())`, k AFTER the region event h (order edge on a skipped event), z=true / false | {`guard k / result true 7`} / {`guard h / guard k / result true 7`} | = / = | 1/0/96 · 2/1/183 |
| A3 use confined to a later-skipped region (R2) | reference COMPLETES `sink true true` | machine REFUSES by name | — |

## Table (j) — singleton non-consumption (`AuditJ.out.txt`, `AuditA.out.txt`)

| program | tape | member | leftover | note |
|---|---|---|---|---|
| J1 forced 4-chain `D = g(g(g(1)))+1` (every edge forced; zero genuine choice) | `[3,3,3]` | ok, z=5, output `g 1 / g 2 / g 3` | `[3,3,3]` (untouched) | explore: leaves=1 sites=0 |
| J1 straight-line control (`callValue` ×3, no `unseq`) | — | ok, z=5, output `g 1 / g 2 / g 3` | — | identical observation (steps 124 vs 140) |
| J2 exactly one width-2 pick (A ∥ B, then Op) | `[1,7,8]` | `B / A` | `[7,8]` | one entry consumed, at the pick |
| J2 | `[0,7,8]` | `A / B` | `[7,8]` | |
| J2 | `[]` | `A / B` (canonical) | `[]` | exhausted stream = slot 0 |
| C1/C2 (candidate tests) | `[5,6]` / `[7]` | ok 8 / ok 3 | `[5,6]` / `[7]` | reproduced in the gate step |

## Baseline (item l) — the audit's own `scripts/ci --diff`

Run because the tracked tails omit the drift block (R6). `scripts/capped scripts/ci --diff` on this branch at `ba8767da`, under the box-wide build lock (`mkdir artifacts/build-lock.d`, owner file, trap-protected release; acquired 2026-09-16T19:24:36Z, released at exit, confirmed absent afterwards): **EXIT=1, 1013 s wall** (`ci-diff-drift.txt`, verbatim blocks). `differential coverage summary: cases=3676 pass=3427 fail=249` — the lane's numbers exactly. The drift block is ONE line:

    DRIFT vs baselines/native-full.tsv (3676 case(s) run):
    imported-goose/channel/google-search	baseline[PASS/membership] -> now[FAIL/membership]

and the row's own reason is `certification: STALE certification: changed dependency build/files/GoLean/CLI.lean` — the 5a-class provenance staleness the handoff §7 names as OWED to the merge train, not an observation change. Every other summary line is `ok` (the new `unseq scheduler (Stage B)` step included); the two `FAIL`s are `certificate provenance` and `baseline diff (DRIFT — see above)`, as the lane reported. This confirms «no other row moved» for the full corpus and item (k)'s byte-identical E13 sets. Not re-run here: `--slow` (the fresh re-enumeration of the certified row); the tracked `gate2-slow-row.txt` («Fresh certification: unchanged set; seconds=153.901») is the only evidence that the certified SET is identical, and its six-member count was not independently recounted (see «not checked»).

## What I did NOT check

- A `fun_cases stepFn` probe to re-derive the positional case numbering (cost; the structural argument in (d) stands: a mis-attached tag breaks the build, it cannot produce an unsound proof of an unchanged statement).
- The dedup engine's refusal at RUN time (`EnumDedup`/`--engine`); read in the diff only.
- `ci --slow` (the tier=slow row's fresh re-enumeration); I rely on the tracked `gate2-slow-row.txt` («Fresh certification: unchanged set») for the six-member equality — the certified record's member COUNT was not independently recounted.
- The race footprint's completeness for the new steps beyond a read of `unseqRunAccesses` (no racy `unseq` program was built; the completion step's reads of the binder cells are unreported, which is harmless because cells are scheduler-private).
- Any Go-level oracle draw: Stage B has no lowering; nothing here is a differential claim.
- `lean_verify`/LSP tooling was not used; `#print axioms` on a scratch file and the candidate's own post-import audit were.
